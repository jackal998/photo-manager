# Changelog

All notable changes to photo-manager are recorded here. From the adoption of news fragments (issue [#274](https://github.com/jackal998/photo-manager/issues/274)) onward, the file is generated by [`towncrier`](https://towncrier.readthedocs.io/) from per-PR fragments under [`news/`](news/) — see [`CONTRIBUTING.md`](CONTRIBUTING.md#adding-a-news-fragment-for-your-pr).

Format: [Keep a Changelog](https://keepachangelog.com/) style, sections per [Semantic Versioning](https://semver.org/) categories.

<!-- towncrier release notes start -->

## [0.2.0-rc2] - 2026-09-26

### Features

- Add `/pr-review` skill for catching semantic drift between code, `docs/features.md`, and `qa/scenarios/sNN_*.py` that the file-touch hooks cannot see (#272). ([#272](https://github.com/jackal998/photo-manager/issues/272))
- Add `permissions.deny` block to `settings.json.example` (sudo, pipe-to-shell, `rm -rf /`) and add Gate 6 to `/pr-review` routing harness/config changes to `/security-scan` (follow-up to #272). ([#288](https://github.com/jackal998/photo-manager/issues/288))
- Extend `/pr-review` with Gates 7-10 (app-level security pattern scan, SQLite migration safety, performance/threading review, test-padding detection) — follow-up to #272. ([#289](https://github.com/jackal998/photo-manager/issues/289))
- Add `/pr-review` Gate 11 (PII audit on project skills) — pattern-scans added/modified lines in `.claude/skills/<name>/` for home paths, IPv4, credential-shaped literals, with a critical filter rule that distinguishes pattern descriptions from literal values. ([#294](https://github.com/jackal998/photo-manager/issues/294))
- Extend `permissions.deny` in `settings.json.example` with `ssh *`, `chmod 777*`, and `* > /dev/*` (AgentShield MEDIUM follow-up to #288). ([#303](https://github.com/jackal998/photo-manager/issues/303))
- Render Similarity column % against the displayed Ref winner (recomputed at render time from each row's pHash), preserving #241's score-aware Ref pick and removing the "X% similar to *what?*" ambiguity that surfaced when the scanner's anchor diverged from the displayed Ref (#253). ([#311](https://github.com/jackal998/photo-manager/issues/311))
- Add Agent Teams enablement harness — three `.claude/agents/` definitions (`docs-reviewer`, `app-security-reviewer`, `quality-reviewer`), three `scripts/hooks/team_*.py` event guards (task-created allowlist, task-completed empty-findings reject, teammate-idle warn logger), and `CLAUDE.md` "Team mode discipline" section — opt-in per `/pr-review` invocation, LEAD-only push/PR/issue creation enforced via per-teammate permission constraints. ([#328](https://github.com/jackal998/photo-manager/issues/328))
- Wire team-mode opt-in branch into `/pr-review` skill — `/pr-review team` spawns `docs-reviewer` (Gates 2+3), `app-security-reviewer` (Gate 7), and `quality-reviewer` (Gates 8+9+10) as parallel teammates; LEAD retains Gates 0, 1, 4, 5, 6, 11 and the post-back; auto-declines on ≤5 behaviour-bearing files or ≤300 diff lines. ([#329](https://github.com/jackal998/photo-manager/issues/329))
- Add `/agentic-engineering` project skill — doc-only reference covering Agent Teams vs subagents decision heuristic, verified teammate lifecycle on Windows CLI, /pr-review team-mode integration summary, hook wiring guide, and known limits as of 2026-05-20. ([#330](https://github.com/jackal998/photo-manager/issues/330))
- Add `researcher-agent` — read-only 3-angle investigator (impact map, history, test coverage) that spawns parallel sub-investigations and returns a structured research brief with complexity score and recommended workflow; first component of the `/work` meta-orchestrator pipeline. ([#331](https://github.com/jackal998/photo-manager/issues/331))
- Add `/work` universal task entry point — accepts any trigger (issue number, free text, branch name, or nothing), spawns researcher-agent for 3-angle investigation, presents complexity-scored plan for human approval, then routes to the right coordination mode (single-session, dev subagent with worktree isolation, or /pr-review team). ([#332](https://github.com/jackal998/photo-manager/issues/332))
- Add `developer-agent` and `qa-agent` — worktree-isolated implementation agent (Write/Edit/Bash, self-corrects up to 2 test-failure rounds, returns structured completion report) and read-only validation agent (runs targeted + full test suite, checks acceptance criteria and coverage gaps, returns PASS/FAIL with actionable details for next dev iteration). ([#333](https://github.com/jackal998/photo-manager/issues/333))
- Wire /work execution layer — replace generic subagent placeholders with named developer-agent (worktree-isolated implementation) and qa-agent (acceptance + coverage validation), completing the research → develop → QA → /pr-review team pipeline. ([#334](https://github.com/jackal998/photo-manager/issues/334))
- Mark auto-selected keepers as locked, with optional aggressive delete-all-others mode (#398). ([#398](https://github.com/jackal998/photo-manager/issues/398))
- Show ActionDialog Simple and Regex sections in a single dual-section view, dropping the mode toggle (#402). ([#402](https://github.com/jackal998/photo-manager/issues/402))
- Add opt-in `ui.action_dialog.window_modality` setting (`"window"`) for cross-window interaction while the Set Action dialog is open; default stays `ApplicationModal` (#382). ([#405](https://github.com/jackal998/photo-manager/issues/405))
- Add **Action → Execute Action (only selected)…** menu entry to pre-filter the Execute dialog to the file rows currently highlighted in the main tree, replacing the in-dialog highlight-then-execute scope path (#410). ([#414](https://github.com/jackal998/photo-manager/issues/414))
- Standalone Windows .exe via PyInstaller — tag-push release pipeline, bundled HEIC + RAW codecs. ([#420](https://github.com/jackal998/photo-manager/issues/420))
- Right-click → "Execute Action (only selected)…" now mirrors the Action-menu entry, inheriting #430's group-membership scope. (#429) ([#438](https://github.com/jackal998/photo-manager/issues/438))
- Offer to prune singleton groups after destructive operations with a remembered preference (always / ask / never). (#426) ([#440](https://github.com/jackal998/photo-manager/issues/440))
- Scan dialog now shows a stage label, progress bar, and live throughput / ETA so slow NAS scans no longer look hung. (#424) ([#442](https://github.com/jackal998/photo-manager/issues/442))
- Add Scan dialog "Hash workers" spinbox (1-32, default 8) with NAS-aware auto-default — 8 for network sources, min(4, cpu_count()) for local; persists to scan.workers (#449). ([#454](https://github.com/jackal998/photo-manager/issues/454))
- Walking-stage progress now shows a live per-file count instead of jumping at source boundaries — single-source NAS scans no longer appear frozen for minutes (#448). ([#455](https://github.com/jackal998/photo-manager/issues/455))
- Walk multiple scan sources in parallel — multi-NAS scan wall time drops from sum-of-walks to max-of-walks (#452). ([#456](https://github.com/jackal998/photo-manager/issues/456))
- Pipeline hash → exiftool stages via a producer-consumer queue so the two stages overlap; total NAS scan wall time drops by ≈ min(hash, exif) (#450). ([#457](https://github.com/jackal998/photo-manager/issues/457))
- Run N exiftool processes in parallel during the EXIF stage (default 2, configurable via scan.exif_workers, capped at min(4, cpu//2)) — typical 10k-file scans drop from 30-90s to 10-25s on the EXIF pass (#451). ([#458](https://github.com/jackal998/photo-manager/issues/458))
- Add "Execute selected" partial-execute affordance to the Execute Action dialog, and classify singletons-with-pending-actions as a separate opt-in bucket in the post-destructive prune confirm so users explicitly see and choose for non-executed rows (#485). ([#485](https://github.com/jackal998/photo-manager/issues/485))
- Add a ``scan.hash_pool`` setting (``"thread"`` default | ``"process"``) that runs the HASH stage across a ProcessPoolExecutor to escape the GIL on CPU-bound hashing; default behaviour is unchanged and the knob is settings-only for now (#486). ([#497](https://github.com/jackal998/photo-manager/issues/497))
- Add ``scan.hash_pool="auto"`` which times a sample of the real scan data through both the thread and process executors at scan start and runs the faster for the full HASH stage (in-situ calibration; logs both timings and the pick) (#486). ([#498](https://github.com/jackal998/photo-manager/issues/498))
- Cache the ``scan.hash_pool="auto"`` calibration in ``settings.json`` keyed by a machine+sources fingerprint, so a re-scan of the same library reuses the measured rates (re-projected to the current file count) instead of re-running the ~2s calibration every time (#486). ([#501](https://github.com/jackal998/photo-manager/issues/501))
- Add a "Re-calibrate hash pool on next scan" checkbox to the scan dialog's Advanced Settings: it forces a fresh thread-vs-process calibration, switches the hash pool to auto, and auto-unchecks itself — with a modal offering calibrate-now vs the safe thread default the first time auto runs on a new machine + folder set (#486). ([#503](https://github.com/jackal998/photo-manager/issues/503))
- Add a type-filter dropdown to the Execute Action review dialog. Three options ("All decisions", "Delete only", "Remove from list only") narrow the tree, summary, warning banner, and Execute commit scope — mirroring the "visible = committed" contract established by #410 / #485. A new banner line surfaces hidden delete decisions when the filter is set to a non-delete view, so switching to "Remove only" while pending deletes exist no longer visually clears the destructive-state warning (#502). ([#504](https://github.com/jackal998/photo-manager/issues/504))
- Confirm near-duplicate matches with a second perceptual hash (dHash) and keep aggressive auto-select from auto-deleting low-confidence, pHash-only matches (#519). ([#519](https://github.com/jackal998/photo-manager/issues/519))
- Add a dHash confidence-threshold slider below pHash in scan Advanced Settings, and restore full word-wrapped setting descriptions (fixes Chinese text overflowing one line) (#521). ([#521](https://github.com/jackal998/photo-manager/issues/521))
- Stop the main result tree from auto-scrolling sideways to the clicked column on row selection, so the horizontal scroll position stays where you left it (#527). ([#527](https://github.com/jackal998/photo-manager/issues/527))
- Scan **grouping** now uses a hand-rolled BK-tree (Hamming metric) for near-duplicate candidate search instead of the O(N²) all-pairs pHash scan — on a ~16k-candidate library that cuts the dominant ~128M pairwise comparisons to roughly O(N log N), so the grouping stage no longer runs for minutes. The grouping verdict is unchanged: every gate (mean-color #462, dHash #524, source priority) still runs per surviving pair, so output is bit-identical to the previous brute-force pass (proven by `TestBKTreeParity`). ([#526](https://github.com/jackal998/photo-manager/issues/526)) ([#529](https://github.com/jackal998/photo-manager/issues/529))
- Folded the near-duplicate **grouping** stage into the #486 auto-pool calibration (completes #526). The scan now measures the grouping micro-rates (brute per-pair vs BK-tree per-candidate) alongside the hash thread/process timing, caches them under the same per-machine fingerprint, and derives the BK-tree crossover floor empirically instead of from a baked constant — so the grouping strategy self-tunes if a future hash recipe shifts the economics. The calibration fingerprint now also folds in `HASH_RECIPE_VERSION` (`scanner/hasher.py`) and `GROUPING_STRATEGY_VERSION` (`scanner/dedup.py`), so a cached calibration is correctly invalidated when the hash recipe or grouping strategy changes — closing the #517 breadcrumb where adding dHash to the hash tuple never invalidated the cache. ([#526](https://github.com/jackal998/photo-manager/issues/526)) ([#530](https://github.com/jackal998/photo-manager/issues/530))
- In a duplicate group, a non-reference image that was pulled in indirectly (a "passenger") now shows its similarity to the group's reference with a trailing star (e.g. 75*%) — the same reference every other row is measured against, so the Similarity column is comparable across rows — and hovering reveals the file it is actually most similar to ("97% similar to <name>"). The bare "—" is now reserved for a passenger with no comparable hash (a Live Photo's video) (#536). ([#545](https://github.com/jackal998/photo-manager/issues/545))
- Hash-pool thread/process calibration now runs automatically on every scan (reusing a cached result when available); the Advanced-Settings "Re-calibrate hash pool on scan" toggle was removed (#560). ([#567](https://github.com/jackal998/photo-manager/issues/567))
- Add the read-knee probe's pure decision layer (`knee_from_throughput` + the `ReadKneeRamp` state machine) for the #551 in-pipeline reader-concurrency tuner, and remove the now-orphaned #577 occupancy probe — Phase 1, default-off and unwired. (#579) ([#579](https://github.com/jackal998/photo-manager/issues/579))
- Wire the #551 in-pipeline read-knee ramp into the scan worker (Phase 2): a per-device semaphore caps active reads and ramps to a measured files/s knee, behind a default-off setting with no user-facing surface yet. (#580) ([#580](https://github.com/jackal998/photo-manager/issues/580))
- Add a "Auto-tune reader concurrency (experimental)" opt-in toggle to the Scan dialog's Advanced Settings (#551 Phase 3): when enabled the scan measures each device's read-concurrency knee at scan start instead of a fixed guess, caches it per device, and reuses it on later scans — default off, never changes which duplicates are found. (#581) ([#581](https://github.com/jackal998/photo-manager/issues/581))
- Enable read-knee autotune by default (#551 Phase 4) — every scan now measures each device's best read concurrency automatically (opt-out via the Scan dialog), instead of the static NAS=8 / HDD=1 / else guess. (#585) ([#585](https://github.com/jackal998/photo-manager/issues/585))
- Unify delete / remove-from-list / singleton-prune into a single manifest `outcome` state, and confirm locked items before a singleton-prune removes them. (#584) ([#588](https://github.com/jackal998/photo-manager/issues/588))
- Preview pane redesigned for dedup workflow — viewport-bounded byte-budget LRU (256 MB total, scaled), DNG embedded JPEG fast path, autoplay killed, double-click opens a modal full-resolution viewer with pan/zoom. Closes the ~35 GB RAM ceiling reported in #621 (#622 Phase 1). ([#624](https://github.com/jackal998/photo-manager/issues/624))
- Main tree gains keyboard shortcuts — `d` marks selected file rows for delete; `k` clears the action. Tree-scoped, multi-select batched, context menu now displays the accelerator hint (#615). ([#625](https://github.com/jackal998/photo-manager/issues/625))
- Memory probe (`scripts/memory_probe.py`) shipped as a permanent regression guard — env-gated (`PHOTO_MANAGER_MEMORY_PROBE=1`), default off, zero overhead on normal launch. Collects RSS / tracemalloc / Qt heap counters at 5 measurement points around DB load; usage in `docs/audits/memory-probe.md` (#627 from #614 follow-up). ([#627](https://github.com/jackal998/photo-manager/issues/627))
- Set Action dialog — Simple section now exposes empty-value selection: pick "Exactly matches" and leave the text input blank to target rows whose field is empty (e.g. rows with no Action set). A tooltip on the text input surfaces the affordance (#633). ([#633](https://github.com/jackal998/photo-manager/issues/633))
- Remove-from-list is now snappy. All "Remove from list" gestures (right-click main tree, List menu, Execute Action accept, singleton prune) use an incremental tree update via the new `TreeController.remove_rows` method instead of rebuilding the entire `QStandardItemModel` — ~170k QStandardItem allocations per remove on a 13k-row manifest are now avoided. Selection and expanded-group state survive the remove (#634, closes #630). ([#634](https://github.com/jackal998/photo-manager/issues/634))
- Bare 'p' on the main tree now toggles play/pause on the single-view video player. PR #624 killed video autoplay; this is the no-mouse path to control playback while reviewing on the keyboard. Grid mode is a silent no-op since there is no unambiguous "focused" player (#635). ([#635](https://github.com/jackal998/photo-manager/issues/635))
- Set Action regex no longer pays for a discarded incremental cell update before the unavoidable full tree rebuild — N wasted `setData` calls per Apply on a large manifest are now skipped (#636, closes #629). ([#636](https://github.com/jackal998/photo-manager/issues/636))
- Cut over to the web client: a local FastAPI service plus a React UI in a pywebview window replace the retired PySide6 desktop app, bringing the Daylight review-screen layout, decision controls, keep-best with undo, an Execute confirm with delete totals, and the zh-TW catalogue (#647). ([#640](https://github.com/jackal998/photo-manager/issues/640))
- Add the headless review backend for the localhost web port: load a manifest into duplicate groups, set per-file decisions and lock state, and browse folders via /api/manifest, /api/decision, /api/lock, /api/settings, and /api/fs/browse. (#659) ([#659](https://github.com/jackal998/photo-manager/issues/659))
- Add the localhost web review UI: a scan dialog with live SSE progress, a virtualized grouped result tree with thumbnails and per-file decision/lock controls, and manifest loading over the Phase 2B1 API. (#660) ([#660](https://github.com/jackal998/photo-manager/issues/660))
- Add the localhost web "act on decisions" backend: execute (send2trash with per-file crash-consistent outcome writes), remove, prune, save, and reveal routes, plus a full-resolution image OOM guard. Every deleted path is validated against the manifest's allowed roots. (#663) ([#663](https://github.com/jackal998/photo-manager/issues/663))
- Add the localhost web "act on decisions" UI: an Execute dialog (type filter, all-delete banner, virtualized tree, Execute/Execute-selected), a persistent preview pane, a full-resolution viewer with a too-large fallback, a right-click context menu, and lock/delete/prune confirm dialogs — all over the Phase 2C1 routes with the same lock-confirm and singleton-prune gates as the desktop app. (#664) ([#664](https://github.com/jackal998/photo-manager/issues/664))
- Serve the localhost web app's built frontend bundle (SPA static mount) and stand up the Playwright DOM-probe QA foundation — a real-browser testid-drift gate running in CI. (#666) ([#666](https://github.com/jackal998/photo-manager/issues/666))
- Add a working web language toggle (EN ↔ 繁體中文) backed by a new GET /api/i18n catalog endpoint, with locale persistence via /api/settings and the ported s22/s58 language scenarios. (#668) ([#668](https://github.com/jackal998/photo-manager/issues/668))
- Add POST /api/action/bulk-decide — server-side resolution of the Set-Action-by-regex pseudo-patterns (plain regex, numeric threshold, top-N) into decisions and lock/unlock, with a preview dry-run and an allowed-roots guard. (#670) ([#670](https://github.com/jackal998/photo-manager/issues/670))
- Add the web "Set Action by Field" dialog — bulk-apply a decision or lock/unlock to manifest rows by regex, simple text match, or numeric threshold / top-N, with a live server-side preview and a locked-rows confirm. (#671) ([#671](https://github.com/jackal998/photo-manager/issues/671))
- Add a filesystem browse picker to the scan dialog, a File/Action/View menu bar, and empty-state Scan/Open actions to the web UI (#672) ([#672](https://github.com/jackal998/photo-manager/issues/672))
- Add an "Apply to Unlocked Only" verdict to the web Set-Action lock-conflict dialog, so a bulk decision can skip locked rows (matching the desktop) instead of forcing unlock-all-or-cancel. (#674) ([#683](https://github.com/jackal998/photo-manager/issues/683))
- Add a ScanDialog "Advanced settings" section to the web app — auto-select after scan, the aggressive "mark all others for delete" sub-option, and the default-on auto-tune reader-concurrency toggle. (#691) ([#691](https://github.com/jackal998/photo-manager/issues/691))
- Add a right-click context menu to the web Execute Action dialog rows — set action (delete/keep), remove from list, lock/unlock, and set action by field (#693) ([#693](https://github.com/jackal998/photo-manager/issues/693))
- Add Ctrl/Shift+click multi-selection to the web result tree, with the row context menu acting on the whole selection (#695) ([#695](https://github.com/jackal998/photo-manager/issues/695))
- Add a web "Execute (only selected)" action that scopes the execute dialog to the selected rows' groups (#430 group-pull), gated on a non-empty main result-tree selection (#696) ([#696](https://github.com/jackal998/photo-manager/issues/696))
- Persist the web Scan dialog's source list and output path across launches — loaded on open, saved on Start Scan (#698) ([#698](https://github.com/jackal998/photo-manager/issues/698))
- Add sortable + resizable, persisted columns to the web result tree — click File Name / Size headers to sort (per group, in-session), drag column edges to resize (restored across launches via localStorage) (#700) ([#700](https://github.com/jackal998/photo-manager/issues/700))
- Port the singleton-prune flow to the web app — after a destructive op leaves single-item groups, the 3-value `ui.prune_singletons` preference (ask / always / never) drives a mixed plain/actioned bucket dialog with an opt-in checkbox and the D6 locked-singleton lock gate, matching the desktop behaviour (#701) ([#701](https://github.com/jackal998/photo-manager/issues/701))
- Port the scan close-guard to the web app — a `beforeunload` handler warns before the tab unloads while a scan is running (the web analog of the desktop "Scan in progress" close prompt), and the s63 late-cancel + close-guard scenario is now covered in the web QA batch (#704) ([#704](https://github.com/jackal998/photo-manager/issues/704))
- Disable the web Set Action dialog's Simple write-through inputs and show an explanatory note when a scan produces zero duplicate groups, matching the desktop no-match_fn placeholder; the Regex input stays interactive (#689) ([#706](https://github.com/jackal998/photo-manager/issues/706))
- Cancel the in-progress scan worker when the user leaves the web app mid-scan (best-effort pagehide beacon), so a leftover scan no longer 409-blocks the next one; ports the desktop close-guard's "Yes = cancel + close" branch (#703) ([#707](https://github.com/jackal998/photo-manager/issues/707))
- Add bare `d` / `k` decision keyboard shortcuts to the web result tree — `d` marks the selected rows for deletion, `k` clears the decision — porting the desktop shortcuts (#708) ([#710](https://github.com/jackal998/photo-manager/issues/710))
- Highlight and scroll to the auto-selected keeper rows in the web result tree after an "Auto select after scan" run, matching the desktop post-scan selection (#692) ([#715](https://github.com/jackal998/photo-manager/issues/715))
- Add Ctrl/Cmd+click toggle and Shift+click range multi-select to the web Execute dialog's tree, so "Execute selected" can act on several rows at once (#697) ([#716](https://github.com/jackal998/photo-manager/issues/716))
- Surface manifest errors (failed load / decision / lock / prune / save) as a footer alert line in the web UI, instead of writing them to a field that nothing rendered (#712) ([#717](https://github.com/jackal998/photo-manager/issues/717))
- Gate the web toolbar Execute button on a loaded manifest — matching the menu Action → Execute, the Set-Action button, and the Qt desktop — so it no longer opens the execute dialog over an empty manifest (#673) ([#719](https://github.com/jackal998/photo-manager/issues/719))
- Gate Start Scan on pending decisions in the web UI — a re-scan with un-executed decisions now raises a "Discard pending decisions?" confirm (web port of s27, #142) ([#722](https://github.com/jackal998/photo-manager/issues/722))
- Web UI: per-photo keep/delete decisions persist immediately and survive a page reload + manifest reopen — no explicit Save step (web port of s12, #673) ([#723](https://github.com/jackal998/photo-manager/issues/723))
- Add web video playback (V1): a new `GET /api/media` endpoint streams video bytes with HTTP Range/206 support, and the web preview pane + full-res viewer render a native `<video>` player for video rows — restoring parity with the Qt desktop's inline player (#729) ([#729](https://github.com/jackal998/photo-manager/issues/729))
- Add a video transcode fallback (V2): when the browser cannot decode a video's codec natively (e.g. HEVC without the OS codec), the web app lazily transcodes the source to H.264 via ffmpeg (cached, served with HTTP Range) so playback is unbreakable across machines (#730) ([#730](https://github.com/jackal998/photo-manager/issues/730))
- Add a web group-grid multi-tile video view with a coordinated playback controller (broadcast play/pause, master scrub, shared volume/mute) so a duplicate group's videos can be reviewed side-by-side — completing web video parity with the Qt grid. (#731) ([#731](https://github.com/jackal998/photo-manager/issues/731))
- Add a `PHOTO_MANAGER_WEB` launcher that runs the app's web UI in a native Edge WebView2 desktop window (via pywebview), alongside the classic Qt desktop app which stays the default — the desktop shell for the localhost web port. (#732) ([#732](https://github.com/jackal998/photo-manager/issues/732))
- Add "Set Action by Field" and "Execute (only selected)" to the web result-tree right-click menu, with a new reduced group-row context menu. (#749) ([#749](https://github.com/jackal998/photo-manager/issues/749))
- Add pHash/dHash/mean-color grouping-threshold controls to the web scan dialog and render the source-folder list alphabetized. (#750) ([#750](https://github.com/jackal998/photo-manager/issues/750))
- Make the web preview panel resizable by dragging the divider, and remember its width across reloads. (#753) ([#753](https://github.com/jackal998/photo-manager/issues/753))
- Show scan throughput (files/sec), a time-remaining estimate, and translated stage names in the web scan progress dialog. (#754) ([#754](https://github.com/jackal998/photo-manager/issues/754))
- Improve the web "Set Action by Field" dialog: Apply is no longer blocked on an empty pattern, a Recent-patterns dropdown remembers what you used, and the delete confirmation now names the field/pattern and file count. (#755) ([#755](https://github.com/jackal998/photo-manager/issues/755))
- When a web Execute can't delete some files, show a clear "Files Not Found" vs "Files Failed to Delete" breakdown with a decoded reason per file (e.g. "file is in use by another process"). (#756) ([#756](https://github.com/jackal998/photo-manager/issues/756))
- Web: per-row decisions in the result tree are now set with a single click on direct None / Delete / Ignore buttons, instead of opening a dropdown and picking. (#744) ([#768](https://github.com/jackal998/photo-manager/issues/768))
- Web review screen: right-clicking a group (or a file in it) now offers "Apply best-copy decisions to this group" — top-score file kept and locked, all others marked delete, with the standard lock-conflict dialog when locked rows are present (#744; review-time twin of scan-time auto-select). ([#771](https://github.com/jackal998/photo-manager/issues/771))
- Tagged releases now ship the web shell (#772): the exe entry point is launcher.py (Qt still the default), the built SPA is bundled and verified inside the artifact, a server-only smoke gates every release build, and the zip includes photo-manager-web.bat to launch web mode with a double-click. ([#774](https://github.com/jackal998/photo-manager/issues/774))
- The web menu bar gains the List menu with "Remove from List" (#678-D): removes the selected rows from the review list exactly like the right-click path (files stay on disk); disabled until rows are selected. ([#776](https://github.com/jackal998/photo-manager/issues/776))
- Mutating web routes are now CSRF-hardened (#662): requests from foreign origins or DNS-rebound hostnames get 403, closing the browser-borne attack surface on the loopback server; normal app flows and non-browser clients are unaffected. ([#777](https://github.com/jackal998/photo-manager/issues/777))
- Extract JPEG EXIF scoring signals in-memory during hashing, eliminating the second NAS read per file (2.6x faster JPEG-heavy NAS scans) (#786) ([#788](https://github.com/jackal998/photo-manager/issues/788))
- Persist EXIF sub-second and timezone (`subsec_time_original` / `offset_time_original`) alongside `shot_date`, which keeps its whole-second format — the ordering within a burst that ties at second resolution is now available to future consumers without a re-scan (#831) ([#831](https://github.com/jackal998/photo-manager/issues/831))
- Make the web full-res viewer, Execute Action dialog and Set Action dialog movable and resizable, each remembering its own position and size across reloads and clamping back into view on a smaller screen (#739). ([#841](https://github.com/jackal998/photo-manager/issues/841))
- Serialise preview decodes per physical device with cancellation tokens, so rapid clicks through a group decode only the first and last image instead of every one, and queue the next group's first image as a 1-ahead prefetch (#622). ([#842](https://github.com/jackal998/photo-manager/issues/842))
- Skip the doomed first video fetch on HEVC-incapable browsers: a memoized MediaCapabilities pre-check now picks the H.264 transcode as the initial source for .mov / .m4v files (#787). ([#843](https://github.com/jackal998/photo-manager/issues/843))
- Expose per-dimension scoring signals (`gps_present`, `exif_tag_count`, `xmp_derived`) in every `GET /api/manifest` FileRow, so the web QA suite can assert each scoring dimension instead of only the composite score (#680). ([#844](https://github.com/jackal998/photo-manager/issues/844))
- Web result tree: ArrowDown/ArrowUp rove the selection through the visible rows, keeping the active row on screen below the sticky column header (#709) ([#849](https://github.com/jackal998/photo-manager/issues/849))
- Skip the H.264 re-encode when a video is already browser-playable: the web transcode endpoint now probes the source with ffprobe and stream-copies it into MP4 instead, so a plain H.264 .mov no longer stalls on a pointless encode (#853). ([#860](https://github.com/jackal998/photo-manager/issues/860))
- Decode the DNG embedded-JPEG preview in draft mode: a click on an iPhone ProRAW file now decodes 3 MP instead of the full 48 MP sensor frame, and a 12 MP embedded frame decodes at half scale instead of full. Previews whose half-scale lands within 2% of the viewport cap now render at that half scale — up to 41 px under the 2048 cap, below one logical pixel on a 4K display at 175% (#865). ([#870](https://github.com/jackal998/photo-manager/issues/870))
- Apply the Daylight theme to the web results tree: 5-state similarity badges that stay distinguishable in grayscale, warm group framing, and a tinted, struck-through row for every file staged for deletion (#900) ([#900](https://github.com/jackal998/photo-manager/issues/900))
- Retone the result row's decision control, lock and score in the Daylight palette: the staged decision shows as a filled colour chip, the lock is a faint/solid padlock with a matching glyph in the column header, and the Score cell gains a mini bar beside its number (#878) ([#903](https://github.com/jackal998/photo-manager/issues/903))
- Extend the Daylight theme to the preview pane, context menus, dialogs and shared UI primitives (#878). ([#904](https://github.com/jackal998/photo-manager/issues/904))
- Retune the web Daylight foundation to the design's 2026-09-18 layout reply: declare the app's first font stacks (Segoe UI / Noto Sans TC for text, Cascadia Code for mono — nothing set a family before, so zh-TW labels fell back per glyph), move the warm accent to #a85a2c with hover and active going darker instead of lighter (the old hover measured worse than rest), lift the muted ink to #6b6358 and retire #a89f8f to rules, dividers, placeholders and disabled glyphs so every text tier now passes AA on warm paper, give the roving keyboard cursor a visible 2px ring plus a ▸ caret that composes over the selection and hover tints without shifting a single row, and declare the caution role (#6b4e12 on #fdf3d9, ▲) the later banner and chip work will share (#911). ([#911](https://github.com/jackal998/photo-manager/issues/911))
- Result-tree Action column now shows the decision instead of the scanner's classification (which moves to the similarity badge's tooltip), the column header reads as chrome with sort and resize affordances, numeric cells go mono and right-aligned, and the table sheds Resolution then Shot Date as it narrows (#912) ([#912](https://github.com/jackal998/photo-manager/issues/912))
- Result-tree rows get the Daylight box — cells centred rather than top-aligned, 72px rows with a 6px breath under each group's last child, a framed 48px thumbnail over a warm hatch, the decision segments seated on an inset track, a 28px padlock hit target, and a labelled score cell with a solid fill — plus a comfortable/compact density preference that persists per browser (the toggle itself lands with the status bar) (#914) ([#914](https://github.com/jackal998/photo-manager/issues/914))
- Promote "Keep best · delete rest" onto every group header — it skips locked rows silently-but-visibly and offers a 6-second undo instead of a confirm dialog; the header now also shows the group's total size and the folder its members share (#915) ([#915](https://github.com/jackal998/photo-manager/issues/915))
- Give the review screen its 52px toolbar and 30px status bar: one primary, a view-only filter, counted bulk verbs, a Delete-N button, the delete count and reclaim total, and a density switch (#916) ([#916](https://github.com/jackal998/photo-manager/issues/916))
- Let a photo be decided in the pane it is shown in: the preview pane gains a title, a 4:3 image frame with its similarity badge, a five-row metadata table, Keep/Delete/Skip buttons sharing the row control's vocabulary and lock behaviour, and a keep-worthiness bar (#905) ([#919](https://github.com/jackal998/photo-manager/issues/919))
- Make the first screen say what the app does — the empty state gains a heading, body copy and a primary Scan CTA at size; the preview pane gains a pinned similarity legend naming all five badge states; and the execute dialog's warning banners leave Tailwind's amber scale for the Daylight caution role (#878) ([#920](https://github.com/jackal998/photo-manager/issues/920))
- Show the delete count and reclaimed size in the Execute confirm, group its rows by folder with per-folder subtotals, and give every file a reason line (#917) ([#921](https://github.com/jackal998/photo-manager/issues/921))

### Bug Fixes

- Emit "Decision set to '<decision>'" status confirmation when applying a regex via the Execute Action dialog's right-click route (#316). ([#317](https://github.com/jackal998/photo-manager/issues/317))
- Extend the "Decision set" status-bar confirmation in the Execute Action dialog from the bulk-regex destructive branch (#316/#317) to every other decision-changing path: single-row right-click lock/unlock, single-row right-click decision-set, multi-row remove-from-list, and bulk-regex lock (#318). ([#322](https://github.com/jackal998/photo-manager/issues/322))
- Surface failed-to-delete files in a separate post-execute warning dialog (#341). ([#341](https://github.com/jackal998/photo-manager/issues/341))
- Fix UI freeze after scan + Close & Load when auto-select produced many keepers — selection is now applied in one batched QItemSelection.select() call instead of per-keeper, so selectionChanged + preview load fires once regardless of N (#344). ([#344](https://github.com/jackal998/photo-manager/issues/344))
- Refuse Apply on empty/invalid regex in the Set Action dialog, preventing every-row destructive actions when the pattern is blank (#347). ([#355](https://github.com/jackal998/photo-manager/issues/355))
- Rename Set Action dialog title to "Set Action by Field" and show field-aware hints in the numeric threshold input (date fields get a YYYY-MM-DD hint, number fields a "type a number" hint) (#347). ([#358](https://github.com/jackal998/photo-manager/issues/358))
- Preserve user-typed regex across Set Action dialog field changes and seed the Simple panel as ("contains", value) when a row is highlighted at open (#347). ([#360](https://github.com/jackal998/photo-manager/issues/360))
- Set Action dialog preview now displays the matched-field string (folder path, score, date, etc.) instead of the file basename for non-File-Name regex, and Top-N preview rows are labeled with their group and value (#347). ([#362](https://github.com/jackal998/photo-manager/issues/362))
- Surface threshold validation feedback (✓/✗ icon plus error label on unparseable input) in the Set Action dialog's numeric panel, and switch the Top-N counter to show the per-group bound ("X matched (≤N per group × G groups)") (#347). ([#364](https://github.com/jackal998/photo-manager/issues/364))
- Surface settings-persistence errors via loguru warnings in the Set Action dialog, validate and auto-heal corrupt entries in the Recent-patterns store, and drop a dead `setCurrentIndex(0)` call (#351). ([#365](https://github.com/jackal998/photo-manager/issues/365))
- Rework the Set Action dialog's Mode/Recent/Simple coordination: Recent now stores (field, pattern) pairs and auto-switches to Simple mode for Simple-representable picks, the Mode toggle is always visible with Simple disabled when no preview is available, the complex-pattern notice has an inline Switch to Regex button, and mode/field/simple_op now persist independently per dialog entry point with a legacy-key fallback (#347, #348, #349, #351). ([#367](https://github.com/jackal998/photo-manager/issues/367))
- `/work` now routes high-blast-radius small changes (migrations, settings keys, workers) through dev↔QA validation instead of skipping them as "simple" (#370). ([#370](https://github.com/jackal998/photo-manager/issues/370))
- Wave 8 polish on the Set Action dialog: validation icons + Recent dropdown arrow now use theme-aware QStyle pixmaps that adapt to dark mode (the previous hex stylesheets blew out in dark theme), the validation icon is hidden when the error label is visible (uncluttering the regex row), splitter handle position persists across reopens with a wider 8px grab target, and a new "Reset window size" button (or Ctrl+0) clears persisted geometry without touching mode/field/simple_op preferences (#349, #350, #351). ([#372](https://github.com/jackal998/photo-manager/issues/372))
- Wave 9a polish on the Set Action dialog: validation icons now expose their accessibleName as a hover toolTip; opening the dialog drops focus straight into the typing widget (Simple text / regex / numeric value / Top-N spinbox) per current mode instead of the field combo; both regex and Simple text inputs gain a native × clear button; clicking the `[abc]` cheatsheet chip selects the inner "abc" so the next keystroke replaces them; Ctrl+Enter triggers Apply from anywhere in the dialog; and Alt-letter mnemonics fire the action buttons — Alt+A Apply, Alt+C Close, Alt+R Recent, Alt+S Switch to Regex, Alt+W reset Window size (#348, #350). ([#376](https://github.com/jackal998/photo-manager/issues/376))
- Wave 9b-trim on the Set Action dialog: the match counter now briefly flashes "Applied to N rows" after Apply so the user gets in-dialog confirmation without leaving the dialog (the main-window status bar still emits "Decision set to ..." as before — the two surfaces complement each other), and the action-combo label rewords from "Set Action:" to "Action for each match:" to make the per-row scope explicit (#348). ([#378](https://github.com/jackal998/photo-manager/issues/378))
- Final wave of the Set Action dialog audit: the "delete" action now surfaces a confirmation modal showing the matched count and a human-readable pattern summary before emitting (Cancel is the default Esc target so a misfired Enter cannot fire a bulk delete; other actions emit immediately unchanged), and a new "Test against:" input above the preview list lets users try a single hand-typed string against the current regex with live ✓/✗ feedback and match-position info — without adding the string to their actual file collection (#350). ([#380](https://github.com/jackal998/photo-manager/issues/380))
- Raise the qa-batch UIA connect default timeout from 5s to 20s so the launch-to-UIA-ready window absorbs GitHub Actions Windows-runner cold-start variance, eliminating the intermittent `ElementNotFoundError: '.*Photo Manager.*'` flake that surfaced on random shards (matches the precedent already in qa/probes/_runtime.py; local runs finish in <2s and are unaffected) (#373). ([#383](https://github.com/jackal998/photo-manager/issues/383))
- Fix ActionDialog Apply silently no-op'ing for non-Size numeric fields (Score, Group Count, Similarity, Creation Date, Shot Date) via the main-window menu route — original audit-triggering bug (#392). ([#394](https://github.com/jackal998/photo-manager/issues/394))
- Clamp Set Action dialog's Recent ▾ menu position to the dialog's current screen so a stale dialog after a multi-monitor disconnect cannot open the menu off-screen (#381). ([#404](https://github.com/jackal998/photo-manager/issues/404))
- Cap the Execute Action dialog's top section so the tree absorbs vertical growth on resize instead of competing with the summary label and "Select by Field/Regex…" button (#408). ([#412](https://github.com/jackal998/photo-manager/issues/412))
- Fix Execute Action dialog preview pane staying blank on row selection — wire the shared `ImageTaskRunner`'s `imageLoaded` signal to the dialog's own `PreviewPane` (#409). ([#413](https://github.com/jackal998/photo-manager/issues/413))
- Reword DeleteRegexConfirmDialog so it stops promising immediate file deletion on the deferred Set-Action path — confirmation now reads "Mark N files for deletion" with explicit "files will be sent to the Recycle Bin when you run Execute Action" body (#415). ([#418](https://github.com/jackal998/photo-manager/issues/418))
- Revert the Set Action dialog combo label from "Set status for each match:" back to "Set action for each match:" — keeps the per-row scope phrasing from Wave 9b-trim B12 while restoring "action" as the canonical user-facing noun, parallel with sibling labels (Set Action by Field title, Set Action context menu) (#416). ([#419](https://github.com/jackal998/photo-manager/issues/419))
- Fix release.yml "Build release notes" step — bash heredoc syntax replaced with PowerShell here-string piped to `python -`. ([#421](https://github.com/jackal998/photo-manager/issues/421))
- release.yml release-notes step rewritten as pure PowerShell — drops Python+here-string interaction that GitHub Actions YAML parser rejected. ([#422](https://github.com/jackal998/photo-manager/issues/422))
- Purge residual "to be moved" / "搬移" wording from translations + scan log, and canonicalise auto-select keeper writes to `user_decision=""` so the tree's Action column renders empty + locked (matching right-click Set Action → keep). (#425) ([#432](https://github.com/jackal998/photo-manager/issues/432))
- Preserve loaded manifest visibility across View → Language switch. (#428) ([#434](https://github.com/jackal998/photo-manager/issues/434))
- Suppress visible exiftool console window during scans in PyInstaller windowed builds. (#427) ([#435](https://github.com/jackal998/photo-manager/issues/435))
- Add row spacing so Advanced Settings descriptions no longer collide with their sliders. (#423) ([#436](https://github.com/jackal998/photo-manager/issues/436))
- Switch "Execute Action (only selected)" to group-membership scope so the dialog keeps peer rows visible for triage. (#430) ([#437](https://github.com/jackal998/photo-manager/issues/437))
- Clear preview pane on Open Manifest / re-scan so the previously selected file no longer lingers after a fresh load. (#431) ([#439](https://github.com/jackal998/photo-manager/issues/439))
- Scope Execute Action dialog's "Select by Field/Regex…" to the rendered group subset, and refresh the main tree when Close is clicked after decision changes (#443, #444). ([#445](https://github.com/jackal998/photo-manager/issues/445))
- Stream the 12-byte magic-bytes header during scan instead of fully reading every HEIC/RAW/PNG/GIF/WEBP file — halves NAS scan I/O for iPhone-photo libraries (#446). ([#447](https://github.com/jackal998/photo-manager/issues/447))
- Harden scan lifecycle: cancel checks at CLASSIFY/SCORE/AUTO-SELECT/WRITE entries, MainWindow scan-running guard, HEIC diagnostics gated behind PHOTO_MANAGER_HEIC_DIAG, aboutToQuit cleanup hook, and minor scanner correctness fixes (#476). ([#476](https://github.com/jackal998/photo-manager/issues/476))
- Kill orphan exiftool subprocesses on Windows by assigning them to a Job Object with KILL_ON_JOB_CLOSE, eliminating the user-reported Explorer freeze after killing the app mid-scan (#460). ([#477](https://github.com/jackal998/photo-manager/issues/477))
- Gate exact-pHash format-duplicate path on mean_color so flat images (solid colour / near-empty composition) no longer collide as EXACT duplicates on pHash 8000000000000000 (#462). ([#478](https://github.com/jackal998/photo-manager/issues/478))
- Atomically replace the manifest sqlite via temp + os.replace so orphan -wal/-shm sidecars no longer survive a write and partial-writes never reach the destination (#464). ([#479](https://github.com/jackal998/photo-manager/issues/479))
- Fix scoring path penalty to exclude the basename (no double-count vs filename score) and de-duplicate per-segment hits when one folder matches multiple bad keywords; add forward-defensive tz-mismatch fallback to date-provenance heuristic (#466, #467). ([#480](https://github.com/jackal998/photo-manager/issues/480))
- Add stdout-read timeout to ExiftoolProcess.execute so a wedged exiftool surfaces as a clean ExiftoolTimeout exception instead of blocking readline() and hanging the scan consumer thread (#465). ([#481](https://github.com/jackal998/photo-manager/issues/481))
- Skip Windows $RECYCLE.BIN and System Volume Information during scans so deleted-by-other-means files no longer leak into the manifest and trip send2trash's "can't recycle a recycle-bin file" error (#482). ([#482](https://github.com/jackal998/photo-manager/issues/482))
- Make the scan WALK stage cooperatively cancellable. Closing the Scan dialog (or clicking Cancel/X) during WALK now stops the walker within one rglob tick instead of waiting for it to exhaust — fixing the user-visible "must close CMD window to stop the scan" symptom, which was the scan QThread being orphaned after the dialog's `wait(3000)` timed out. Adds observability log lines when the close-path `wait()` does time out, so any future cancel-checkpoint gap is diagnosable from the log instead of from the symptom (#491). ([#491](https://github.com/jackal998/photo-manager/issues/491))
- Stop the Action dialog's live-preview timer on close so an orphaned dialog can't fire a stale timeout into a destroyed widget (a Windows/3.11 use-after-free that surfaced as CI test-isolation flake) (#492). ([#495](https://github.com/jackal998/photo-manager/issues/495))
- Project ``scan.hash_pool="auto"`` calibration to the real file count (measuring process spawn cost and per-file rate separately) so a large scan correctly picks the process pool instead of being penalised by the one-time spawn cost charged against the small calibration sample (#486). ([#499](https://github.com/jackal998/photo-manager/issues/499))
- Stop silently dropping ``.avi`` files from the scan manifest — ``.avi`` was walked and counted but had no file-type mapping, so it resolved to "skip" and never produced a row; it now maps to the video type and is hashed like other videos (#461). ([#506](https://github.com/jackal998/photo-manager/issues/506))
- Write the delete audit CSV on the in-dialog Execute Action delete path so a real UI deletion is recorded (group, path, success, reason) like the regex/service path already was — previously only DeleteService wrote it, leaving UI deletes with no audit trail (#505). ([#508](https://github.com/jackal998/photo-manager/issues/508))
- Scans no longer abort on an unreadable folder entry (broken junction/symlink) — the walker skips it and continues — and the scan dialog's progress bar no longer stays stuck on "scanning" after an empty or failed scan (#511). ([#511](https://github.com/jackal998/photo-manager/issues/511))
- Dispose the bare top-level widgets that the scan-dialog tests leak, ending the residual Qt dialog-teardown use-after-free that flaked the suite on Windows/3.12 (#515). ([#515](https://github.com/jackal998/photo-manager/issues/515))
- Stop flat / near-empty images (icons, blank scans) with degenerate pHashes from collapsing unrelated files into one false duplicate group (#518). ([#518](https://github.com/jackal998/photo-manager/issues/518))
- Stop tiny images (UI icons, thumbnails) below 128px from forming false perceptual near-duplicate groups; they still participate in exact-duplicate detection (#523). ([#523](https://github.com/jackal998/photo-manager/issues/523))
- Require an independent second perceptual hash (dHash) to agree before grouping near-duplicates, removing ~44% of false-positive near-dup matches on real photos (#525). ([#525](https://github.com/jackal998/photo-manager/issues/525))
- Fix the app aborting on every Open Manifest load — restore the missing `UIUpdaterImpl.clear_preview` proxy so the post-load preview reset no longer raises inside the loader thread (#528). ([#528](https://github.com/jackal998/photo-manager/issues/528))
- Fixed the Scan dialog's **Advanced settings** descriptions appearing truncated. PySide6 6.11 stopped flagging `hasHeightForWidth()` on word-wrapped `QLabel`s, so the full-text inline descriptions (added in #521) were clipped to one line. Restored the concise design: each control now shows a short one-line description under its bold title with the full technical detail in a hover tooltip. ([#533](https://github.com/jackal998/photo-manager/issues/533))
- Scan dialog Advanced settings: the hover tooltip now lives on the description line only (the bold title no longer pops a tooltip), and long tooltips word-wrap to a tidy ~360px block — long zh_TW tooltips previously rendered as one over-long line off the screen edge because Qt only word-wraps rich-text tooltips and CJK has no spaces to break on. ([#534](https://github.com/jackal998/photo-manager/issues/534))
- Fixed the app freezing indefinitely (one CPU core at 100%) after a scan when clicking **Close & Load** with auto-select on. Auto-selecting + auto-previewing a tall portrait keeper triggered an unbounded resize⇄refit loop in the single-image preview: with the vertical scrollbar set to "as needed", the fitted image's height straddled the viewport and toggled the scrollbar on/off, each toggle resizing the viewport and forcing another refit. The single-image preview now reserves the vertical scrollbar so the viewport width stays constant and the fit converges. ([#535](https://github.com/jackal998/photo-manager/issues/535))
- Aggressive auto-select no longer auto-marks Ref-tier "—" passengers for deletion. A file pulled into a duplicate group by a same-stem pair edge (RAW+JPG, HEIC+JPG, or a Live Photo's video) is shown as a "—" passenger and is now never auto-tagged for delete, so a complementary original can't be silently swept; only files the scanner positively classified as exact or near-duplicates remain eligible (#536). ([#540](https://github.com/jackal998/photo-manager/issues/540))
- Files that merely share a name no longer get grouped as duplicates when they are actually different photos. The scanner pairs same-stem files (e.g. a RAW+JPG or a Live Photo's video) by filename, which previously merged genuinely-unrelated files that collided on a name across merged folders, multiple cameras, or Google Takeout exports. Such a pair is now kept together only when their image content agrees (or one side is a video with no comparable hash), so a name collision no longer fabricates a duplicate group (#539). ([#541](https://github.com/jackal998/photo-manager/issues/541))
- Near-duplicate grouping is now a true transitive closure: a genuine near-duplicate whose only link into a group was an already-classified file is no longer left out and shown as a unique file. Such a reconnected file — and any non-reference member of a group — now shows how similar it is to the nearest file in its group in the Similarity column, instead of a blank "—" (#538, #536). ([#543](https://github.com/jackal998/photo-manager/issues/543))
- Fixed a crash when clicking "Keep all" in the prune-singletons confirmation dialog (the prompt shown after Remove from List collapses a group down to a single item). The keep button re-entered its own close handler without bound, overflowing the stack and taking the app down; it now closes cleanly and keeps the singletons as intended (#547). ([#547](https://github.com/jackal998/photo-manager/issues/547))
- Process-mode scans no longer leave orphaned worker processes reading the disk after a force-quit, and cancelling no longer stalls on an in-flight file read (#553). ([#553](https://github.com/jackal998/photo-manager/issues/553))
- Fix the Windows kill-on-close Job Object so exiting or force-killing the app actually terminates the exiftool and hash-worker child processes instead of orphaning them (#556). ([#556](https://github.com/jackal998/photo-manager/issues/556))
- Auto hash-pool mode now keeps the per-device thread path (NAS reads overlapping the local disk) on mixed-device scans instead of switching to a flat single pool that bypassed the per-device optimization (#557). ([#557](https://github.com/jackal998/photo-manager/issues/557))
- Closing the scan dialog mid-scan now terminates the exiftool helper processes immediately and aborts promptly, instead of hanging the close and leaving exiftool running after the app exits (#562). ([#562](https://github.com/jackal998/photo-manager/issues/562))
- Force-killing or crashing the app mid-scan no longer leaves an orphaned exiftool process running on the desktop, while keeping the CI EXIF pass uncorrupted via a job-nesting gate (#558). ([#563](https://github.com/jackal998/photo-manager/issues/563))
- Fix a memory-backpressure gap in the read/compute scan pipeline: in-flight image bytes are now bounded so a fast disk feeding a slow decode stage can no longer grow memory without limit (#570). ([#571](https://github.com/jackal998/photo-manager/issues/571))
- Bound the exiftool queue and make its producer cancel-safe, capping scan-time RAM growth and closing a cancel-deadlock hazard on large scans. (#575) ([#575](https://github.com/jackal998/photo-manager/issues/575))
- Group SMB shares on one physical NAS server into a single hash-reader pool, so two mapped drive letters on one box no longer double-subscribe it with 16 concurrent reads. (#576) ([#576](https://github.com/jackal998/photo-manager/issues/576))
- Fix scan crash (out of memory) on photo libraries containing many large RAW/DNG files — the HASH stage now bounds in-flight reads by total bytes, not file count (#587). ([#590](https://github.com/jackal998/photo-manager/issues/590))
- Fix RAW/DNG files being needlessly re-read from disk during hashing (rawpy 0.26.1 had dropped the in-memory `open_buffer` API the code relied on); they are now decoded directly from the bytes already read for the checksum (#591). ([#593](https://github.com/jackal998/photo-manager/issues/593))
- Fix the Scan dialog not stopping the scan when closed mid-scan — closing now reliably cancels the worker instead of leaving it running, and no longer prints a "cannot schedule new futures" error on exit (#594). ([#595](https://github.com/jackal998/photo-manager/issues/595))
- Fix multi-device scans stalling reads from a fast source (e.g. a NAS) when another drive is busy with large RAW/DNG files — the HASH-stage read memory budget is now split per device instead of shared globally (#596). ([#598](https://github.com/jackal998/photo-manager/issues/598))
- Revert the durable volume-GUID device key (#605/#583) — it broke the spinning-HDD reader cap (4 readers instead of 1), causing seek-thrash on local HDD scans. (#606) ([#606](https://github.com/jackal998/photo-manager/issues/606))
- Closing the scan dialog after hashing finishes (during the EXIF post-drain) now hard-kills the live exiftool consumer so it doesn't survive dialog dismissal at 100 % CPU — fills the cancel-checkpoint gap that #561 left on the post-HASH path (#607). ([#607](https://github.com/jackal998/photo-manager/issues/607))
- Multi-device scans that include a NAS now use the process-pool hash executor instead of thread, escaping Python's GIL contention on shared compute. Real-rig D+J 3400-file measurement: 1.61× faster end-to-end (601s timeout vs 421s complete) and the 70-second compute-freeze episodes during big-DNG decode are gone (#609). ([#609](https://github.com/jackal998/photo-manager/issues/609))
- Multi-device scans that mix a local disk + NAS now run each device on its own ProcessPoolExecutor (sized by `hash_workers_for_root`) instead of a flat shared pool, closing the seek-thrash regression that #609 exposed: real-rig D+H+J 4500-file measurement completes 2.6× faster (513s vs 900s timeout) with mean CPU utilization 1.93× higher and idle CPU samples down from 50% to 25% (#610). ([#610](https://github.com/jackal998/photo-manager/issues/610))
- Fix the won't-close bug: closing the main window now reliably exits the photo-manager process. Previously the python.exe parent could stay alive idle for hours (0% CPU, no visible windows, no child processes) because Qt 6's `lastWindowClosed` signal didn't fire after the hidden main window, so `aboutToQuit` never ran and `app.exec()` parked forever. `MainWindow.closeEvent` now calls `QApplication.instance().quit()` directly when the close is accepted, bypassing the flaky last-window-closed machinery (#612). ([#612](https://github.com/jackal998/photo-manager/issues/612))
- TreeController.refresh_model now tears down the prior QStandardItemModel + QSortFilterProxyModel — fixes the ~250 MB/reload leak that caused 8-10 GB residency on close (#618, root cause of #614). ([#619](https://github.com/jackal998/photo-manager/issues/619))
- Live Photo passenger detection now requires same-folder match — cross-folder same-basename MOVs no longer mis-classified as a HEIC's passenger (score=None, "—" Similarity); existing manifests self-heal on next rescore (#620). ([#623](https://github.com/jackal998/photo-manager/issues/623))
- Execute Action — release preview video handle before delete, decode HRESULT reasons in the failure dialog, and merge the singleton-prune "ask" cascade into one rebuild. Fixes the OLE error 0x80270027 / `COPYENGINE_E_SHARING_VIOLATION_SRC` that blocked deleting a previewed video file (#631). ([#631](https://github.com/jackal998/photo-manager/issues/631))
- Preview pane — DNG embedded-JPEG fast path now honours the embedded EXIF Orientation tag. Portrait-grip iPhone ProRAW DNGs no longer render 90° rotated relative to Lightroom / File Explorer (#632 fixes a #624 regression). ([#632](https://github.com/jackal998/photo-manager/issues/632))
- Manifest reloads now release in-memory image cache RAM and stop accumulating `ManifestLoadWorker` Qt children, and `PhotoRecord` / `PhotoGroup` use `__slots__` to drop ~200 bytes per instance — three O(N) memory wins across the manifest lifecycle (#637, refs #616). ([#637](https://github.com/jackal998/photo-manager/issues/637))
- Single-view preview's resolution row reads off the UI thread (no more 3-5 s NAS-DNG stutter), and the first-launch thumbnail-cache rebuild notice now actually reaches the status bar (#638). ([#638](https://github.com/jackal998/photo-manager/issues/638))
- Fix the web Execute dialog committing every decided row regardless of the active type filter; Execute now scopes to the visible rows ("visible = committed") and a banner warns when a filter hides pending deletes. (#676) ([#682](https://github.com/jackal998/photo-manager/issues/682))
- Fix the web result-tree "Remove from list" to FINALIZE `outcome='ignored'` — the row now leaves the review tree immediately, matching the desktop result-tree "Remove from List" (it previously only staged a reversible ignore decision until Execute) (#694) ([#705](https://github.com/jackal998/photo-manager/issues/705))
- Fix the web prune-lock flow folding still-locked items into the prune set when their unlock PATCH fails — hold them back while still pruning the unlocked buckets, and surface a prune-context error (#702) ([#714](https://github.com/jackal998/photo-manager/issues/714))
- Port Qt destructive guardrails to the web app: the pre-execute confirm now fires whenever ANY group would be entirely deleted, and decision writes on locked rows are gated behind the lock-confirm dialog instead of silently overwriting (#733) ([#745](https://github.com/jackal998/photo-manager/issues/745))
- Web scan now reads back hash-pool / read-knee calibration caches and `scan.exif_workers` from settings before scanning, matching the Qt dialog — the second web scan of a library reuses the first one's calibration instead of re-measuring (#652) ([#746](https://github.com/jackal998/photo-manager/issues/746))
- Web grid video tiles now show a real decoded poster frame behind the play glyph (Shell/WIC rescue attempt, shared with Qt) instead of a static dark placeholder, with dark fallback if the poster fails to load (#734) ([#747](https://github.com/jackal998/photo-manager/issues/747))
- Cut the first-view stall when playing HEVC videos in the web app (ffmpeg ultrafast transcode) and show a "Preparing video…" indicator while it prepares. (#751) ([#751](https://github.com/jackal998/photo-manager/issues/751))
- Recognize JPG+MOV Live Photo pairs (iPhone "Most Compatible" mode) so the motion half is grouped as a "—" passenger, not scored as a separate duplicate. (#752) ([#752](https://github.com/jackal998/photo-manager/issues/752))
- The web scan dialog now remembers the "Auto-tune reader concurrency" toggle across launches, and the full-resolution viewer's zoom range matches the desktop (5%–800%). (#758) ([#758](https://github.com/jackal998/photo-manager/issues/758))
- The web app's status-footer error message now clears as soon as you take another action, instead of lingering on screen until the next manifest load. (#760) ([#760](https://github.com/jackal998/photo-manager/issues/760))
- The web scan dialog now resets its state once a scan finishes or is cancelled, so reopening it shows your current scan sources instead of the previous run's stale ones. (#761) ([#761](https://github.com/jackal998/photo-manager/issues/761))
- Cancelling the lock-confirm prompt during Execute Actions now keeps the Execute review dialog open (matching the desktop app) so you can adjust and retry, instead of dropping you back to the review tree. (#721) ([#765](https://github.com/jackal998/photo-manager/issues/765))
- Cancelling the lock-confirm prompt in the "Set Action by Field" dialog now keeps that dialog open (matching the desktop app) so your field/pattern selection is preserved and you can adjust and retry, instead of closing it. (#764) ([#766](https://github.com/jackal998/photo-manager/issues/766))
- The scan dialog now shows a clear "lost connection to the scanner" error if the server connection drops mid-scan, instead of freezing indefinitely with no feedback. Brief network blips still recover automatically. (#661) ([#767](https://github.com/jackal998/photo-manager/issues/767))
- settings.json writes are now crash-safe (temp file + atomic replace, #653) and a settings update that would silently destroy an existing value now fails loudly with a clear error instead — surfaced as 422 by PATCH /api/settings (#658). ([#775](https://github.com/jackal998/photo-manager/issues/775))
- fix the 2026-07-16 goal-audit findings: recycle-bin wording, visible reveal errors, runtime-derived phase-4 gate, mtime-fresh preview cache, full web i18n incl. zh_TW (#782) ([#782](https://github.com/jackal998/photo-manager/issues/782))
- Fix stable-tag GitHub releases failing when an empty positional arg was passed to `gh release create` (#789). ([#789](https://github.com/jackal998/photo-manager/issues/789))
- Run blocking web read/serve and mutating route I/O off the async event loop so a slow NAS call no longer freezes other requests or the live scan-progress stream (#803). ([#803](https://github.com/jackal998/photo-manager/issues/803))
- Surface real manifest-migration failures instead of silently swallowing every ALTER TABLE error as a duplicate column (#806). ([#806](https://github.com/jackal998/photo-manager/issues/806))
- Sort string fields in the correct order when descending is selected — previously descending on a string silently produced ascending order (#807). ([#807](https://github.com/jackal998/photo-manager/issues/807))
- Make duplicate-keeper selection deterministic on same-priority ties (lexicographically smallest path) so re-scans reproduce the same keep/duplicate decisions (#808). ([#808](https://github.com/jackal998/photo-manager/issues/808))
- Harden the scan EXIF pass: survive a malformed base EXIF IFD instead of dropping the file, and backfill in-memory JPEG dates even when exiftool is absent (#811). ([#811](https://github.com/jackal998/photo-manager/issues/811))
- Show the human-readable message from object-shaped API errors instead of a raw JSON blob (#812). ([#812](https://github.com/jackal998/photo-manager/issues/812))
- Fix a column-resize listener leak when the result tree unmounts mid-drag, and declare the scan.exif_workers settings key the backend serves (#813). ([#813](https://github.com/jackal998/photo-manager/issues/813))
- Apply EXIF Orientation before pHash/dHash so a rotated copy and its upright original land in the same near-duplicate group, and hash the RAW embedded preview at the JPEG path's working resolution from a single LibRaw open (#822, #826). ([#828](https://github.com/jackal998/photo-manager/issues/828))
- Stop the web settings, scan-start and bulk-decide routes blocking the event loop on synchronous disk I/O (#790). ([#829](https://github.com/jackal998/photo-manager/issues/829))
- Dismiss recycle-bin and system-folder rows left in the manifest by a pre-#482 scan when it is opened, and skip Synology's `#recycle` share folder during a walk (#821) ([#834](https://github.com/jackal998/photo-manager/issues/834))
- Group a RAW original with its renamed lossy export when their pHashes match exactly, so the pair is reviewable instead of two invisible singletons; both stay undecided (#824). ([#836](https://github.com/jackal998/photo-manager/issues/836))
- End a result-tree column-resize drag when the mouse is released outside the window, instead of leaving the column resizing on every later mouse move (#796) ([#840](https://github.com/jackal998/photo-manager/issues/840))
- Align the web result tree's virtualizer coordinates with its scroll container by offsetting them with the sticky column header's measured height, so post-scan scroll-to-keeper lands centred and row windowing is correct near scroll boundaries (#699). ([#846](https://github.com/jackal998/photo-manager/issues/846))
- Web qa write helpers wait for the API write their click causes, ending the moving "expected decision 'delete', got ''" flake family in the advisory web-scenario-batch job (#855) ([#855](https://github.com/jackal998/photo-manager/issues/855))
- Require a non-blank reason in the `[docs-not-needed: ...]` and `[qa-not-needed: ...]` PR-gate bypass tokens, so a pasted placeholder can no longer disable either gate silently (#857). ([#859](https://github.com/jackal998/photo-manager/issues/859))
- End the web preview-panel resize drag on an off-window release, so the layout no longer sticks to the cursor (#851). ([#861](https://github.com/jackal998/photo-manager/issues/861))
- Make web dialogs actually animate open and closed — the `animate-in` / `zoom-in-95` classes emitted no CSS until `tw-animate-css` was installed (#852). ([#867](https://github.com/jackal998/photo-manager/issues/867))
- Bundle a pinned LGPL ffmpeg/ffprobe in the Windows release so the HEVC video fallback works without a manual install (#854). ([#868](https://github.com/jackal998/photo-manager/issues/868))
- Require a real reason in the `[skip-news: ...]` changelog bypass token — a blank one, an unclosed bracket, and the `<reason>` placeholder quoted in prose no longer disable the news gate, and the `[docs-not-needed: ...]` / `[qa-not-needed: ...]` twins reject the placeholder too (#858). ([#871](https://github.com/jackal998/photo-manager/issues/871))
- Floor the pHash and dHash near-duplicate thresholds at 2 on both the Qt and web scan dialogs, and explain in the tooltip/helper that odd values usually match the same files as the even value below them — position 1 admitted nothing at all and silently switched near-duplicate detection off (#823) ([#875](https://github.com/jackal998/photo-manager/issues/875))
- Reject a near-duplicate threshold below 2 (or above 20) at `POST /api/scan` instead of running a scan that can only report "no near-duplicates found" (#876). ([#879](https://github.com/jackal998/photo-manager/issues/879))
- Honour `thumbnail_disk_cache_dir` and `video_transcode_cache_dir` in the web client — its lifespan built both media services without settings, so every cache landed in `%LOCALAPPDATA%` regardless of configuration (#874). ([#880](https://github.com/jackal998/photo-manager/issues/880))
- Stop a bypass token's reason from swallowing the rest of the PR body (#872). ([#881](https://github.com/jackal998/photo-manager/issues/881))
- Write settings.json next to photo-manager.exe instead of inside the replaceable _internal tree, through one shared resolver (#882). ([#891](https://github.com/jackal998/photo-manager/issues/891))
- Seed the Set Action matcher from the highlighted row again, and stop the page itself scrolling so the toolbar stays on screen (#893, #894). ([#895](https://github.com/jackal998/photo-manager/issues/895))
- Proofread the web client copy: translate the Scan dialog, the result tree's group header and the Execute dialog's warning banners into 繁體中文, fix count lines that read "1 groups" / "1 files found" / "row(s)", replace raw backend error text with user-worded sentences, rename the menu entry to "Set Action by Field…", restore the "~dup" similarity label, and normalise zh-TW punctuation (#907). ([#907](https://github.com/jackal998/photo-manager/issues/907))
- Unify the web decision labels on Keep / Delete / Skip (保留 / 刪除 / 略過) across the row control, right-click menus, Execute filter and badge, and the Set Action combo — three of which were hardcoded English and untranslated in zh_TW — drop the duplicate "Ref" pill from the keeper row, and render the Action column's classifier value through the catalog instead of the raw enum (#908). ([#908](https://github.com/jackal998/photo-manager/issues/908))

### Documentation

- Clarify CLAUDE.md "Security gates" — per gated action, not per pipeline. ([#295](https://github.com/jackal998/photo-manager/issues/295))
- Add CLAUDE.md pointer to user-global "My decision style" preferences (#296). ([#296](https://github.com/jackal998/photo-manager/issues/296))
- List missing app/views helpers (main_window_helpers, preview_pane_helpers, image_tasks_helpers, window_state) in the README project tree. ([#315](https://github.com/jackal998/photo-manager/issues/315))
- Correct stale "21 scenarios / 30-60 seconds" claims in the qa-explore skill docs; point at qa.scenarios._batch.ALL_SCENARIOS as the canonical scenario list. ([#319](https://github.com/jackal998/photo-manager/issues/319))
- Correct five stale cross-references in docs/features.md after the tree_controller module moved from handlers/ to components/ and the main_window.py handlers shifted line numbers. ([#321](https://github.com/jackal998/photo-manager/issues/321))
- Add probe-layer authoring recipe to docs/testing.md: when to add a probe, three flavours (static/soft-live/exploration), copyable skeletons, forcing-function design, coverage-cascade warning, and common pitfalls. ([#337](https://github.com/jackal998/photo-manager/issues/337))
- Update README test count and document probe layer in CLAUDE.md (#340). ([#340](https://github.com/jackal998/photo-manager/issues/340))
- Refresh six stale source line references in `docs/features.md` (Execute Action cluster + sort persistence) and bump the verification date to 2026-05-21 across the remaining entries whose Behaviour text still matches current source (#326). ([#342](https://github.com/jackal998/photo-manager/issues/342))
- Retire scenario-drivers table; point at qa.scenarios._batch.ALL_SCENARIOS (#352). ([#352](https://github.com/jackal998/photo-manager/issues/352))
- Replace the skip-news classification criterion ("user-visible or contributor-visible change worth noting") with a capability test ("no diff worth a one-line record") so authors can apply it without judging visibility boundaries (#375). ([#375](https://github.com/jackal998/photo-manager/issues/375))
- Prune stale preamble in qa-explore scenario-drivers.md skill doc (#323). ([#389](https://github.com/jackal998/photo-manager/issues/389))
- Document the mmap-RAW audit decision in scanner/hasher.py — no code change warranted because rawpy needs a bytes buffer and video SHA already streams in 64 KB chunks (#453). ([#459](https://github.com/jackal998/photo-manager/issues/459))
- Documented the Google Takeout workflow in the README: run [`galbum`](https://github.com/jackal998/google-album-metadata) first to write the `.json` sidecar metadata (original date / GPS) back into the media files, then scan — so Takeout photos date and dedupe correctly instead of landing in `UNDATED`. Records that #471's scanner-side sidecar matching is superseded by the standalone galbum tool. ([#471](https://github.com/jackal998/photo-manager/issues/471)) ([#532](https://github.com/jackal998/photo-manager/issues/532))
- Correct two stale prose descriptions in the GroupMediaController feature doc (master-position truncation and mute-restore-to-default), and flag the web-port tech-design §5 testid contract as superseded by the generated testids and features.md. (#744) ([#769](https://github.com/jackal998/photo-manager/issues/769))
- Full Qt->web parity matrix + 51-issue census committed to docs/audits/web-parity-matrix-2026-07.md: zero unrecorded feature gaps; 28 issues identified as fixed-but-administratively-open with per-row evidence; perf cutover gate reduced to #737 (#652 verified done). ([#770](https://github.com/jackal998/photo-manager/issues/770))
- record the 2026-07-16 web-port goal audit and correct stale design/matrix claims (no-op-stub note, real-gap verdicts for shipped fixes) (#780) ([#780](https://github.com/jackal998/photo-manager/issues/780))
- add the #744 single divergence list with complete owner sign-off (24 rows: 19 keep, 3 fix via #739, 2 n/a) (#783) ([#783](https://github.com/jackal998/photo-manager/issues/783))
- record the NAS probe session results: small-file knee at 16, walk do-not-build, exiftool second-read cost, HEVC first-byte gate numbers (#785) ([#785](https://github.com/jackal998/photo-manager/issues/785))
- Drop the removed 'Apply best-copy' right-click action from README; point at the Top-N-per-group Score condition and scan-time auto-select instead (#804) ([#804](https://github.com/jackal998/photo-manager/issues/804))
- Backfill audited web-port docs drift: 8 features.md gaps closed and README gains a Web UI section (#805) ([#805](https://github.com/jackal998/photo-manager/issues/805))
- Add the visual auto-select feasibility study (moment_id / visual_score census, measured costs, proxy-label and human validation, per-capability verdicts) plus the local ground-truth labelling harness under scripts/visual_gt (#819). ([#819](https://github.com/jackal998/photo-manager/issues/819))
- Record the #622 Phase 3 J: NAS measurement — boxes 1/3/4 pass, box 2 fails at 4.242x against its 5x bar (#622). ([#864](https://github.com/jackal998/photo-manager/issues/864))
- Stacked PRs must be linked with `gh stack link` the moment the upper PR opens; the PR-create skill now carries the owner's rule, the per-merge `mergeable` re-check and the merge-not-rebase cascade (#901). ([#901](https://github.com/jackal998/photo-manager/issues/901))
- Rewrite the README workflow walkthrough for the web client, dropping the retired Qt menu paths (#924). ([#924](https://github.com/jackal998/photo-manager/issues/924))

### Removals

- Drop ActionDialog Close button; move Reset window-size to the preview header (#399). ([#399](https://github.com/jackal998/photo-manager/issues/399))
- Remove ActionDialog "Test against" playground; live preview covers the same need (#400). ([#400](https://github.com/jackal998/photo-manager/issues/400))
- Remove ActionDialog pre-Apply gate; receiver surfaces empty/invalid regex via QMessageBox (#401). ([#401](https://github.com/jackal998/photo-manager/issues/401))
- Remove the legacy MOVE action and dest_path column (photo-transfer contract); opening an old manifest auto-migrates (#514) ([#514](https://github.com/jackal998/photo-manager/issues/514))
- Remove the PySide6 desktop client, its qa-explore harness and the Qt CI job; the web shell is now the only client and PySide6 is gone from requirements.txt (#646). ([#885](https://github.com/jackal998/photo-manager/issues/885))

### Miscellaneous

- Localise the action-bucket row labels in the scan-dialog progress log so raw internal action strings (`KEEP` / `MOVE` / `EXACT` / `REVIEW_DUPLICATE` / `UNDATED`) stop leaking into user-visible output; internal values stay unchanged (#242). ([#242](https://github.com/jackal998/photo-manager/issues/242))
- Add layer-1 coverage for `app/views/main_window.py` via helper extraction + thin-proxy tests (#185). ([#283](https://github.com/jackal998/photo-manager/issues/283))
- Add layer-1 coverage for `app/views/widgets/group_media_controller.py` via helper extraction + fake-self thin-proxy tests (#185). ([#285](https://github.com/jackal998/photo-manager/issues/285))
- Add layer-1 coverage for `app/views/preview_pane.py` via helper extraction (closes #185). ([#289](https://github.com/jackal998/photo-manager/issues/289))
- Add layer-1 coverage for `app/views/handlers/dialog_handler.py`, `app/views/image_tasks.py`, and `app/views/widgets/video_player.py` via helper extraction + fake-self thin-proxy tests; remove all three from the cascade-omit list (closes #293). ([#293](https://github.com/jackal998/photo-manager/issues/293))
- Trim `parallel-brief-generator` description from 1064 → 676 chars to fit Anthropic's 1024-char limit; trigger examples moved into the body's "When to use" section. ([#295](https://github.com/jackal998/photo-manager/issues/295))
- Split `pr-review` SKILL.md (801 lines) via progressive disclosure: Gates 7-11 moved into sibling `app-gates.md`; SKILL.md now 471 lines under Anthropic's 500-line ceiling. ([#298](https://github.com/jackal998/photo-manager/issues/298))
- Split `qa-explore` SKILL.md (958 lines) via progressive disclosure: project-context, Phase 4 driving details, and scenario-driver authoring moved into three sibling files; SKILL.md now 388 lines under Anthropic's 500-line ceiling. ([#299](https://github.com/jackal998/photo-manager/issues/299))
- Pin the legacy `sources.{iphone,takeout,jdrive}` → `sources.list` migration shim in `ScanDialog._load_from_settings` with a layer-1 regression test, so a future cleanup PR cannot silently empty source lists for users upgrading from a pre-`sources.list` build (#258). ([#304](https://github.com/jackal998/photo-manager/issues/304))
- Add `qa/probes/` live-UIA exploration layer with two probes (`field_dropdown_inventory`, `group_label_audit`); ship the easy-payoff pair from the #243 roster, defer three more to follow-ups. ([#305](https://github.com/jackal998/photo-manager/issues/305))
- Adopt cursor-review-pr-bundle pieces and refactor `/pr-review` into a manager that composes per-gate sub-skills (`docs-features-drift`, `qa-scenario-drift`, `app-security-patterns`, `sqlite-migration-safety`, `scanner-perf-patterns`, `test-padding-patterns`, `skill-pii-audit`) plus four GitHub-API mechanic skills (`conventional-comments`, `github-pr-review-pending`, `github-pr-review-submitted`, `github-pr-review-fetch`) covering inbound and outbound review comms for both human-in-loop and agent-to-agent flows. ([#306](https://github.com/jackal998/photo-manager/issues/306))
- Validate PR vs issue refs in parallel-brief-generator output to prevent mismatched `#N` in briefs (#292). ([#309](https://github.com/jackal998/photo-manager/issues/309))
- Refine the "Opening PRs or pushing to a remote" CLAUDE.md gate to distinguish branch pushes / PR creation (still gated) from posting PR review threads via the `github-pr-review-pending` / `github-pr-review-submitted` skills (auto-approved per the skill's own invocation contract), so `/pr-review`'s default-on post-back actually fires in fresh sessions instead of being silently swallowed by the gate. ([#313](https://github.com/jackal998/photo-manager/issues/313))
- Fold #311 (news-gate omission) and #312 (`docs_guard` single-trigger misread) cold-session feedback into the `parallel-brief-generator` skill so generated briefs name the `news-gate` CI workflow and `docs_guard`'s behavioural-modify trigger precisely (#314). ([#314](https://github.com/jackal998/photo-manager/issues/314))
- qa-batch runner: resolve descendant pids when waiting for the main window (silences a per-scenario false "main window did not appear" warning) and dismiss the unsaved-changes prompt on close (eliminates the spurious "app did not exit cleanly, terminating" log on 14/52 scenarios). ([#320](https://github.com/jackal998/photo-manager/issues/320))
- Patch audit gaps in /work pipeline: wire team-event hooks in settings.json.example, add security gate reminder to git merge step, document pipeline agents in CLAUDE.md, and short-circuit branch input directly to /pr-review. ([#335](https://github.com/jackal998/photo-manager/issues/335))
- Close remaining /work audit gaps: gh preflight, compact guidance, and input validation. ([#336](https://github.com/jackal998/photo-manager/issues/336))
- Replace the qa batch runner's Tab+Tab+Enter positional close with a UIA label-based selector, and extract `MainWindow.closeEvent`'s "Unsaved Changes" button list into a module-level spec pinned by L1 tests so a future button-order reorder fails fast instead of silently leaking "app did not exit cleanly, terminating" (#325). ([#338](https://github.com/jackal998/photo-manager/issues/338))
- Add layer-3 qa coverage for `ExecuteActionDialog`'s right-click context-menu paths (s53 — Lock / Unlock / Set Action → delete via right-click; s54 — Set Action → "remove from list" with its Yes-confirm), closing the gap PR #322 left under `[qa-not-needed]` (#324). ([#339](https://github.com/jackal998/photo-manager/issues/339))
- Add Phase 5 (post-PR completion) to the `/work` skill — inline news-fragment write, `gh pr checks --watch` with 20-min timeout, one auto-iteration on red, `/work watch <N>` resume mode for listener-flakiness recovery, plus a Phase 2 file-overlap check that forces multi-issue tasks with shared files to sequence in a single session (#343). ([#343](https://github.com/jackal998/photo-manager/issues/343))
- Add `test_probe_destructive_surface_inventory` static probe to flag duplicate destructive entry points across menu bar + context menu (#353). ([#353](https://github.com/jackal998/photo-manager/issues/353))
- Add qa/probes/post_scan_visual_state — assert selection set matches keeper rows after scan+load (#356). ([#356](https://github.com/jackal998/photo-manager/issues/356))
- Add Apply-gate live-UIA probe blocks to qa scenarios s14 + s31 (#357). ([#357](https://github.com/jackal998/photo-manager/issues/357))
- Add Wave 11 layer-3 probes closing the regex-dialog audit qa-coverage backlog (#359, #361, #363, #371, #377, #379). ([#384](https://github.com/jackal998/photo-manager/issues/384))
- Fix s31 B2 probe (mode-aware widget lookup) and s43 main-flow counter assertion (post-#378 format) — closes Wave 11 layer-3 test debt (#386, #387). ([#388](https://github.com/jackal998/photo-manager/issues/388))
- Add s55 scenario covering ActionDialog C1 (Simple-radio-disabled with no match_fn) — partially closes #366. ([#390](https://github.com/jackal998/photo-manager/issues/390))
- Add live UIA probe for Set-Action-by-Field dialog panel visibility (#301). ([#403](https://github.com/jackal998/photo-manager/issues/403))
- Combine auto-select's post-scan decision + lock writes into a single SQLite transaction; saves one connection-open and one fsync per scan. ([#406](https://github.com/jackal998/photo-manager/issues/406))
- Reword Set Action dialog combo label to "Set status for each match" and drop the "(remove action)" parenthetical on `keep` (#407). ([#411](https://github.com/jackal998/photo-manager/issues/411))
- Ungate gh pr/issue create and git push in CLAUDE.md and settings example (#441). ([#441](https://github.com/jackal998/photo-manager/issues/441))
- Extract per-file hash compute into a top-level picklable ``run_hash_for_record`` — structural precondition for a future ProcessPoolExecutor migration of the HASH stage (#486). ([#487](https://github.com/jackal998/photo-manager/issues/487))
- Add real-input contract tests for the picklable ``run_hash_for_record`` per-file dispatch path (idx passthrough, exception-to-skip mapping, image-vs-video phash handling) — deferred from #486, unblocked by the #492 test-isolation fix (#486). ([#496](https://github.com/jackal998/photo-manager/issues/496))
- Add layer-3 qa scenarios for "Execute selected" partial-execute, the actioned-singleton prune dialog, and late-stage / WALK-stage scan cancel + the main-window-X scan guard (#512). ([#512](https://github.com/jackal998/photo-manager/issues/512))
- Give the locked-rows confirm dialog context-specific wording so the user can tell whether "Apply" deletes files now (Execute-time gate: "Unlock & Delete All") or only queues a decision (Set Action gate: "Unlock & Set Action") (#513). ([#513](https://github.com/jackal998/photo-manager/issues/513))
- Tidy the scan dialog Advanced Settings: one-line descriptions with full detail on hover, and drop the manual hash-workers spinbox in favour of the NAS-aware auto-pick (#520). ([#520](https://github.com/jackal998/photo-manager/issues/520))
- Add the `github-pr-create` skill as the single source of truth for opening a PR that goes green, and rewire `/work` Phase 5 to delegate to it (#531). ([#531](https://github.com/jackal998/photo-manager/issues/531))
- Scan hashing now runs one I/O pool per physical device concurrently, so mixed local-disk + NAS scans no longer bottleneck all readers on a single drive (#550). ([#550](https://github.com/jackal998/photo-manager/issues/550))
- Cap local spinning-HDD hash readers to 2 so concurrent reads no longer seek-thrash the one spindle (#552). ([#552](https://github.com/jackal998/photo-manager/issues/552))
- Confirmed spinning HDDs now use a single sequential hash reader (down from 2) to minimise seeks, since the disk — not the CPU — is the bottleneck on a mechanical drive (#559). ([#559](https://github.com/jackal998/photo-manager/issues/559))
- The HASH stage now overlaps disk reads with CPU decode via a read/compute pipeline split, making mixed local-disk + NAS scans roughly 2.3× faster and steadier (less throughput oscillation) (#566). ([#568](https://github.com/jackal998/photo-manager/issues/568))
- Speed up JPEG decoding ~4x during hashing via shrink-on-load (Image.draft); near-duplicate grouping is unchanged. The win shows on warm re-scans and compute-bound machines; a read-bound first scan is unaffected (#569). ([#572](https://github.com/jackal998/photo-manager/issues/572))
- Add an env-gated occupancy probe (PHOTO_MANAGER_AUTOTUNE_PROBE, off by default) that measures read/compute queue regime, toward validating the #551 auto-tuner. (#577) ([#577](https://github.com/jackal998/photo-manager/issues/577))
- Add a real round-trip test for the #551 read-knee persistence slot (read_knee_measured to store_read_knee to the next scan's cache-hit handoff), covering the one previously-untested link before the Phase-4 default-ON flip. (#582) ([#582](https://github.com/jackal998/photo-manager/issues/582))
- Add layer-3 qa coverage for the D6 locked-singleton-prune gate on both the "ask" and "always" preference paths. (#589) ([#592](https://github.com/jackal998/photo-manager/issues/592))
- Audit record for the post-#606 scanner-perf clean re-run: replaces the confounded #604 conclusion (ratio 1.005 on HDD-thrashed code) with isolated J: NAS-only measurements (ratio 1.112 — autotune −11 % on this well-fit NAS), validates the #598 per-device byte-budget fix on real HW (NAS no longer starved by D: ProRAW), and re-decides #586 (stays closed won't-do, now evidence-backed) (#608). ([#608](https://github.com/jackal998/photo-manager/issues/608))
- Decision record for the #604→#610 scanner-perf saga retro — every premature claim, every measurement-after-the-fact, every self-written disclaimer treated as a footnote when it should have been a block-gate (#611). ([#611](https://github.com/jackal998/photo-manager/issues/611))
- Set-action / set-locked-state on the main duplicate-groups tree no longer rebuild the entire `QStandardItemModel` on every click — incremental cell update + O(1) path index + APPLY_ALL_UNLOCKED single-transaction collapse (#613). ([#617](https://github.com/jackal998/photo-manager/issues/617))
- Scaffold the React frontend toolchain (Vite/React 19/Tailwind 4/Vitest) and the Python->TS testid generator for the localhost web port. (#657) ([#657](https://github.com/jackal998/photo-manager/issues/657))
- Add the first 5 Playwright web-QA scenarios (scan, status bar, context menu, execute preview) plus an advisory CI batch that runs them against a live server. (#667) ([#667](https://github.com/jackal998/photo-manager/issues/667))
- Port action-dialog qa scenarios (s14/s29/s56) to the web Playwright harness; web qa parity 12->15. (#675) ([#675](https://github.com/jackal998/photo-manager/issues/675))
- Port execute-dialog qa scenarios (s13/s33/s36/s64) to the web Playwright harness; web qa parity 15->19. (#677) ([#677](https://github.com/jackal998/photo-manager/issues/677))
- Port scan-lifecycle qa scenarios (s01/s03/s05/s24/s68) to the web Playwright harness; web qa parity 19->24. (#679) ([#679](https://github.com/jackal998/photo-manager/issues/679))
- Port scan-pipeline-data qa scenarios (s06/s07/s08/s11/s42/s52/s65) to the web Playwright harness; web qa parity 24->31. (#681) ([#681](https://github.com/jackal998/photo-manager/issues/681))
- Add web qa coverage for the result-tree context menu (Open folder reveal, lock/unlock), the empty-area no-menu guard, and the group-header expand/collapse toggle (s19/s25/s35/s40). (#684) ([#684](https://github.com/jackal998/photo-manager/issues/684))
- Add web qa coverage for walker exclusions, multi-source priority, and the ActionDialog numeric (Size) threshold (s09/s10/s43). (#690) ([#690](https://github.com/jackal998/photo-manager/issues/690))
- Mark the three desktop-only QA scenarios (s28 dirty-exit prompt, s39 window geometry, s48 dialog geometry) as `skip` in the web scenario map — they have no browser equivalent — so the web parity ledger reflects honest progress (#688) ([#711](https://github.com/jackal998/photo-manager/issues/711))
- Add web QA scenario s34 (execute-time lock-confirm Cancel verdict) and reclassify s59 as skip — web-architecture-eliminated (#720) ([#720](https://github.com/jackal998/photo-manager/issues/720))
- Add `scripts/bench_web_port.py`, a Qt-vs-web scan-throughput A/B bench that drives the shared Qt-free `run_pipeline` through both arms and asserts each arm's `files_per_s > 0` — the Phase-0 exit gate and T6 baseline for the web-port cutover (#725) ([#725](https://github.com/jackal998/photo-manager/issues/725))
- Add `scripts/bench_thumbnail_latency.py`, the T4 web-port cutover gate: asserts the 40 MiB full-res cap returns 413, a real ProRAW full-res serve stays under a peak-RSS budget, and warm `size=thumb` p95 ≤ 200 ms — with a `make_qa_large_source.py --include-large-raw` fixture helper (#726) ([#726](https://github.com/jackal998/photo-manager/issues/726))
- Run the web-port perf benches in CI via an advisory `bench-sanity` job in `web-eval-gates.yml`: `bench_web_port.py` (web-arm scan-throughput liveness) and `bench_thumbnail_latency.py` (full-res 40 MiB cap + warm `size=thumb` p95) (#727) ([#727](https://github.com/jackal998/photo-manager/issues/727))
- Extract Qt-free logic ahead of the web-port Qt removal: relocate `ALL_SCENARIOS` (→ `qa/scenario_ids.py`), the #258 legacy-settings migration (→ `core/app_service/settings_migration.py`, now also applied on `GET /api/settings`), and `IGNORE_DECISION` (→ `core/constants.py`) so the web CI gates + tests survive — PySide6 untouched (#728) ([#728](https://github.com/jackal998/photo-manager/issues/728))
- Scan now writes the manifest and its auto-select keeper locks/decisions as a single atomic step, so an interrupted or failed scan can no longer leave a manifest with rows but missing keeper locks — and a failed re-scan leaves the prior manifest untouched. (#759) ([#759](https://github.com/jackal998/photo-manager/issues/759))
- The docs-coverage guard no longer blocks a pure refactor of the manifest repository that leaves the schema untouched, and the testing docs now spell out that real-browser interaction behaviour must be proven by a Playwright scenario rather than a jsdom unit test. (#762) ([#762](https://github.com/jackal998/photo-manager/issues/762))
- CI now path-scopes the Qt qa-batch and pytest jobs so a pure-frontend PR skips them instead of paying the full ~7-minute backend + 5-shard UIA cost; the filter fails open so a real backend change is never silently skipped. (#763) ([#763](https://github.com/jackal998/photo-manager/issues/763))
- replace web-eval-gates workflow-level paths filter with a scoped job gate so web QA jobs can later be made required without the stuck-pending trap (#773) ([#779](https://github.com/jackal998/photo-manager/issues/779))
- add the NAS read-probe toolkit and session runbook (knee ladder, walk timing, exif second-read, HEVC first-byte) (#784) ([#784](https://github.com/jackal998/photo-manager/issues/784))
- Fix worktree-safe interpreter path for the PreToolUse guard hooks so docs/qa/zombie guards run outside repo-root sessions (#802) ([#802](https://github.com/jackal998/photo-manager/issues/802))
- Scope the tests and frontend CI workflows to least-privilege contents:read permissions (#809). ([#809](https://github.com/jackal998/photo-manager/issues/809))
- Point run.bat at launcher.py so the dev launcher honours PHOTO_MANAGER_WEB like the packaged build (#810). ([#810](https://github.com/jackal998/photo-manager/issues/810))
- Strengthen the manifest default-sort test to assert real ordering and drop a send2trash coverage-padding test (#814). ([#814](https://github.com/jackal998/photo-manager/issues/814))
- Flip the four web-eval-gate CI jobs from advisory to blocking for the Phase-4 cutover (#817) ([#817](https://github.com/jackal998/photo-manager/issues/817))
- Pin the six project agent definitions and their spawn sites to `model: opus` so dispatches stop failing the opus-only model guard (#825). ([#827](https://github.com/jackal998/photo-manager/issues/827))
- Revive the shared WIC thread pool between tests so collection order no longer decides whether the suite passes (#832). ([#832](https://github.com/jackal998/photo-manager/issues/832))
- Give the top-N-by-score keeper rule a single home so bulk-decide and apply-best-copy cannot pick different keepers (#833) ([#833](https://github.com/jackal998/photo-manager/issues/833))
- Pin the #811 scanner-robustness fixes with the acceptance tests #793 asks for: a real malformed-EXIF JPEG that must still be hashed into the manifest, and an exiftool-missing run where a JPEG dated only in XMP keeps its shot_date instead of being filed as UNDATED (#835) ([#835](https://github.com/jackal998/photo-manager/issues/835))
- Repoint the Qt Execute Action dialog at the shared winerror reason table so desktop and web failure reasons cannot drift (#839) ([#839](https://github.com/jackal998/photo-manager/issues/839))
- Add web qa scenario s61 variant `lock_apply_remove`, driving Remove after Unlock & Apply on the singleton-prune lock gate (#713). ([#845](https://github.com/jackal998/photo-manager/issues/845))
- Add the #622 Phase 3 preview probe and its owner runbook — a headless Qt harness that records per-click time-to-first-paint, the decode route actually taken, RSS and LRU occupancy, and the full-res viewer's open/close lifecycle into one citable JSON (#622). ([#856](https://github.com/jackal998/photo-manager/issues/856))
- Run the qa-batch Qt UIA shards only on PRs touching a Qt surface — web QA is the gate (#863). ([#866](https://github.com/jackal998/photo-manager/issues/866))
- Remux videos whose first audio track is copyable even when a later track is not, and cap concurrent ffprobe with the same 2-process semaphore as ffmpeg (#862). ([#873](https://github.com/jackal998/photo-manager/issues/873))
- Retarget the docs, qa-scenario and zombie guards at the web client — they matched only deleted paths and had gone silently inert (#646). ([#889](https://github.com/jackal998/photo-manager/issues/889))
- Move the photo-scanner-patterns Claude skill from user level into the repo (#902). ([#902](https://github.com/jackal998/photo-manager/issues/902))


## Historical (pre-towncrier) - 2025-09-10 → 2026-05-17

Baseline reconstructed from the titles of the 166 merged pull requests (#1 through #269) prior to adoption of news fragments. PR titles were categorised by their conventional-commit prefix; entries without a recognised prefix fall under *Miscellaneous*. For the full per-commit history, see [`git log`](https://github.com/jackal998/photo-manager/commits/master).

### Features

- feat(scanner): pHash-based deduplication scanner engine ([#8](https://github.com/jackal998/photo-manager/pull/8))
- feat(review): terminal review CLI for REVIEW_DUPLICATE triage ([#9](https://github.com/jackal998/photo-manager/pull/9))
- feat(ui): connect migration_manifest.sqlite to existing review GUI ([#12](https://github.com/jackal998/photo-manager/pull/12))
- feat(ui): integrate scan into GUI — File > Scan Sources… ([#13](https://github.com/jackal998/photo-manager/pull/13))
- feat: dynamic multi-source scan — unlimited folders, per-source recursive toggle ([#17](https://github.com/jackal998/photo-manager/pull/17))
- feat(ux): select dialog + context menu redesign ([#19](https://github.com/jackal998/photo-manager/pull/19))
- feat(dedup): transitive group_id via union-find ([#21](https://github.com/jackal998/photo-manager/pull/21))
- feat(dedup): mean-color false-positive gate + single-read hashing pipeline ([#23](https://github.com/jackal998/photo-manager/pull/23))
- feat(ui): expose hamming_distance, configurable mean-color threshold, Similarity column ([#24](https://github.com/jackal998/photo-manager/pull/24))
- feat(scanner): pixel dimensions from open PIL image, Resolution column in UI ([#26](https://github.com/jackal998/photo-manager/pull/26))
- feat(qa): /qa-explore skill, isolated fixtures, and PHOTO_MANAGER_HOME ([#43](https://github.com/jackal998/photo-manager/pull/43))
- feat(i18n): phase 1 — Traditional Chinese (zh_TW) localization ([#157](https://github.com/jackal998/photo-manager/pull/157))
- feat(scan-dialog): two-column layout — tree + source list on the left ([#160](https://github.com/jackal998/photo-manager/pull/160))
- feat(regex-dialog): Phase A — live preview, validation, right-click parity ([#162](https://github.com/jackal998/photo-manager/pull/162))
- feat(regex-dialog): Phase B — Beginner mode, cheatsheet, recent patterns, match highlight ([#167](https://github.com/jackal998/photo-manager/pull/167))
- feat(regex-dialog): Phase C — Simple rename, 3-col cheatsheet, regex syncs across modes ([#168](https://github.com/jackal998/photo-manager/pull/168))
- feat(decisions): Locked state — pin per-file decisions against bulk regex ([#175](https://github.com/jackal998/photo-manager/pull/175))
- feat(scan-dialog): collapse Grouping Parameters under Advanced Settings ([#179](https://github.com/jackal998/photo-manager/pull/179))
- feat(lock): freeze-all + unlock-confirm dialog ([#182](https://github.com/jackal998/photo-manager/issues/182), supersedes [#175](https://github.com/jackal998/photo-manager/issues/175)) ([#183](https://github.com/jackal998/photo-manager/pull/183))
- feat(hooks): add docs_guard + zombie_check PreToolUse hooks ([#184](https://github.com/jackal998/photo-manager/pull/184))
- feat(window): persist geometry + lift splitter min-width floor ([#191](https://github.com/jackal998/photo-manager/pull/191))
- feat(empty-state): add Scan / Open buttons next to first-run hint ([#137](https://github.com/jackal998/photo-manager/issues/137)) ([#197](https://github.com/jackal998/photo-manager/pull/197))
- feat(tree): double-click row in results tree ([#143](https://github.com/jackal998/photo-manager/issues/143)) ([#198](https://github.com/jackal998/photo-manager/pull/198))
- feat(schema): scoring columns for keep-worthiness scorer ([#187](https://github.com/jackal998/photo-manager/issues/187)) ([#199](https://github.com/jackal998/photo-manager/pull/199))
- feat(scoring): MediaExtract schema + extended exiftool batch ([#187](https://github.com/jackal998/photo-manager/issues/187)) ([#200](https://github.com/jackal998/photo-manager/pull/200))
- feat(scoring): two-tier compute_score + 8 weighted dimensions ([#187](https://github.com/jackal998/photo-manager/issues/187)) ([#202](https://github.com/jackal998/photo-manager/pull/202))
- feat(scoring): wire scoring into scan pipeline + rescore API ([#187](https://github.com/jackal998/photo-manager/issues/187)) ([#203](https://github.com/jackal998/photo-manager/pull/203))
- feat(scoring): COL_SCORE column + within-group score-DESC sort ([#187](https://github.com/jackal998/photo-manager/issues/187)) ([#204](https://github.com/jackal998/photo-manager/pull/204))
- feat(scoring): right-click "Apply best-copy to group" action ([#187](https://github.com/jackal998/photo-manager/issues/187)) ([#205](https://github.com/jackal998/photo-manager/pull/205))
- feat(scoring): s42 end-to-end QA scenario + docs ([#187](https://github.com/jackal998/photo-manager/issues/187)) ([#206](https://github.com/jackal998/photo-manager/pull/206))
- feat(execute-dialog): scope execution to highlighted rows ([#211](https://github.com/jackal998/photo-manager/issues/211)) ([#219](https://github.com/jackal998/photo-manager/pull/219))
- feat(select-dialog): numeric comparison conditions ([#221](https://github.com/jackal998/photo-manager/pull/221))
- feat(tree): persist column order and width to QSettings ([#214](https://github.com/jackal998/photo-manager/issues/214)) ([#227](https://github.com/jackal998/photo-manager/pull/227))
- feat(dialogs): persist geometry for scan, execute, and set-action dialogs ([#228](https://github.com/jackal998/photo-manager/pull/228))
- feat: auto-select highest-scoring file per group after scan ([#212](https://github.com/jackal998/photo-manager/issues/212)) ([#232](https://github.com/jackal998/photo-manager/pull/232))
- feat(execute): add Execute Mode toggle alongside the dialog (refs [#165](https://github.com/jackal998/photo-manager/issues/165)) ([#236](https://github.com/jackal998/photo-manager/pull/236))
- feat(execute): preview pane in Execute Action dialog ([#165](https://github.com/jackal998/photo-manager/issues/165)) ([#260](https://github.com/jackal998/photo-manager/pull/260))
- feat(hooks): docs_guard catches behavioural changes; update-docs skill row ([#268](https://github.com/jackal998/photo-manager/pull/268))

### Bug Fixes

- fix(scanner): chunk exiftool, early-exit walker, --limit for debug dry-runs ([#11](https://github.com/jackal998/photo-manager/pull/11))
- fix: UX bugs, dual Set Action entry points, sort aggregation, test coverage ([#14](https://github.com/jackal998/photo-manager/pull/14))
- fix(ux): execute action dialog filtering + set action regex bug ([#20](https://github.com/jackal998/photo-manager/pull/20))
- fix(ui): sort Similarity column by group_number, not scanner action type ([#29](https://github.com/jackal998/photo-manager/pull/29))
- fix(ux): collapse two-step delete confirm into single QMessageBox ([#30](https://github.com/jackal998/photo-manager/pull/30))
- fix(scanner,db): correct DNG resolution + exclude executed rows on reload ([#31](https://github.com/jackal998/photo-manager/pull/31))
- fix(scanner,preview): correct DNG resolution in scanner, single view, and grid view ([#32](https://github.com/jackal998/photo-manager/pull/32))
- fix: restore dev-requirements.txt with pytest and linter pins ([#36](https://github.com/jackal998/photo-manager/pull/36))
- fix(scanner): one bad file no longer aborts the whole scan ([#46](https://github.com/jackal998/photo-manager/issues/46)) ([#53](https://github.com/jackal998/photo-manager/pull/53))
- fix(scan): treat empty input as benign success, not failure ([#62](https://github.com/jackal998/photo-manager/pull/62))
- fix(scan): surface corrupt image files instead of silent UNDATED ([#63](https://github.com/jackal998/photo-manager/pull/63))
- fix(scan): forward scan progress and errors to loguru ([#64](https://github.com/jackal998/photo-manager/pull/64))
- fix(ux): four small dialog and main-window UX fixes ([#65](https://github.com/jackal998/photo-manager/pull/65))
- fix(ux): polish batch — title, button label, group order, first-run hint ([#66](https://github.com/jackal998/photo-manager/pull/66))
- fix(qa): align driver button title with post-[#54](https://github.com/jackal998/photo-manager/issues/54) "Close & Load" ([#73](https://github.com/jackal998/photo-manager/pull/73))
- fix(scan): narrow corrupt-image detection to PIL-decoded formats only ([#77](https://github.com/jackal998/photo-manager/pull/77))
- fix(ui): put every Ref-tier action at top of group, not just KEEP ([#78](https://github.com/jackal998/photo-manager/pull/78))
- fix(ui): order EXACT before REVIEW_DUPLICATE within a group ([#81](https://github.com/jackal998/photo-manager/pull/81))
- fix(ui): preserve prior manifest's actions when a new Open Manifest fails ([#110](https://github.com/jackal998/photo-manager/pull/110))
- fix(ui): pluralize 'pair'/'file'/'group' in status bar ([#109](https://github.com/jackal998/photo-manager/issues/109)) ([#112](https://github.com/jackal998/photo-manager/pull/112))
- fix(ui): focus Close button after empty/failed scan ([#86](https://github.com/jackal998/photo-manager/issues/86)) ([#113](https://github.com/jackal998/photo-manager/pull/113))
- fix(scan): reconcile manifest summary with skipped count ([#87](https://github.com/jackal998/photo-manager/issues/87)) ([#114](https://github.com/jackal998/photo-manager/pull/114))
- fix(qa): make _uia click helpers reliable under Windows foreground-lock ([#126](https://github.com/jackal998/photo-manager/pull/126))
- fix(qa): drive QFileDialog via Qt non-native widget on hosted CI ([#131](https://github.com/jackal998/photo-manager/pull/131))
- fix(scanner): exif batch drift + stream interleaving ([#145](https://github.com/jackal998/photo-manager/issues/145)) ([#146](https://github.com/jackal998/photo-manager/pull/146))
- fix(scan): confirm before re-scan overwrites pending decisions ([#142](https://github.com/jackal998/photo-manager/issues/142)) ([#147](https://github.com/jackal998/photo-manager/pull/147))
- fix(qa): s13 fixture verify-and-retry to eliminate flake ([#148](https://github.com/jackal998/photo-manager/issues/148)) ([#149](https://github.com/jackal998/photo-manager/pull/149))
- fix(dialogs): set ApplicationModal on Execute and Action dialogs ([#139](https://github.com/jackal998/photo-manager/issues/139)) ([#151](https://github.com/jackal998/photo-manager/pull/151))
- fix(menus): add Alt-key mnemonics to top-level menus ([#135](https://github.com/jackal998/photo-manager/issues/135)) ([#155](https://github.com/jackal998/photo-manager/pull/155))
- fix(scanner): handle NTFS case-collisions + trailing-dot folders ([#174](https://github.com/jackal998/photo-manager/pull/174))
- fix(dedup): always group Live Photo pairs, decouple their actions ([#88](https://github.com/jackal998/photo-manager/issues/88)) ([#178](https://github.com/jackal998/photo-manager/pull/178))
- fix(scan-dialog): surface inline error when typed path doesn't exist ([#144](https://github.com/jackal998/photo-manager/issues/144)) ([#192](https://github.com/jackal998/photo-manager/pull/192))
- fix(status-bar): persistent baseline survives timeouts + menu hover ([#138](https://github.com/jackal998/photo-manager/issues/138), [#140](https://github.com/jackal998/photo-manager/issues/140)) ([#193](https://github.com/jackal998/photo-manager/pull/193))
- fix(lock-confirm): pass total delete count to lock-confirm dialog ([#207](https://github.com/jackal998/photo-manager/issues/207)) ([#217](https://github.com/jackal998/photo-manager/pull/217))
- fix(lock-guard): block remove-from-list on locked items ([#208](https://github.com/jackal998/photo-manager/issues/208)) ([#218](https://github.com/jackal998/photo-manager/pull/218))
- fix(scan-dialog): use absolute path default in browse button ([#216](https://github.com/jackal998/photo-manager/issues/216)) ([#220](https://github.com/jackal998/photo-manager/pull/220))
- fix: save manifest dialog opens with folder picker visible ([#230](https://github.com/jackal998/photo-manager/issues/230)) ([#231](https://github.com/jackal998/photo-manager/pull/231))
- fix(qa/s44): read file-row coords from UIA, not 1x-DPI offsets ([#229](https://github.com/jackal998/photo-manager/issues/229)) ([#233](https://github.com/jackal998/photo-manager/pull/233))
- fix: gate "Set Action by Field/Regex" on manifest load ([#244](https://github.com/jackal998/photo-manager/issues/244)) ([#248](https://github.com/jackal998/photo-manager/pull/248))
- fix(dialog): pass groups= to ActionDialog from main-window callsite ([#237](https://github.com/jackal998/photo-manager/issues/237)) ([#249](https://github.com/jackal998/photo-manager/pull/249))
- fix(tree): one Ref label per group ([#241](https://github.com/jackal998/photo-manager/issues/241)) ([#252](https://github.com/jackal998/photo-manager/pull/252))
- fix(scan): visually select KEEP rows after auto-select scan ([#239](https://github.com/jackal998/photo-manager/issues/239)) ([#255](https://github.com/jackal998/photo-manager/pull/255))
- fix(i18n): translate 9 numeric-panel zh_TW strings ([#245](https://github.com/jackal998/photo-manager/issues/245)) ([#256](https://github.com/jackal998/photo-manager/pull/256))

### Documentation

- docs(readme): rewrite README for scanner-based workflow ([#10](https://github.com/jackal998/photo-manager/pull/10))
- docs: audit and fix stale documentation ([#16](https://github.com/jackal998/photo-manager/pull/16))
- docs(readme): sync usage docs with current UI and code ([#33](https://github.com/jackal998/photo-manager/pull/33))
- docs: reframe layer 2 as on-demand spot-tests, not a maintained suite ([#90](https://github.com/jackal998/photo-manager/pull/90))
- docs: qa scenario authoring guide (lessons from [#128](https://github.com/jackal998/photo-manager/issues/128)) ([#130](https://github.com/jackal998/photo-manager/pull/130))
- docs(qa): refresh qa-explore skill stale references after [#133](https://github.com/jackal998/photo-manager/issues/133) ([#134](https://github.com/jackal998/photo-manager/pull/134))
- docs: sync README + testing.md with PRs [#157](https://github.com/jackal998/photo-manager/issues/157)–[#160](https://github.com/jackal998/photo-manager/issues/160) ([#161](https://github.com/jackal998/photo-manager/pull/161))
- docs: sync README + testing.md after the regex-dialog Phase A/B/C arc ([#172](https://github.com/jackal998/photo-manager/pull/172))
- docs(features): canonical feature inventory + execute-action backfill ([#263](https://github.com/jackal998/photo-manager/pull/263))
- docs(features): backfill full app behaviour + README audit ([#267](https://github.com/jackal998/photo-manager/pull/267))
- docs(readme): audit fixes for Step 1 / 2 / 4 ([#264](https://github.com/jackal998/photo-manager/issues/264), [#265](https://github.com/jackal998/photo-manager/issues/265), [#266](https://github.com/jackal998/photo-manager/issues/266)) ([#269](https://github.com/jackal998/photo-manager/pull/269))

### Removals

- feat(ui): Action menu + remove Select feature (checkboxes) ([#27](https://github.com/jackal998/photo-manager/pull/27))
- qa(s15): scope _row_anchor to result tree, drop hardcoded y_min=600 ([#105](https://github.com/jackal998/photo-manager/pull/105))
- qa(s21) + fix: List > Remove from List driver, fix multi-word plural ([#118](https://github.com/jackal998/photo-manager/pull/118))
- qa(s20): drive right-click multi-selection > Remove from List ([#119](https://github.com/jackal998/photo-manager/pull/119))
- feat(actions): bulk regex 'remove from list' + dirty-flag exit prompt ([#158](https://github.com/jackal998/photo-manager/pull/158))
- qa: scenarios for exit-dirty prompt (s28) + deferred bulk remove (s29) ([#159](https://github.com/jackal998/photo-manager/pull/159))
- feat(execute-dialog): clickable jump-to in all-delete banner ([#166](https://github.com/jackal998/photo-manager/issues/166)) ([#181](https://github.com/jackal998/photo-manager/pull/181))
- feat(scan-dialog): remove priority/arrows, sort folder list by path ([#213](https://github.com/jackal998/photo-manager/issues/213)) ([#223](https://github.com/jackal998/photo-manager/pull/223))
- feat(context-menu): remove superseded "Apply best-copy" action ([#210](https://github.com/jackal998/photo-manager/issues/210)) ([#224](https://github.com/jackal998/photo-manager/pull/224))
- feat(dialog): expose Score, Lock, Resolution in Select dialog dropdown ([#238](https://github.com/jackal998/photo-manager/issues/238)) ([#250](https://github.com/jackal998/photo-manager/pull/250))

### Miscellaneous

- build scaffold ([#1](https://github.com/jackal998/photo-manager/pull/1))
- Comprehensive Update to Photo Management Application ([#2](https://github.com/jackal998/photo-manager/pull/2))
- Refactor Application Structure and Enhance Functionality ([#3](https://github.com/jackal998/photo-manager/pull/3))
- Enhance Video Playback and Logging Features in Menu Controller ([#4](https://github.com/jackal998/photo-manager/pull/4))
- Enhance ImageService with DNG Support via rawpy ([#5](https://github.com/jackal998/photo-manager/pull/5))
- Enhance CSV Import/Export Functionality and Improve UI Interactions ([#6](https://github.com/jackal998/photo-manager/pull/6))
- Add pre-processing script and README ([#7](https://github.com/jackal998/photo-manager/pull/7))
- perf: cache manifest metadata to eliminate NAS I/O at load time ([#15](https://github.com/jackal998/photo-manager/pull/15))
- perf(sqlite): WAL mode, executemany in save(), drop dead VACUUM ([#18](https://github.com/jackal998/photo-manager/pull/18))
- perf(exif): public parse_exif_date, -fast flag, ignore settings.json ([#22](https://github.com/jackal998/photo-manager/pull/22))
- chore: remove stale docs, refresh README, drop empty core/rules stub ([#25](https://github.com/jackal998/photo-manager/pull/25))
- chore(settings): untrack settings.json, graceful missing-file fallback ([#34](https://github.com/jackal998/photo-manager/pull/34))
- chore: remove stale files from abandoned rule-engine design ([#35](https://github.com/jackal998/photo-manager/pull/35))
- chore(security): add project CLAUDE.md and Claude Code settings template ([#37](https://github.com/jackal998/photo-manager/pull/37))
- Post-audit cleanup: symlink guard + dead code + settings template gates ([#39](https://github.com/jackal998/photo-manager/pull/39))
- chore(ci): coverage gate + scanner.media tests + impact-map skill + GitHub Actions ([#44](https://github.com/jackal998/photo-manager/pull/44))
- chore(qa): drop save-to-disk screenshots, expand scenario menu ([#45](https://github.com/jackal998/photo-manager/pull/45))
- QA: UIA-driven scenario drivers for /qa-explore ([#59](https://github.com/jackal998/photo-manager/pull/59))
- chore: finish CSV-removal cleanup, drop dead closeEvent block ([#60](https://github.com/jackal998/photo-manager/pull/60))
- test(walker): pin walker-exclusions fixture to expected 2-file result ([#61](https://github.com/jackal998/photo-manager/pull/61))
- chore: delete unused PhotoVM module ([#67](https://github.com/jackal998/photo-manager/pull/67))
- qa(s01): probe first-run hint, menu-enabled state, and status bar text ([#79](https://github.com/jackal998/photo-manager/pull/79))
- chore: bump coverage fail_under from 75 to 80 ([#82](https://github.com/jackal998/photo-manager/pull/82))
- tests: lift coverage on the lowest-coverage modules ([#83](https://github.com/jackal998/photo-manager/pull/83))
- chore: honest per-file coverage gate + 3-layer testing docs ([#84](https://github.com/jackal998/photo-manager/pull/84))
- chore: bake the 3-layer testing model into every doc/skill that mentions tests ([#85](https://github.com/jackal998/photo-manager/pull/85))
- chore: QA UIA focus fix, default-batch skill, permission policy ([#89](https://github.com/jackal998/photo-manager/pull/89))
- qa(s12): drive Save Manifest end-to-end; fix [#91](https://github.com/jackal998/photo-manager/issues/91) WAL+copy data loss ([#92](https://github.com/jackal998/photo-manager/pull/92))
- qa(s13): drive Execute Action end-to-end (destructive recycle-bin send) ([#93](https://github.com/jackal998/photo-manager/pull/93))
- qa(s14): drive Set Action by Field/Regex standalone (menu-bar path) ([#94](https://github.com/jackal998/photo-manager/pull/94))
- qa(s15): drive right-click context-menu Set Action ([#95](https://github.com/jackal998/photo-manager/pull/95))
- UX consistency audit: shared helpers + cross-scenario invariants ([#96](https://github.com/jackal998/photo-manager/pull/96))
- ui: unify dismiss-button label to "Close" across primary modals ([#106](https://github.com/jackal998/photo-manager/pull/106))
- qa(s16): drive File > Open Manifest async load (happy + error) ([#107](https://github.com/jackal998/photo-manager/pull/107))
- qa(s17): drive ScanDialog _SourceListWidget operations ([#111](https://github.com/jackal998/photo-manager/pull/111))
- qa(s18): drive Log menu — happy path + not-found titles ([#101](https://github.com/jackal998/photo-manager/issues/101)) ([#115](https://github.com/jackal998/photo-manager/pull/115))
- qa(s19): drive right-click → Open Folder context-menu action ([#102](https://github.com/jackal998/photo-manager/issues/102)) ([#116](https://github.com/jackal998/photo-manager/pull/116))
- qa(s18): close spawned Notepad/Explorer windows via WM_CLOSE ([#117](https://github.com/jackal998/photo-manager/pull/117))
- test: lint qa-scenario UI label constants against app/ source ([#127](https://github.com/jackal998/photo-manager/pull/127))
- ci: add qa-batch workflow for layer-3 drift detection ([#74](https://github.com/jackal998/photo-manager/issues/74)) ([#128](https://github.com/jackal998/photo-manager/pull/128))
- chore(qa): untrack qa/settings.json drift; document [#129](https://github.com/jackal998/photo-manager/issues/129) escape hatch in skill ([#132](https://github.com/jackal998/photo-manager/pull/132))
- perf(qa): window-ready poll + UTF-8 logs on CI ([#133](https://github.com/jackal998/photo-manager/pull/133))
- chore(qa): add optional symlink fixture for s09 walker-exclusions check ([#150](https://github.com/jackal998/photo-manager/pull/150))
- qa(s25): right-click empty area / menu bar / unselected row scenario ([#124](https://github.com/jackal998/photo-manager/issues/124)) ([#152](https://github.com/jackal998/photo-manager/pull/152))
- qa(s23): scan dialog settings persist across app restart ([#122](https://github.com/jackal998/photo-manager/issues/122)) ([#153](https://github.com/jackal998/photo-manager/pull/153))
- qa(s24): open manifest with stale source paths scenario ([#123](https://github.com/jackal998/photo-manager/issues/123)) ([#154](https://github.com/jackal998/photo-manager/pull/154))
- qa(s26): keyboard-only navigation scenario ([#125](https://github.com/jackal998/photo-manager/issues/125)) ([#156](https://github.com/jackal998/photo-manager/pull/156))
- chore(hooks): enforce qa/scenarios coverage on feature PRs ([#176](https://github.com/jackal998/photo-manager/pull/176))
- ci: run qa-batch on every PR (drop paths filter) ([#177](https://github.com/jackal998/photo-manager/pull/177))
- chore(ci): add monthly pip-audit workflow ([#69](https://github.com/jackal998/photo-manager/issues/69)) ([#186](https://github.com/jackal998/photo-manager/pull/186))
- chore: enable desktop parallel-fanout workflow ([#189](https://github.com/jackal998/photo-manager/pull/189))
- chore(skill): rename parallel-fanout to parallel-brief-generator + refine ([#190](https://github.com/jackal998/photo-manager/pull/190))
- chore(skill): expand parallel-brief-generator post-merge cleanup section ([#194](https://github.com/jackal998/photo-manager/pull/194))
- chore(ci): shard qa-batch into 5 parallel jobs ([#188](https://github.com/jackal998/photo-manager/issues/188)) + s19 title-poll ([#196](https://github.com/jackal998/photo-manager/issues/196)) ([#195](https://github.com/jackal998/photo-manager/pull/195))
- chore(ci): trigger CI on PRs to any branch, not just master ([#201](https://github.com/jackal998/photo-manager/pull/201))
- chore(tests): pin qa-scenario registry completeness ([#222](https://github.com/jackal998/photo-manager/pull/222))
- chore(skill): fix venv path in parallel-brief-generator brief template ([#225](https://github.com/jackal998/photo-manager/pull/225))
- chore: gitignore .claude/skills/ entirely ([#226](https://github.com/jackal998/photo-manager/pull/226))
- chore: remove stale SKIP/MOVE wording ([#180](https://github.com/jackal998/photo-manager/issues/180)) ([#234](https://github.com/jackal998/photo-manager/pull/234))
- test: add s45 sort persistence scenario ([#121](https://github.com/jackal998/photo-manager/issues/121)) ([#235](https://github.com/jackal998/photo-manager/pull/235))
- Add UI invariants probe layer ([#243](https://github.com/jackal998/photo-manager/issues/243)) ([#246](https://github.com/jackal998/photo-manager/pull/246))
- ci(qa-batch): fail when a soft probe is ready to be promoted ([#247](https://github.com/jackal998/photo-manager/pull/247))
- test(s50): cover [#238](https://github.com/jackal998/photo-manager/issues/238) Resolution field end-to-end via expand → End → Enter ([#251](https://github.com/jackal998/photo-manager/pull/251))
- revert([#165](https://github.com/jackal998/photo-manager/issues/165) prototype): remove Execute Mode toggle ([#240](https://github.com/jackal998/photo-manager/issues/240)) ([#254](https://github.com/jackal998/photo-manager/pull/254))

