Proofread the web client copy: translate the Scan dialog, the result tree's group header and the Execute dialog's warning banners into 繁體中文, fix count lines that read "1 groups" / "1 files found" / "row(s)", replace raw backend error text with user-worded sentences, rename the menu entry to "Set Action by Field…", restore the "~dup" similarity label, and normalise zh-TW punctuation (#907).
