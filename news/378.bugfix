Wave 9b-trim on the Set Action dialog: the match counter now briefly flashes "Applied to N rows" after Apply so the user gets in-dialog confirmation without leaving the dialog (the main-window status bar still emits "Decision set to ..." as before — the two surfaces complement each other), and the action-combo label rewords from "Set Action:" to "Action for each match:" to make the per-row scope explicit (#348).
