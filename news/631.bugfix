Execute Action — release preview video handle before delete, decode HRESULT reasons in the failure dialog, and merge the singleton-prune "ask" cascade into one rebuild. Fixes the OLE error 0x80270027 / `COPYENGINE_E_SHARING_VIOLATION_SRC` that blocked deleting a previewed video file (#631).
