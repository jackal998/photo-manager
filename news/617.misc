Set-action / set-locked-state on the main duplicate-groups tree no longer rebuild the entire `QStandardItemModel` on every click — incremental cell update + O(1) path index + APPLY_ALL_UNLOCKED single-transaction collapse (#613).
