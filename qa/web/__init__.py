# qa.web — Web QA harness for the photo-manager FastAPI layer.
#
# Phase 1: scaffold only — testid constants, scenario map, parity check,
# Playwright context wrapper stubs, and a smoke test.
# Phase 2+ will add driver modules under qa/web/scenarios/.
