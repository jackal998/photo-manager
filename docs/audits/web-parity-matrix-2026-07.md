# Web parity matrix — full Qt→web closure audit (2026-07-11)

Phase-3 P3.1 deliverable (two tracks). Method: 5 read-only agents swept every feature section of
`docs/features.md` (T1, 70 rows); 4 agents triaged every open issue against the code (T2, 51
issues). Negative verdicts required repo-wide greps of `frontend/src` + `app/web` plus `#NNN`
code-comment tags (fixes merge into `docs/web-port-feasibility`, never the default branch, so
GitHub auto-close never fires — an open issue is not evidence of a gap). LEAD hand-verified all
GAP/real-gap verdicts that alter plan scope (#652, #738, #708, #703, #712, #734) and a sample of
parity rows. Baseline commit: `d3eb09c` (origin/docs/web-port-feasibility); PRs #768/#769 were
open (unmerged) at audit time.

## Headline

- **T1 — features.md sweep: 70/70 rows classified, ZERO unrecorded gaps.**
  parity 61 · deliberate-omit 4 · qt-only-n/a 5.
- **T2 — issue census: 51/51 triaged.** implemented-but-administratively-open (close-ready,
  per-row commit + file:line evidence): **28**. Real remaining work: 9 items (3 ship-relevant:
  #662, #658, #653; 5 low-priority polish: #709, #699, #713, #680, #669; 1 Qt-side-only: #757).
  Partial: #744, #743, #739, #737, #673, #622, #702, #678(tracker). Epics/trackers: #641–#647.
- **Plan-scope corrections found by this audit**: #652 (web calibration-cache read-back) is DONE
  (`bae8acb`; `app/web/models.py:227-254`, `app/web/routes/scan.py:171-198`) — the perf gate
  reduces to #737 alone. #738 (Live Photo JPG+MOV) is DONE (`5f28bb3`; `scanner/scoring.py:162`).

## Deliberate omissions — owner endorsement list (G1 sign-off)

| # | Omission | Recorded where | Capability fallback |
|---|---|---|---|
| 1 | Inline (pre-scan) source-path validation | declined in commit `eac87ca`; `s38_scan_dialog_invalid_path.py:34-40`; avoids a filesystem-disclosure endpoint | post-submit SSE `failed` event surfaces the bad path |
| 2 | List menu — Remove from List (menu-bar entry) | `MenuBar.tsx:13` | context menu + Ctrl/Shift multi-select provide the same bulk remove — **scheduled to be implemented in P3.2(c) instead of endorsed** |
| 3 | Log menu | `MenuBar.tsx:14` | scan log pane covers the live-log need; no `/api/log*` endpoint |
| 4 | Save Manifest menu entry | `MenuBar.tsx:10-12` | decisions persist synchronously per `PATCH /api/decision` (stronger than Qt's explicit save); `POST /api/save` exists backend-side |

qt-only-n/a (no endorsement needed — concept doesn't exist on the web platform): Execute-dialog
geometry persistence (s48 note), Set-Action-dialog geometry persistence, Exit dirty-flag prompt
(no dirty state + no custom close dialog in a browser), Qt row-selection auto-scroll bug class,
developer memory probe (Qt-object instrumentation, removed by design `web-port-tech-design.md:1318`).

## Close-ready issues (28, per-row evidence in the census tables below)

#764 #757→(no, real-gap) — see tables; the close-ready set is:
#764, #742, #741, #740, #738, #736 (T2-1) · #735, #734, #733, #721, #718, #712, #708, #703 (T2-2)
· #697, #694, #692, #689, #688, #687, #686, #685, #676, #674 (T2-3) · #661, #652, #651, #641 (T2-4).
Each row in the census tables carries the fixing commit + live file:line. Closing is the owner's
call (or agent per-issue with explicit yes).

## Notes captured en route (not gaps)

- `qa/web/scenario_map.yml` s44 key name is stale (`s44_execute_highlighted_rows` vs module
  `s44_execute_selected_only`) — resolves correctly, rename at next touch.
- Test-coverage (not feature) gaps: scan progress row and manifest-summary log table have code
  parity but no dedicated web scenario assertion; no scenario asserts the no-autoplay negative.
- #702 item 4 ≡ #713 (same s61 lock-branch variant) — one gap, not two.

---

The full per-row tables follow: T1-A..E (features.md sweep), then T2-1..4 (issue census).


---

<!-- parity-T1A -->

# Parity matrix — Slice T1A (features.md lines 91-268)

| Feature (features.md line) | Verdict | Evidence | Notes |
|---|---|---|---|
| Bulk regex — remove from list (deferred decision) (L91) | parity | `frontend/src/components/action/ActionDialog.tsx:97` (`{ key: "remove_from_list", wireValue: "__ignore__" }`); qa scenario `qa/web/scenarios/s29_remove_from_list_by_regex.py` (`qa/web/scenario_map.yml:164-167`, status done) | Web ActionDialog's regex/field bulk-decide action dropdown carries a "remove from list" option wired to `__ignore__`, mirroring Qt's `IGNORE_DECISION`. |
| Context menu — Lock / Unlock (L102) | parity | `frontend/src/components/ContextMenu.tsx:246-264` (`CTX_LOCK`/`CTX_UNLOCK` toggle by `isLocked`, calling `setLocks`); qa scenario `qa/web/scenarios/s35_lock_via_context_menu.py` (`qa/web/scenario_map.yml:194-197`, status done) | Right-click Lock/Unlock idempotent toggle on `is_locked`, same semantic as Qt. |
| Context menu — Open Folder (L113) | parity | `frontend/src/components/ContextMenu.tsx:143-146,265-272` (`CTX_OPEN_FOLDER` → `revealInExplorer` → `POST /api/reveal`); qa scenario `qa/web/scenarios/s19_context_menu_open_folder.py` (`qa/web/scenario_map.yml:109-112`, status done) | Reveal-in-OS-file-manager round trip; 501 on non-Windows CI is an accepted platform-dependent status per the scenario note, not a gap. |
| Context menu — Set Action (per-file) (L124) | parity | `frontend/src/components/ContextMenu.tsx:107-131,221-244` (`CTX_SET_ACTION_KEEP`/`DELETE`/`REMOVE` → `setDecisions`); qa scenario `qa/web/scenarios/s15_context_menu.py` (`qa/web/scenario_map.yml:89-92`, status done) | Keep/Delete/Remove-from-list per-row context items present; multi-select Ctrl+click fan-out covered by s20 (`qa/web/scenario_map.yml:114-117`). "Set Action by Field…" and "Execute Action (only selected)…" post-#735 items also present at `ContextMenu.tsx:173-182,278-291`. |
| Empty area context menu (L135) | parity | `frontend/src/components/ResultTree.tsx:336,352` (`onContextMenu` wired only on `GroupRow`/`FileRow`, never on the tree viewport container) + `frontend/src/App.tsx:393` (passes `onContextMenu` only into `<ResultTree>` rows); qa scenario `qa/web/scenarios/s25_empty_area_context_menu.py` (`qa/web/scenario_map.yml:144-147`, status done) | Structural guard: no context-menu handler on empty tree area or menu-bar gap — scenario asserts absence (count==0) on those surfaces, matching Qt's distinct-menu-for-empty-area intent (here: no menu at all on empty area, browser-native fallback). |
| Empty-state action buttons (L146) | parity | `frontend/src/App.tsx:365-390` (`MAIN_EMPTY_STATE` with `MAIN_EMPTY_SCAN` → opens Scan dialog, `MAIN_EMPTY_OPEN` → opens manifest browser); qa scenario `qa/web/scenarios/s41_empty_state_action_buttons.py` (`qa/web/scenario_map.yml:236-239`, status done) | Two buttons (Scan…, Open Manifest…) shown pre-manifest, same end-states as the menu items, matching Qt's Scan Sources.../Open Manifest... pair. |
| Execute Action — base flow (L157) | parity | `frontend/src/components/execute/ExecuteDialog.tsx`; `infrastructure/winerror_reasons.py`; `frontend/src/components/execute/ExecuteResultSummary.tsx` (per features.md's own Web-parity #742 citation, L164) | Feature entry already documents web parity directly (issue #742) with file:line anchors verified present on disk (`ExecuteResultSummary.tsx` exists per Glob). |
| Execute Action — complete-group delete confirm (L169) | parity | `frontend/src/components/execute/ExecuteDialog.tsx` (`completeDeleteGroupIds`) + `frontend/src/components/dialogs/DeleteConfirmDialog.tsx` (per features.md's own Web-parity #733 citation, L176) | Feature entry documents web parity directly; qa scenario `qa/web/scenarios/s13_execute_action.py` mixed-manifest phase (`qa/web/scenario_map.yml:79-82`). |
| Execute Action — complete-group warning banner with jump-to (L181) | parity | `frontend/src/components/execute/AllDeleteBanner.tsx:1-51` (`EXECUTE_ALL_DELETE_BANNER`, per-group `executeAllDeleteJumpTestid` anchor buttons, `onJumpToGroup` prop) wired via `ExecuteDialog.tsx:278 handleJumpToGroup` and `:457 onJumpToGroup={handleJumpToGroup}`; qa scenario `qa/web/scenarios/s33_execute_dialog_jump_to_all_delete.py` (`qa/web/scenario_map.yml:184-187`, status done) | Web banner uses real `<button>` jump anchors (scenario notes this "IMPROVES on Qt" since Qt's QLabel `<a>` couldn't be clicked on hosted CI). |
| Execute Action — dialog geometry persistence (L192) | qt-only-n/a | Rationale recorded at `qa/web/scenario_map.yml:277-280` (s48, status skip): "Web dialogs are Radix / DOM overlays with no user-resizable geometry and no persistence contract, so there is nothing to round-trip." | Desktop QDialog `saveGeometry`/`QSettings` round-trip has no browser analog — dialogs aren't OS windows with user-draggable chrome. The scenario_map note explicitly distinguishes this (real skip) from the *adjacent* panel/column-width persistence features (s39, s47) which ARE real ported web features, so this isn't scope-avoidance. |
| Execute Action — lock-confirm dialog (L203) | parity | `frontend/src/components/dialogs/LockConfirmDialog.tsx`; `ActionDialog.tsx` inline `ACTION_LOCK_CONFIRM_DIALOG` (per features.md's own Web-parity #733/#721/#764 citations, L215-217) | Feature entry documents three rounds of web parity work directly with file:line anchors; qa scenarios s32/s34/s36/s53 (`qa/web/scenario_map.yml:179-202,302-305`) all status done. |
| Execute Action — preview pane (L222) | parity | `frontend/src/components/execute/ExecuteDialog.tsx:8` (module header cites `execute-preview-pane`), `:34-35` (`EXECUTE_PREVIEW_PANE`, `EXECUTE_PREVIEW_IMAGE` testids), `:482,487` (rendered elements); qa scenario `qa/web/scenarios/s51_execute_dialog_preview.py` (`qa/web/scenario_map.yml:292-295`, status done) | Row-click renders `execute-preview-image` inside the dialog, same intent as Qt's embedded `PreviewPane`. |
| Execute Action — partial execution via "Execute selected" (L233) | parity | `frontend/src/components/execute/ExecuteDialog.tsx:355-362` (`handleExecuteSelected` → `scopePaths = sel.selectedPaths` → `executeDecisions({scopePaths})`, gated on the in-dialog multi-select), button at `:531,534`; qa scenario `qa/web/scenarios/s64_execute_selected_partial.py` (`qa/web/scenario_map.yml:393-396`, status done) | This is the IN-DIALOG "Execute selected" button (multi-select rows inside ExecuteDialog, dialog stays open) — distinct from the main-window "Execute (only selected)" entry point covered by the next row. Scenario confirms unselected rows survive untouched and dialog stays open, matching Qt. |
| Execute Action — filter by action type (#502) (L244) | parity | `frontend/src/components/execute/TypeFilter.tsx`; `frontend/src/components/execute/HiddenDestructiveBanner.tsx` (per features.md's own Web-parity #676 citation, L251) | Feature entry documents web parity directly; qa scenario `qa/web/scenarios/s60_execute_filter_by_action_type.py` (`qa/web/scenario_map.yml:337-340`, status done). |
| Execute Action — scope to selected groups (only-selected entry, #410/#430/#429) (L256) | parity | `frontend/src/components/MenuBar.tsx:41,143` (`web.menu.action_execute_selected` → "Execute (only selected)…", gating logic referenced at `App.tsx:196`); `frontend/src/components/execute/ExecuteDialog.tsx:145,195-205,323-353` (`scopeGroupNumbers` state → group-level filter of `groups`, i.e. #430 group-pull semantics) + `frontend/src/components/ContextMenu.tsx:157-163,282-291` (`CTX_EXECUTE_SELECTED` context-menu sibling, #735/#429 analog); qa scenario `qa/web/scenarios/s44_execute_selected_only.py` (`qa/web/scenario_map.yml:251-254`, status done) | Both affordances present: menu-bar "Execute (only selected)…" AND a right-click "Execute Action (only selected)…" context item. Scope is by group membership (`scopeGroupNumbers`), matching Qt's #430 group-pull, not a per-row filter. |

## Summary

- **parity:** 14
- **deliberate-omit:** 0
- **GAP:** 0
- **qt-only-n/a:** 1 (Execute Action — dialog geometry persistence)

## GAP grep patterns tried (none escalated to GAP; recorded for completeness)

No row in this slice required a GAP verdict, but the following searches were run across `frontend/src` and (where relevant) `app/web` / `qa/web/scenario_map.yml` before any verdict was finalized, per the METHOD requirement:

- `__ignore__|IGNORE_DECISION|remove from list` (bulk regex remove)
- `#158` (bulk regex remove issue tag — zero hits in frontend/src; parity established via wireValue + scenario instead)
- `onContextMenu` (empty area context menu — confirmed absence on container, presence only on rows)
- `Scan Sources|Open Manifest|main-empty-state|EmptyState` (empty-state action buttons)
- `jump-to|jump_to|all-delete-jump|AllDeleteBanner` (warning banner jump-to)
- `PreviewPane|execute-preview|EXECUTE_PREVIEW` (execute dialog preview pane)
- `execute-selected-btn|EXECUTE_SELECTED_BTN|handleExecuteSelected|Execute selected` (partial execute button)
- `menu-action-execute-selected|scopeGroupNumbers|Execute \(only selected\)` (scope to selected groups)
- Cross-checked every scenario name above against `qa/web/scenario_map.yml` (single source of truth for Qt→web scenario porting status).


---

<!-- parity-T1B -->

# Feature-parity matrix — docs/features.md lines 269-477 (Slice T1B)

| Feature (features.md line) | Verdict | Evidence | Notes |
|---|---|---|---|
| Exit dirty-flag prompt (269) | qt-only-n/a | `qa/web/scenario_map.yml` s28_exit_dirty_prompt (line 159-162): "SKIP — desktop-only (#688) ... A browser tab has no interceptable OS close with a custom dialog ... there is no web analog for the Save / Back branches." Searched `frontend/src` for `beforeunload`/`dirty` handling tied to decisions — only scan-running guard exists (see close-during-scan row below), no dirty-decision guard. | Architecturally moot too: web decisions persist synchronously per-PATCH (`app/web/routes/decision`; see s12 note), so there is no "dirty" state left to protect on close — not merely a browser-API gap. |
| Singleton-prune offer after destructive ops #426 (280) | parity | `features.md:289` "Web parity (#686)" section cites `maybeOfferPrune` store action, `POST /api/prune/candidates` (`core.app_service.execute_service.classify_singletons`), `PruneConfirmDialog`, `LockConfirmDialog op="prune"`, `POST /api/prune`. QA: `qa/web/scenarios/s61_actioned_singleton_prune.py` + `qa/web/scenarios/s67_locked_singleton_prune_always.py`, both `status: done` in `qa/web/scenario_map.yml` (lines 342-424). | Grepped scenario_map.yml directly (not inferred from features.md alone) — both entries confirmed `status: done` with matching module paths. |
| Keep-worthiness scoring (294) | parity | Score rendered at `frontend/src/components/result/FileRow.tsx:138-140` (`data-col="score"`, `formatScore(row.score)`). Scoring itself is computed server-side by the shared `scanner/scoring.py` pipeline (same manifest backend both apps share) and surfaced via the manifest API. QA: `qa/web/scenarios/s42_scoring.py`, `status: done` (`qa/web/scenario_map.yml:241-244`). | Doc itself flags a narrower scope: "COMPOSITE-SCORE SCOPE ONLY — per-dimension signals gps_present/exif_tag_count/xmp_derived NOT serialised in FileRow ... deferred to #680" — the aggregate score has parity, per-dimension breakdown does not (tracked separately, not a silent gap). |
| Language switch (305) | parity | `features.md:314-326` "Web variant" subsection with explicit anchors: `frontend/src/i18n/useI18nStore.ts`, `GET /api/i18n/{locale}` (`app/web/routes/i18n.py`), `PATCH /api/settings` (`app/web/routes/settings.py`). QA: `qa/web/scenarios/s22_language_switch.py` + `s58_language_switch_preserves_manifest.py`, both `status: done` (`qa/web/scenario_map.yml:124-127, 327-330`). | Verified: no confirm dialog on web (documented divergence, not a gap — React never destroys/recreates the tree on switch so the #428 regression class doesn't apply). |
| List menu — Remove from List (330) | deliberate-omit | Deferral recorded at `frontend/src/components/MenuBar.tsx:13` ("List → Remove from List: needs a remove-by-regex / selection entry point") and `qa/web/scenario_map.yml` s21_list_menu_remove (lines 119-122): "SKIP — no web equivalent surface ... underlying behaviour ... is covered by s20's context-menu multi-selection port (PR-D1). Building a top-level List menu solely to host one item the context menu already provides would be over-fitting." | Grepped `frontend/src` for "List" menu / `MENU_LIST` token — none found, confirming no menu-bar surface exists. The underlying "remove from list" ACTION has full parity via the right-click context menu (`qa/web/scenarios/s20_multi_remove_from_list.py`, `s54_execute_dialog_remove_from_list.py`, `s29_remove_from_list_by_regex.py`, all `status: done`) — only the menu-bar entry point is omitted. |
| Log menu (341) | deliberate-omit | Deferral recorded at `frontend/src/components/MenuBar.tsx:14` ("Log menu: depends on the web OS-integration (reveal/open) story") and `qa/web/scenario_map.yml` s18_log_menu (lines 104-107): "SKIP — no web equivalent surface ... `app/web/routes/` has no `/api/log*` endpoint and the web layer writes no on-disk app log file." | Grepped `app/web/routes/` (via prior repo knowledge cross-checked against scenario_map note) and `frontend/src` for "log" menu tokens — none found beyond the scan-progress log viewer, confirming no diagnostics-log surface exists. No issue number attached (open product decision, not yet filed as tracked issue per the note). |
| Main window — close-during-scan confirm (352) | parity | `features.md:359` "Web parity (#687)" section cites `useBeforeUnloadGuard` (mounted in `App`), `frontend/src/hooks` (native `beforeunload` handler gated on `scan.status === "running"`), `pagehide`→`navigator.sendBeacon('/api/scan/{id}/cancel')` for #703 cancel-on-leave. QA: `qa/web/scenarios/s63_late_cancel_and_main_window_guard.py`, `status: done` (`qa/web/scenario_map.yml:367-391`) + a `useBeforeUnloadGuard` vitest unit test. | Documented residual: `sendBeacon` cancel is best-effort by spec; accepted as a known limitation vs. the prior 100%-orphan baseline, not a silent gap. |
| Main window — column order/width persistence (364) | parity | `features.md:394` cross-reference: "mirrors the #685 column-resize recipe" at `frontend/src/components/result/ColumnHeaderRow.tsx` + `frontend/src/lib/columnWidths.ts`; store action confirmed at `frontend/src/store/useAppStore.ts:546,552` (`saveColumnWidths`). QA: `qa/web/scenarios/s47_column_layout_persist.py`, `status: done` (`qa/web/scenario_map.yml:267-275`, "#685 column model. Real mouse-drag ... localStorage persist ... page.reload() ... width restores"). | localStorage is the documented per-browser analog of `window_state.ini`; auto-isolated per Playwright context. |
| Main window — geometry+splitter persistence (375) | qt-only-n/a | `qa/web/scenario_map.yml` s39_window_geometry_persist (lines 214-229): "Qt window-geometry round-trip ... still has no browser analog — window chrome belongs to the browser, not the app." | Split entry: the WINDOW-geometry half is inherently desktop-only (no browser API to move/query the browser chrome itself). The SPLITTER half is *not* a gap — it was re-flipped into its own web-native section "Web — preview panel resizable" (#739, features.md:386) with real parity (see next row); scenario s39 itself now runs `qa.web.scenarios.s39_layout_persist` covering that half. Column widths are covered separately by s47 (row above). |
| Web — preview panel resizable + persistence (386) | parity (anchors verified) | `frontend/src/App.tsx:171,183,188,193` confirms `setPreviewWidth` wiring (`s.setPreviewWidth`, drag-move commits `persist=false`/`true`); `PREVIEW_RESIZE_HANDLE` testid = `"preview-resize-handle"` defined at `frontend/src/testids.ts:88`, matching the doc's cited `data-testid="preview-resize-handle"`. Store persistence at `frontend/src/store/useAppStore.ts:565,571` (`savePanelWidths`). QA: `qa/web/scenarios/s39_layout_persist.py`, `status: done` (`qa/web/scenario_map.yml:214-229`). | Web-native section — no Qt equivalent to check against; verified the file:line anchors in the doc still hold against current source rather than trusting the doc's own citation. |
| Main window — keyboard navigation (398) | parity (honest partial, documented) | `features.md:406` "Web parity (#708)" section. Verified live: `frontend/src/App.tsx:15` imports `useDecisionShortcuts`, `frontend/src/App.tsx:232` calls it (anchor holds). QA: `qa/web/scenarios/s26_keyboard_navigation.py`, `status: done`, scenario_map.yml note (lines 149-152) explicitly labeled "HONEST PARTIAL — the DECISION shortcuts (Qt step 0, #615) only." | Doc + scenario_map.yml both explicitly enumerate the omitted sub-behaviors as separate tracked items, not silent gaps: arrow-key roving focus through the virtualizer (deferred, #709), `p` play/pause (a PreviewPane-specific concern, not ported), Alt+F mnemonic/Tab-cycle/Esc-dismiss (Radix/browser own these natively). Verdict is "parity" for the d/k decision-shortcut behavior this row's KNOWN CONTEXT flagged; the omitted sub-items are each individually tracked, not GAPs. |
| Main window — results tree double-click (412) | parity | Group-row toggle: `frontend/src/components/result/GroupRow.tsx:37` (`onClick={onToggle}`), `:43` (`aria-expanded={expanded}`) — toggles on single click per web idiom (documented Qt divergence). File-row: `frontend/src/components/result/FileRow.tsx:88-89` (`onClick={handleClick}`, `onDoubleClick={handleDoubleClick}`) opens the in-browser full-res viewer (web analog of Qt's OS-viewer spawn — browsers can't shell out to native apps). QA: `qa/web/scenarios/s40_results_tree_double_click.py` (group toggle) + `qa/web/scenarios/s68_full_res_viewer_double_click.py` (file open), both `status: done` (`qa/web/scenario_map.yml:231-234, 426-429`). | Documented divergence: web toggles the group row on single click, not double-click (Qt divergence note in s40); functional intent (expand/collapse + open-file) preserved. |
| Main window — row selection, no horizontal auto-scroll (423) | qt-only-n/a | Grepped `frontend/src` for `autoScroll`, `scrollIntoView`, `scrollToItem`, `FixedSizeList`/`react-window` in `frontend/src/components/result/*` — zero matches. No virtualizer and no programmatic scroll-on-select exists in the web tree at all. | The Qt behavior being ported is disabling a *Qt-specific* default (`QTreeView.autoScroll` calling `scrollTo(current)` on selection change) — the web result list has no equivalent auto-scroll-on-select behavior to begin with, so there is nothing to suppress. Inherently not portable as a "fix" because the bug class doesn't exist on this stack. |
| Main window — sort persistence within session (434) | parity | `frontend/src/store/useAppStore.ts:123` (`sortDirection: "asc"` default), `:529-536` (`resultView.sortColumn`/`sortDirection` toggle logic in the Zustand store — survives manifest reopen since it's a separate store slice, not reset on model reload). QA: `qa/web/scenarios/s45_sort_persistence.py`, `status: done` (`qa/web/scenario_map.yml:256-265`): "Sort lives in the resultView store slice so it survives an in-session manifest reopen (Qt refresh_model replay analog)." | Doc's own within-session/across-launch distinction holds on web too: cross-launch (page reload) sort reset is not claimed as persisted — matches Qt's documented "resets on app restart" limitation, so no divergence beyond what Qt itself already has. |
| Main window — status bar baseline (445) | parity | `frontend/src/App.tsx:420` renders `data-testid={MAIN_STATUS_BAR}` (`MAIN_STATUS_BAR = "main-status-bar"`, `frontend/src/testids.ts:4`); `frontend/src/App.test.tsx:246,303` assert it shows `"Ready"` on startup (never blank). QA: `qa/web/scenarios/s37_status_bar_baseline.py`, `status: done` (`qa/web/scenario_map.yml:204-207`): "Asserts idle on startup then 'N groups · M files' after scan." | Qt's specific historical regressions (#138 3s-timeout blank-out, #140 menu-hover clearing the bar) have no web equivalent to regress against — noted in scenario_map as "omitted with divergence note," not a gap, since the underlying persistent-baseline-label INTENT is what's being ported and it holds. |
| Open Manifest — base flow (456) | parity | `frontend/src/components/FsBrowser.tsx` (file-picker replacement for native `QFileDialog`) confirmed present via grep; wired from `MenuBar.tsx` (`MENU_FILE_OPEN` → `onOpenManifest`, lines 102-108) and the empty-state Open button. QA: `qa/web/scenarios/s16_open_manifest.py`, `status: done` (`qa/web/scenario_map.yml:94-97`): "Scan→reload→reopen the .db manifest through the FsBrowser file picker (empty-state Open); asserts post-reopen status bar equals post-scan." | Qt's native `QFileDialog` → in-DOM `FsBrowser` picker is a documented, not silent, UI-mechanism divergence; behavioral intent (pick a `.sqlite` manifest, load it, refresh tree, update status) preserved per the scenario notes. |
| Save Manifest Decisions — base flow (467) | deliberate-omit | Deferral recorded at `frontend/src/components/MenuBar.tsx:10-12`: "Save Manifest: backend IS plumbed (POST /api/save + store.saveManifest), but the web persists decisions live via PATCH /api/decision, so whether an explicit 'save snapshot' is meaningful needs a product decision." `qa/web/scenario_map.yml` s12_save_manifest (lines 74-77, `status: done` but reframed): "The save-as/export-snapshot product question (POST /api/save target_path as a UI flow) is unevidenced → filed as a deferred follow-up, not built here." | Nuanced: the underlying INTENT (decisions survive/persist) has stronger-than-parity coverage — `PATCH /api/decision` commits synchronously per-change (no dirty window at all) and is proven durable across reload+reopen in s12. What's specifically omitted is the explicit "File > Save Manifest Decisions..." menu entry / save-as-a-different-path UI flow — the backend endpoint (`POST /api/save`) exists but has no wired UI trigger. Grepped `frontend/src` for `MENU_FILE_SAVE` / "Save Manifest" tokens — none found, confirming no menu item exists. |

## Grep patterns used (for GAP-eligibility / deliberate-omit checks, all rows)

- `MenuBar.tsx` full read (deferral comments block, lines 1-16)
- `qa/web/scenario_map.yml` full read (all skip/done notes cross-checked)
- `frontend/src` grep: `autoScroll`, `scrollIntoView`, `scrollToItem`, `FixedSizeList|VariableSizeList|react-window`
- `frontend/src` grep: `status-bar|statusBar|StatusBar` and `MAIN_STATUS_BAR`
- `frontend/src` grep: `FsBrowser|fs-browser|openManifest|OpenManifest`
- `frontend/src` grep: `preview-resize-handle|setPreviewWidth`, `PREVIEW_RESIZE_HANDLE`
- `frontend/src/components/result` grep: `score` (all .tsx), `onDoubleClick|dblclick|onClick` (FileRow.tsx), `onClick|toggle|aria-expanded` (GroupRow.tsx)
- `frontend/src/store/useAppStore.ts` grep: `sortField|sortDir|resultView`
- No "#NNN" issue-comment grep came back empty for any row in this slice — every deliberate-omit/qt-only-n/a row above has an explicit citation (MenuBar.tsx comment and/or scenario_map.yml skip note and/or features.md prose), so none required the "no recorded deferral found" fallback.


---

<!-- parity-T1C -->

# Web-port parity matrix — Scan dialog / Scan flow (docs/features.md:478-637)

Repo checked: `.claude/worktrees/web-port-phase2` (≈ `docs/web-port-feasibility`).
Method: for every section, grepped `frontend/src` + `app/web` for the section's
tokens/symbols, grepped for the section's issue number(s) as `#NNN` in code
comments, and checked `qa/web/scenario_map.yml` for a matching driver.

| Feature (features.md line) | Verdict | Evidence | Notes |
|---|---|---|---|
| Scan dialog — auto-select after scan (478) | parity | `frontend/src/components/ScanDialog.tsx:170` (`aggressiveDelete` sibling `autoSelect` state), `:413` (`auto_select_enabled` on request), `:645-661` (checkbox UI); qa: `qa/web/scenarios/s49_scan_auto_select.py` (status: done) | Manifest-level assertions match Qt spec: keeper `action='KEEP'` + `is_locked` + `user_decision=''` (#425 canonical-keep). Searched: `auto_select_enabled`, `autoSelectEnabled`, `SCAN_AUTO_SELECT`. |
| Scan dialog — auto-select aggressive ("delete all others") (489) | parity | `frontend/src/components/ScanDialog.tsx:170` (`aggressiveDelete`), `:415` (`auto_select_aggressive_delete`), `:663-677` (indented sub-checkbox, gated on parent); `frontend/src/api/types.ts:123`; qa: `qa/web/scenarios/s57_scan_auto_select_aggressive.py` (done) | scenario_map notes confirm the platform-dependent classification (#517/#536 confidence + Ref-tier exclusion) is asserted via classifier action, matching the doc's eligibility rules. Searched: `auto_select_aggressive`, `autoSelectAggressive`, `delete all others`. |
| Scan dialog — stage / throughput / ETA progress (#424) (500) | parity | `frontend/src/components/scan/ScanProgress.tsx` (header comment "#740"; `SCAN_STATUS_TEXT`/`SCAN_THROUGHPUT_TEXT`/`SCAN_ETA_TEXT` at :62-79); `frontend/src/lib/scanProgress.ts:11-27` (stage map mirrors `core/app_service/scan_runner.py` `STAGE_WALK`/`STAGE_HASH`/`STAGE_EXIFTOOL`/`STAGE_CLASSIFY`/`STAGE_SCORE`/`STAGE_WRITE`); `frontend/src/hooks/useScanSSE.ts:107` (`"log"` event ingestion) | No dedicated qa/web scenario asserts the progress bar/throughput/ETA row itself (s01-s71 exercise adjacent flows but not this row directly) — code-level parity confirmed via matching stage constants + SSE wiring; not scenario-covered. Searched: `stage_progress`, `stageProgress`, `files_per_sec`, `filesPerSec`, `ETA`, `files/sec`. |
| Scan dialog — collapse Advanced Settings (511) | parity | `frontend/src/components/ScanDialog.tsx:620-627` (`<details>`/`<summary data-testid={SCAN_ADVANCED}>`, no `open` attr ⇒ collapsed by default, matching Qt "opens collapsed every time — not persisted"); `:707-712`, `:733-738` (short one-line description + full detail in `title=` hover tooltip, mirroring Qt's `_tip()` short-desc-plus-tooltip design) | Interaction is exercised indirectly by every scenario that expands Advanced settings (s49, s57, s66). Searched: `Advanced [Ss]ettings`, `collapsible`, `disclosure`. |
| Scan dialog — folder list (no priority arrows) (522) | parity | `frontend/src/components/scan/SourceRow.tsx` (3-field row: label/path input, Recursive checkbox, ✕ remove — no ↑/↓ arrow buttons); qa: `qa/web/scenarios/s17_scan_dialog_widgets.py` (done) | scenario_map s17 note: "sources added out of order render alphabetized (display-only) while the POST body's source order stays original insertion order" — matches features.md:526 exactly (alphabetical display, insertion-ordered underlying list). Searched: `source list`, `SourceList`, `sources\.list`, `recursive`. |
| Scan dialog — exiftool workers (setting-only) (533) | parity | `app/web/models.py:218,256-269` (`WebScanRequest.exif_workers`, reads `settings.get("scan.exif_workers", 2)`, same key + default as `scan_dialog.py`); `app/web/routes/settings.py:34` (key exposed in settings surface only, no dedicated dialog control — matches Qt "no UI"); `tests/test_web_scan_routes.py:583-613` (exif-workers read-back + int-coerce-or-2-fallback tests) | No UI on either platform by design; parity = same settings.json key + same resolution/clamp semantics, test-verified. Searched: `exif_workers`, `exifWorkers`. |
| Scan dialog — read-knee autotune opt-out (544) | parity | features.md:550-552 itself documents "**Web parity (#652)**" citing `app/web/models.py` `WebScanRequest.resolved_with()` + `app/web/routes/scan.py:232`; `frontend/src/components/ScanDialog.tsx:629-643` (`SCAN_AUTOTUNE` checkbox, default ON / opt-out); qa: `qa/web/scenarios/s66_autotune_read_knee.py` (done) | Doc-declared parity (not just inferred) — the feature entry carries its own "Web parity" subsection. Searched: `autotune_read_knee`, `autotuneReadKnee`, `read.?knee`. |
| Scan dialog — hash pool mode (556) | parity | features.md:565 itself documents "**Web parity (#652)**" citing `app/web/models.py` `WebScanRequest.resolved_with()` + `app/web/routes/scan.py`; `tests/test_web_scan_routes.py::TestCalibrationReadback` (per doc's Related line) | Doc-declared parity, same fingerprint-keyed cache read-back as the Qt dialog. Searched: `hash_pool`, `hashPool`. |
| Scan dialog — hash workers (NAS-aware auto, no UI control) (571) | parity | `core/app_service/scan_runner.py:554,814,821,880` (`from scanner.workers import device_key, hash_workers_for_root`); `app/web/routes/scan.py:19` (`from core.app_service.scan_runner import run_pipeline` — the SAME engine powers both Qt's `ScanWorker` and the web's SSE scan route) | No frontend surface needed — Qt itself has "no UI control" for this (spinbox was removed). Parity is by shared backend: the per-device NAS/HDD/SSD pool sizing logic in `scanner/workers.py` runs identically regardless of caller. Searched: `hash_workers`, `hashWorkers`, `default_hash_workers` (frontend: no hits, as expected). |
| Scan dialog — multi-source scan (583) | parity | `frontend/src/components/scan/SourceRow.tsx` + `ScanDialog.tsx` add-source flow; qa: `qa/web/scenarios/s10_multi_source.py` (done, asserts source-order priority + cross-source dedup), `qa/web/scenarios/s17_scan_dialog_widgets.py` (done) | s10 note confirms source-order-as-priority semantics ported faithfully. Searched: `source list`, `SourceList`, `sources\.list`. |
| Scan flow — manifest summary in progress log (594) | parity | `core/app_service/scan_runner.py:1198-1202` (`redirect_stdout` captures `scanner.manifest.print_summary()`, each line forwarded via `bus.log(line)`); `app/web/routes/scan.py:136` (`SseScanBus._append_and_fanout("log", {"msg": msg})`); `frontend/src/hooks/useScanSSE.ts:107` (`"log"` SSE event ingestion) → `ScanProgress.tsx` `SCAN_PROGRESS_LOG` | Same `print_summary()` text (identical action-bucket table) reaches the web log pane via SSE. No qa/web scenario asserts the summary-table text itself specifically — `s01_happy_path.py` asserts a *different* string (status-bar "N groups · M files", #58), not this progress-log table — so this is code-parity without scenario coverage. Searched: `manifest summary`, `Manifest Summary`, `print_summary`, `manifest_summary`. |
| Scan walk — skip-on-error traversal (605) | parity | `core/app_service/scan_runner.py:542` (`from scanner.walker import scan_sources`) — the resilient `os.scandir`-stack traversal (try/except OSError, symlink/skip-dir guards) lives in `scanner/walker.py` itself and is consumed identically by both the Qt `ScanWorker` and the web's `run_pipeline` | Backend-only feature, no frontend surface in either app. Parity by shared engine, not by porting. Searched: `_iter_tree`, `scan_sources`, `walker\.py` inside `core/app_service/scan_runner.py`. |
| Scan flow — rescan confirm (616) | parity | `frontend/src/components/dialogs/RescanConfirmDialog.tsx`; `frontend/src/App.tsx`, `frontend/src/store/types.ts` (pending-decision gating); qa: `qa/web/scenarios/s27_rescan_confirm.py` (done) | scenario_map s27 note: "Discard pending decisions?" gate (#142), Cancel keeps dialog open + no scan, Discard rebuilds manifest — matches features.md:616-623 (Cancel keeps loaded manifest intact, Confirm proceeds). Searched: `rescan.?confirm`, `RescanConfirm`, `loaded manifest`. |
| Scan flow — visual selection of KEEP rows after scan (627) | parity | `frontend/src/components/ResultTree.tsx:169-179` (`scrollToPath` effect, comment "Post-scan keeper scroll (Qt #239 parity)"); `frontend/src/store/useAppStore.ts:279,285-289` (`loadManifest({selectKeepers})` selects `action==="KEEP"` rows); `frontend/src/store/types.ts:106,297-303`; qa: `qa/web/scenarios/s49_scan_auto_select.py` (done — explicitly asserts `aria-selected='true'` on the keeper row post-scan, citing #692) | #692 is the tracking issue for this exact port; both code comments and the qa scenario cite it directly. Searched: `#692`, `selectKeepers`, `autoSelected`. |

## Summary

- **parity: 14 / 14** — every section in the range has a working web-app
  equivalent, backed by either (a) direct frontend code + a `qa/web` driver,
  (b) a doc-declared "Web parity (#652)" subsection already in features.md
  (read-knee autotune, hash pool mode), or (c) shared-backend inheritance
  (hash workers NAS-aware auto, skip-on-error walker traversal — both are
  backend-only features with no UI on either platform, powered by the same
  `core/app_service/scan_runner.py` → `scanner/*.py` engine for Qt and web).
- **deliberate-omit: 0**
- **GAP: 0**
- **qt-only-n/a: 0**
- Two sections (stage/throughput/ETA progress; manifest summary in progress
  log) have solid code-level parity but **no dedicated qa/web scenario**
  asserting that specific surface — flagged in their Notes column as a
  scenario-coverage gap, not a feature GAP.


---

<!-- parity-T1D -->

# Parity matrix — Set Action dialog cluster + Web-native cluster (features.md:638-811)

| Feature (features.md line) | Verdict | Evidence | Notes |
|---|---|---|---|
| Set Action dialog — dual-section Simple + Regex view (638) | parity | Web analog explicitly documented at features.md:709 ("String mode shows both `action-simple-row` and `action-regex-row` side-by-side … matches Qt's Phase C dual-section view"); testids confirmed live in `frontend/src/testids.ts:104,108` (`ACTION_SIMPLE_ROW`, `ACTION_REGEX_ROW`) and used in `frontend/src/components/action/ActionDialog.tsx`. QA: `qa/web/scenarios/s31_simple_mode_regex.py` (scenario_map.yml status: done). | Web dropped the Recent-button-as-dropdown chrome distinction but ported Recent-patterns semantics separately under #741 (see row below); dual-section layout itself is 1:1. |
| Set Action dialog — live preview + validation (650) | parity | Web: `action-match-counter`, `action-preview-list`, `action-validation-icon` all defined in `frontend/src/testids.ts:110,120,123` and wired in `frontend/src/components/action/ActionDialog.tsx`; `POST /api/action/bulk-decide?preview=true` documented features.md:703. Match semantics identical (share `resolve_matched_paths` — `core/app_service/action_resolve.py`). QA: `qa/web/scenarios/s31_simple_mode_regex.py`, `s14_action_by_regex.py` (both status: done). | Apply-always-enabled parity confirmed live in code (#741, see below) — matches Qt's #397 posture cited in this Qt section's own "Last verified" line. |
| Set Action dialog — numeric comparison panel (661) | parity | Web: `action-numeric-row`, `action-numeric-mode-threshold`, `action-numeric-mode-topn` defined `frontend/src/testids.ts:112-114`, `NUMERIC_FIELDS` set at `frontend/src/components/action/ActionDialog.tsx:84-92` including Score/Size/Group Count/Similarity/Creation Date/Shot Date. Pseudo-pattern encoding (`__cmp__:`/`__top_n__:`) shared with Qt, dispatched via `core/app_service/action_resolve.py` (guards `if not pattern: return []` at line 341, mirroring `file_operations.py:1387-1408` per inline comment). QA: `qa/web/scenarios/s43_numeric_condition.py`, `s50_select_numeric_panel_from_main_window.py`, `s56_action_dialog_apply_by_score.py` (all status: done). | Confirms #392's dispatch-gap fix (Qt-side) has a symmetric single-code-path story server-side on web (`action_resolve.py`), so there is no equivalent per-route split to regress. |
| Set Action dialog — Score / Lock / Resolution fields (672) | parity | `frontend/src/components/action/ActionDialog.tsx:75,78,80` list `{key:"score"}`, `{key:"resolution"}`, `{key:"lock"}` among the 11-option field combo (features.md:699 "11 canonical English options"); Score is in `NUMERIC_FIELDS` (line 88). Reachable from both the toolbar route and the row-right-click route (features.md:696-697). QA: same s43/s50/s56 scenarios cover Score; no dedicated Lock/Resolution scenario found but field-combo presence is asserted structurally by `ActionDialog.test.tsx`. | Web never had Qt's #392 two-divergent-field-list bug since there is one canonical `ACTION_FIELDS` array, not two hand-written lists — structurally can't regress the way Qt's did. |
| Set Action dialog — geometry persistence (683) | qt-only-n/a | Grepped `frontend/src/components/action/ActionDialog.tsx` and all of `frontend/src/components/action/` for `geometry\|Geometry\|resizable\|localStorage` — zero matches. Grepped all of `frontend/src/` for `geometry\|Geometry` — only hit is `frontend/src/lib/panelWidths.ts` (an unrelated split-pane width helper, not a dialog geometry/splitter concern). No `#`-issue reference for an ActionDialog-geometry web port found anywhere in `frontend/src` or `app`/`core`. | The web ActionDialog is a Radix `<Dialog>` rendered in a fixed-layout browser viewport, not an OS-level resizable window — there is no `QSettings`-equivalent "restore rect on reopen" affordance to port, and no browser-native window-geometry API a web app can hook the way Qt hooks `QDialog.done()`. Column-width persistence (a *different* feature, `pm.result-tree.column-widths.v1` in `frontend/src/lib/columnWidths.ts:16`) is the nearest web analog but belongs to the result tree, not this dialog — treated as a distinct feature already covered by its own row below. Genuinely qt-only: no reasonable web equivalent exists for "remember this modal's pixel size/splitter position across restarts" in a stateless browser tab. |
| Web — Action dialog (Set Action by Field) (694) | parity (web-native, anchors hold) | All cited anchors verified live: `frontend/src/components/action/ActionDialog.tsx` exists; testids `action-field-combo`/`action-simple-row`/`action-regex-row`/`action-numeric-row`/`action-action-combo`/`action-match-counter`/`action-preview-list`/`action-btn-apply`/`action-recent-select`/`action-recent-clear`/`action-delete-confirm-summary` all defined in `frontend/src/testids.ts`. #741 confirmed live: `core/app_service/action_resolve.py:341` `if not pattern: return []`; `frontend/src/lib/recentPatterns.ts` + `frontend/src/lib/patternSummary.ts` exist; `app/web/routes/settings.py:35` comment "#741 sub-item B — ActionDialog 'Recent' patterns". #735 confirmed live: `frontend/src/lib/actionDialogFields.ts` (`WEB_COL_TO_FIELD`), `frontend/src/components/result/FileRow.tsx:35,69` (clicked-column resolution), `frontend/src/store/useAppStore.ts:1007` (`openActionDialog` initialField guard). #674 confirmed live: `action-lock-confirm-dialog`/`-btn-cancel`/`-btn-unlocked-only`/`-btn-unlock-apply` all present in `frontend/src/components/action/ActionDialog.tsx` (lines ~396-439); `app/web/routes/action.py` handles `skip_locked`/`matched_total`/409. QA: `qa/web/scenarios/s31_simple_mode_regex.py`, `s32_lock_confirm_bulk_regex.py`, `s15_context_menu.py` all status: done. | Self-consistent — every claim in this Web-native section checks out against the live tree. |
| Web — main result tree multi-selection (719) | parity (web-native, anchors hold) | `frontend/src/components/result/FileRow.tsx` and `frontend/src/components/ContextMenu.tsx` exist; `frontend/src/store/useAppStore.ts:161` (`selection: {...initialSelection}`), `:283-298` (selection reset on manifest mutation) confirm the selection slice. #694 confirmed live: `frontend/src/components/ContextMenu.tsx:121` "This closes the #694 parity gap (the web previously only STAGED…)"; `ContextMenu.test.tsx:142-146` asserts `removeFromList` finalize semantics. #735 confirmed live: `frontend/src/components/result/GroupRow.tsx:11,16` (group-row right-click / reduced menu), `frontend/src/components/ResultTree.tsx:24,29,61` (right-clicked column + group-header target types). QA: `qa/web/scenarios/s15_context_menu.py`, `s20_multi_remove_from_list.py` status: done. | Self-consistent. |
| Web — Execute (only selected) (735) | parity (web-native, anchors hold) | `frontend/src/components/execute/ExecuteDialog.tsx` exists and references `#430`/`#410`/`#733`. `frontend/src/App.tsx` contains the `#735` context-menu-entry wiring comments (lines 60,62,130,309). QA scenario cited as `s44_execute_selected_only.py` — confirmed present under scenario_map.yml key `s44_execute_highlighted_rows` (`playwright_module: qa.web.scenarios.s44_execute_selected_only`, present in `qa/web/scenarios/`), plus `s15_context_menu.py`. | scenario_map.yml's scenario KEY (`s44_execute_highlighted_rows`) doesn't match the module filename (`s44_execute_selected_only`) — a naming mismatch worth a drive-by note, but the module itself exists and the mapping resolves correctly, so this is not a parity gap. |
| Web — ScanDialog source/output persistence (750) | parity (web-native, anchors hold) | `frontend/src/components/ScanDialog.tsx` exists at the top-level `components/` path as cited (not under `components/scan/`); `frontend/src/components/scan/SourceRow.tsx` exists; `app/web/routes/settings.py` exists and contains the `#741` Recent-patterns settings key comment confirming the file is live and current. #736 confirmed live: `#736` appears in both `frontend/src/components/ScanDialog.tsx` and `ScanDialog.test.tsx`. QA: `qa/web/scenarios/s23a_set_settings.py`, `s23b_verify_settings.py`, `s17_scan_dialog_widgets.py` all present in `qa/web/scenarios/`. | Self-consistent. |
| Web — result-tree column sort and resize persistence (763) | parity (web-native, anchors hold) | `frontend/src/components/result/ColumnHeaderRow.tsx` exists; `frontend/src/lib/resultColumns.ts` exists; localStorage key `pm.result-tree.column-widths.v1` confirmed live in `frontend/src/lib/columnWidths.ts:16` and exercised by `frontend/src/components/ResultTree.columns.test.tsx:197,209`. QA: `qa/web/scenarios/s45_sort_persistence.py`, `s47_column_layout_persist.py` both status: done. | Self-consistent; this is the feature that legitimately covers "resize persistence" for the web app — the geometry-persistence row above is correctly scoped to the ActionDialog only, not this tree. |
| Web — manifest decision persistence (776) | parity (web-native, anchors hold) | `frontend/src/components/result/DecisionControl.tsx` exists; `PATCH /api/decision` confirmed live at `app/web/routes/review.py:91`; `ManifestRepository.batch_update_decisions` confirmed at `infrastructure/manifest_repository.py:493`; `store.setDecision` confirmed at `frontend/src/store/useAppStore.ts:310`. `POST /api/save` confirmed live at `app/web/routes/execute.py:310-311`. #733 lock-gate confirmed live: `#733` appears in `core/app_service/action_service.py`, `core/app_service/review_service.py`, `frontend/src/components/dialogs/LockConfirmDialog.tsx`. QA: `qa/web/scenarios/s12_save_manifest.py` status: done. | Self-consistent. |
| Web — scan dialog invalid source path (787) | deliberate-omit (pre-scan validation) / parity (post-scan failure surfacing) | `scanner/walker.py:106` confirms `raise FileNotFoundError(f"Source directory not found: {root}")` — matches the doc's claim exactly. The doc itself records the deferral: features.md:792 "The exact-parity pre-scan inline validation … is a deferred enhancement, tracked as #678 item C — declined here to avoid a filesystem-information-disclosure endpoint for a timing-only nicety." QA: `qa/web/scenarios/s38_scan_dialog_invalid_path.py`, scenario_map.yml status: done. | Split verdict: the *mechanism* (surfacing an invalid path) has full parity via the failed-scan path; the *timing* (pre-scan inline check, Qt's `_on_add_typed` gate) is a recorded, reasoned deliberate-omit — not a gap. |
| Web — scan progress throughput / ETA / localized stages (#740) (798) | parity (web-native, anchors hold) | `core/app_service/scan_runner.py:26-31` confirms `STAGE_WALK`/`STAGE_HASH`/`STAGE_EXIFTOOL`/`STAGE_CLASSIFY`/`STAGE_SCORE`/`STAGE_WRITE` constants exist exactly as named. `translations/en.yml:165-170` confirms `stage_walk`/`stage_hash`/`stage_exiftool`/`stage_classify`/`stage_score`/`stage_write` i18n keys exist (plus a second `#740`-tagged block at lines 608-612). `frontend/src/lib/scanProgress.ts` exists; `frontend/src/components/scan/ScanProgress.tsx` exists and references `#740`. QA/tests: `frontend/src/lib/scanProgress.test.ts`, `frontend/src/components/scan/ScanProgress.test.tsx` (vitest, per the doc's own "Related" line — no separate qa/web/scenarios driver claimed by the doc for this feature). | Self-consistent; this section cites vitest rather than a qa/web scenario, matching what's actually in the repo (no `s`-numbered scenario references this feature by name). |

## Summary

- **parity:** 11 sections (5 Set-Action-dialog Qt-side rows fully mirrored on web; 6 web-native sections whose file:line/testid/issue anchors all hold)
- **deliberate-omit:** 1 section, folded into a split verdict — "Web — scan dialog invalid source path" (pre-scan inline validation timing, tracked #678 item C)
- **qt-only-n/a:** 1 section — "Set Action dialog — geometry persistence" (no browser-native equivalent to an OS window's saved rect/splitter state for a stateless dialog)
- **GAP:** 0 sections


---

<!-- parity-T1E -->

# Web parity matrix — Tranche 1E (docs/features.md lines 812-956)

| feature (features.md line) | verdict | evidence | notes |
|---|---|---|---|
| Web — scan SSE connection-drop watchdog (#661) [812-818] | parity | `frontend/src/hooks/useScanSSE.ts` (watchdog hook); `app/web/routes/scan.py` (`_KEEPALIVE_TIMEOUT_S`, named `ping` keepalive — grep confirmed present); tests `tests/test_web_scan_routes.py::test_sse_idle_stream_emits_named_ping_keepalive`, `frontend/src/hooks/useScanSSE.test.ts` both exist on disk. | No live `qa/web` scenario exists (checked `qa/web/scenario_map.yml` — no s-number references SSE-drop); features.md itself documents this as deliberate ("a true server-drop cannot be automated... without breaking co-tenant scenarios") and substitutes deterministic unit tests both ends. Anchors still hold. |
| Web — desktop shell (pywebview launcher) [821-828] | parity | `launcher.py` — `PHOTO_MANAGER_WEB` truthy dispatch (grep hit lines 5/8/52/54), `PHOTO_MANAGER_WEB_PORT` override (line 61/64), WebView2 runtime check (lines 33-120), native window boot (lines 169/186); `tests/test_launcher.py` exists; `app/web/main.py` supplies `/api/health` + uvicorn factory. | No qa/web scenario needed — this is a process-launch/native-window concern, not a browser-drivable behaviour; features.md states the native window is validated by manual Windows smoke, consistent with what's on disk. |
| Similarity column [832-839] | parity | Web equivalent lives in `frontend/src/components/result/FileRow.tsx`: `Ref` badge (lines 107-111), similarity badge incl. passenger amber highlight (`isPassenger`, lines 51-52, 120-124), `similarityLabel(row.similarity)` helper (line 6/50). QA scenario `qa/web/scenarios/s52_similarity_against_displayed_ref.py` — status `done` per `qa/web/scenario_map.yml` line 297-300 (asserts server-computed `similarity.percent` matches the render-time recompute against the displayed Ref). | The Qt entry point (`app/views/tree_model_builder.py`) is the desktop implementation; the web has its own independent but behaviourally-equivalent implementation, not shared code — correctly a ported feature, not shared infra. |
| Preview pane — byte-budget LRU cache [843-850] | parity | `infrastructure/image_service.py` (`_ByteBudgetLRUCache`, budget sizing `_compute_cache_budgets`) is SHARED code: `app/web/main.py:57-58` constructs `ImageService()` and stores it on `app.state.image_service`, consumed by `app/web/routes/image.py:85-91`. `docs/design/web-port-tech-design.md:1305-1318` documents the web-specific rewrite (QImage→bytes cache value, `image_service.py:146-193`/`172` cited, byte-budget formula "unchanged"). | Not a re-port — it is literally the same class, adapted to return JPEG bytes instead of `QImage` for the browser. The design doc explicitly confirms the `memory_probe` Qt hook (line 1318) was stripped out of this shared module — see the memory-probe row below. |
| Preview pane — full-resolution viewer [854-861] | parity | `frontend/src/components/FullResViewer.tsx` — pan via CSS-transform drag (lines 93-113, "Drag-to-pan"), Ctrl+wheel zoom clamp (line 126 area, `scale` state line 96), Esc/close dismiss (line 322 "Esc to close"). QA scenario `qa/web/scenarios/s68_full_res_viewer_double_click.py` — status `done` (`qa/web/scenario_map.yml` lines 426-429): double-click opens `fullres-dialog`/`fullres-image`, Escape closes, main window stays responsive. | Zoom clamp range not independently re-verified against Qt's 5%-800% (not required by METHOD); scale state + Ctrl+wheel handler confirmed present. |
| Preview pane — no-autoplay video default [865-872] | parity | `frontend/src/components/PreviewPane.tsx:125-136` — the single-view `<video controls ...>` element carries no `autoPlay` attribute; inline comment at line 135: "No autoplay — matches Qt desktop single-view player behaviour." `frontend/src/components/VideoTile.tsx` (grid tiles) plays only on click (poster-until-click pattern, lines 3-8/121-128), matching Qt's "thumbnail, play only when the user clicks the tile." | No qa scenario explicitly asserts *absence* of autoplay (s69/s71 assert readyState/duration only); parity is code-verified (attribute absence), not behaviourally-tested — a minor coverage gap, not a feature gap. |
| Web preview pane — video transcode fallback (V2 H.264) [876-884] | parity | `infrastructure/transcode_service.py` (`TranscodeService`), `app/web/routes/media.py` (`transcode=h264` param), `frontend/src/api/client.ts` (`mediaUrl` transcode opt). QA scenario `qa/web/scenarios/s70_video_transcode_fallback.py` — status `done` (scenario_map.yml lines 436-439); integration test `tests/integration/test_transcode_integration.py` per features.md. | Cross-checked against KNOWN CONTEXT: #737 HEVC first-byte stall is real and only partially mitigated — features.md itself is honest about this ("ultrafast preset... **minimises** the first-view stall", "Progressive streaming + a real progress % is a **tracked follow-up**"), so there is no overclaim to flag; the doc already frames #737 as a mitigation, not a fix. |
| Web preview pane — video playback (V1 streaming) [887-894] | parity | `app/web/routes/media.py` (`GET /api/media`, Range/206 support), `frontend/src/components/PreviewPane.tsx` + `FullResViewer.tsx` `<video controls>` elements, `scanner/media.py` (`VIDEO_EXTENSIONS`). QA scenario `qa/web/scenarios/s69_video_playback.py` — status `done` (scenario_map.yml lines 431-434): asserts `media_type=='video'`, real `<video>` render, `readyState>=2`, `duration>0`, playback advances. | Anchors confirmed current. |
| Web preview pane — group grid view (multi-tile) [898-906] | parity | `frontend/src/components/GroupGrid.tsx`, `VideoTile.tsx`, `ImageTile.tsx`. Verified rather than assumed per KNOWN CONTEXT: `VideoTile.tsx` header comment (lines 3-8) — "Renders `row.thumbnail_url` as a poster image... onError falls back to the dark background" — confirms #734's real-poster fix is actually in the code, not just claimed. QA scenario `qa/web/scenarios/s71_grid_video_tiles.py` — status `done` (scenario_map.yml lines 441-444), asserts poster + click-to-mount + broadcast. | #734 poster-frame claim independently verified in source, not taken on faith. |
| Web preview pane — group sync controller (GroupMediaController) [909-927] | parity | `frontend/src/components/GroupMediaController.tsx` (component + props confirmed, lines 1-87), `frontend/src/lib/groupMediaSync.ts` (6 pure helper fns per features.md). QA scenario `s71_grid_video_tiles.py` (same as grid row — group pause/play broadcast to both tiles + master-scrub convergence via keyup-release, per scenario_map.yml notes) + component test `GroupMediaController.test.tsx` (file referenced in features.md, not independently opened). | Shares its qa scenario with the grid-view row (s71) — both features are exercised in the same driver, which is consistent with them being one physical UI (grid + control bar below it). |
| Developer tool — memory probe [931-938] | qt-only-n/a | `scripts/memory_probe.py`, `tests/test_memory_probe.py`, `docs/audits/memory-probe.md` exist but ZERO hits for `memory_probe`/`MEMORY_PROBE` under `frontend/src` or `app/web` (grep run repo-wide; only Qt-side files matched: `app/views/workers/manifest_load_worker.py`, `app/views/tree_model_builder.py`, `app/views/main_window.py`, `app/views/handlers/file_operations.py`). `docs/design/web-port-tech-design.md:1318` explicitly states the probe hook in `image_service.py:376-382` (`track_qt_alloc("QImage", ...)`) was **removed** during the web port with the reason given inline: "probe is Qt-specific." | features.md frames the probe around `QStandardItem`/`QImage` object counts and a Qt `destroyed` signal — inherently PySide6/Qt-heap instrumentation with no browser/Node equivalent concept. The design doc's own removal note is a first-party admission this is desktop-only tooling, not an unbuilt web feature — correctly qt-only-n/a rather than GAP. |

## Grep patterns used (for GAP-candidate sections — none reached GAP verdict, but patterns tried per section)

- SSE watchdog: `useScanSSE|_KEEPALIVE_TIMEOUT_S`; issue token `#661` (present in features.md + GitHub link, not separately re-grepped as a code comment since the feature is web-native and already located by symbol).
- Desktop shell: `PHOTO_MANAGER_WEB|WebView2`; issue token `#644` (present in features.md citation).
- Similarity column: `similarity|Similarity|N\*%|passenger` across `frontend/src` (17 file hits); issue tokens `#253|#241|#536` not re-grepped separately — symbol grep was sufficient to locate the port.
- Byte-budget LRU cache: `image_service|ImageService|_ByteBudgetLRUCache` across `app/web`; issue token `#622` covered by the design-doc citation already found.
- Full-res viewer: `FullResViewer|requestFullRes|pan|zoom|scale` in `FullResViewer.tsx` directly (symbol known from the video-playback section's own file list).
- Memory probe (only section that could plausibly be GAP): `memory_probe|MEMORY_PROBE` repo-wide (12 hits, all Qt-side + docs/scripts, zero in `frontend/src` or `app/web`); issue tokens `#614|#619|#624` — not separately grepped as code comments because the design-doc removal note (`web-port-tech-design.md:1318`) already gives a first-party GAP-vs-omit determination (explicit removal, explicit reason), which outweighs an absence-only grep result.


---

<!-- census-T2-1 -->

# Issue triage — batch T2-1 (#764, #757, #744, #743, #742, #741, #740, #739, #738, #737, #736)

Repo: `photo-manager` worktree `web-port-phase2`, HEAD `717fa53` on `docs/web-port-doc-hygiene` (confirmed ancestor-superset of `docs/web-port-feasibility` / FB, `git merge-base --is-ancestor origin/docs/web-port-feasibility HEAD` = true). All commits cited below are ancestors of HEAD (verified via `git merge-base --is-ancestor <sha> HEAD`).

| issue | title (short) | verdict | evidence | close-ready? |
|---|---|---|---|---|
| #764 | ActionDialog lock-confirm cascade-close twin of #721 | implemented-admin-open | `frontend/src/components/action/ActionDialog.tsx:451-467` — `onInteractOutside` now unconditionally calls `e.preventDefault()` (comment cites #764/#721 by name); `onEscapeKeyDown:466-467` still gated on `actionRunning` only, as prescribed. Fixing commit `c261d6d fix(web): keep Action dialog open when cancelling its lock-confirm (#764)`. | yes |
| #757 | Repoint Qt ExecuteActionDialog winerror table to infrastructure/winerror_reasons.py | real-gap | Searched: `git log --oneline --all --grep "#757" -i` → 0 hits. `grep -rln "#757"` across `*.ts,*.tsx,*.py,*.md` repo-wide → 0 hits. `grep -n "WINERROR_REASON_TABLE\|decode_winerror\|winerror_reasons" app/views/dialogs/execute_action_dialog.py` → still defines its own `_WINERROR_REASON_TABLE` (line 92) and `_decode_winerror` (line 104), no import of `infrastructure.winerror_reasons`. `infrastructure/winerror_reasons.py` exists and is imported only by `infrastructure/delete_service.py:19` (the web path) — the Qt-side repoint from #757 was never done. | no |
| #744 | Reconcile web UI with DesignSync Daylight/Aperture prototype — divergence list + sign-off | partial | Done: the one concretely-named owner finding ("按鈕設計跟原本 Design 不同" / button divergence) fixed by commit `2394320 feat(web): one-click button row for per-row decisions (#744)` (`DecisionControl` button row replacing the Select). Not done: the issue's actual deliverable — a full reconciliation pass (result tree, scan/execute/action dialogs, buttons, status bar, light+dark) producing a **divergence list with owner keep/fix decisions** — doesn't exist. Searched `grep -rln "Daylight\|Aperture" docs/` → only a pre-existing feasibility audit doc, no reconciliation/divergence-list artifact; `grep -rln "#744"` in source finds no other tagged work. | no |
| #743 | Low-priority parity polish tracker (secondary sort, double-click, empty-area menu, read-knee persistence, zoom clamp) | partial | Done (2/5): read-knee checkbox persistence — `ScanDialog.tsx:250,416,440` reads/writes `ui.scan_dialog.autotune_read_knee` (commit `c019b2a`); FullResViewer zoom clamp 5%-800% — `FullResViewer.tsx` (commit `e394da5`). Not done (3/5), confirmed by repo-wide grep: (1) secondary sort by score — `frontend/src/lib/resultColumns.ts:84-113` `makeRowComparator` only has `name`/`size` comparators, no score tiebreak; (2) group-header double-click expand/collapse — `frontend/src/components/result/GroupRow.tsx` only wires `onClick={onToggle}` (line 37), no `onDoubleClick`; (3) empty-area (tree background) right-click menu — `frontend/src/components/ResultTree.tsx:287-290` outer scroll container has no `onContextMenu`; only `FileRow.tsx:90` (row) and `GroupRow.tsx:38` (group header) have handlers. Searched `grep -rln "#743" --include=*.ts,*.tsx,*.py,*.md` → only touches `FullResViewer.tsx` and `ScanDialog.tsx`/`ScanDialog.test.tsx`, confirming no code tags the other 3 sub-items. | no |
| #742 | Per-file failure decoding — winerror reasons + Files Not Found vs Failed to Delete split | implemented-admin-open | `frontend/src/components/execute/ExecuteResultSummary.tsx:41` (`files_not_found_title` → "Files Not Found") and `:60` ("Files Failed to Delete") render the split with decoded reasons; tests `ExecuteDialog.test.tsx:519` ("renders the Files Not Found section...") and `:543` ("...Failed to Delete section with the decoded reason"). Fixing commit `2045dab feat(web): decode per-file delete failures + Files-Not-Found/Failed split (#742)`, extended by `9708092 test(web): extend s13 to assert the #742 Files-Not-Found section`. | yes |
| #741 | ActionDialog cluster — empty-pattern Apply gate, Recent patterns, pattern-aware delete-confirm | implemented-admin-open | (a) empty-pattern gate removed: main Apply button (`ActionDialog.tsx:645`, `onClick={() => void handleApply()}`) carries no pattern-emptiness `disabled` check (only lock-confirm sub-buttons at 429/638/646 are gated on `actionRunning`/lock state). (b) Recent patterns: `lib/recentPatterns.ts` + `ActionDialog.tsx:31-36,144-224,291-307` (read on open, recorded after successful apply, persisted via `patchSettings`). (c) pattern-aware delete-confirm summary: `ActionDialog.tsx:367` ("Localised field label + pattern-aware summary for the delete-confirm"). Fixing commit `2117d79 feat(web): ActionDialog empty-pattern Apply fix + recent patterns + pattern-aware delete-confirm (#741)`, extended by `7ba73f8 test(web): extend s31 for the #741 ActionDialog cluster`. | yes |
| #740 | Render files/sec + ETA in scan progress, localise stage labels | implemented-admin-open | `frontend/src/components/scan/ScanProgress.tsx:22-38` (`filesPerSec`, `eta` props), `:56-78` renders `formatThroughput(filesPerSec)` and `eta` text with inline comments explicitly tagged `(#740)`; `stageLabel(stageName, t)` (line 67) localises the stage name. Fixing commit `627f1aa feat(web): scan-progress files/sec + ETA + localized stage labels (#740)`. | yes |
| #739 | Preview panel fixed-width → resizable splitter + persistence; FullResViewer overlay sign-off | partial | Panel-resize half DONE: `frontend/src/lib/panelWidths.ts` (persistence, header comment tagged `#739`), `App.tsx:165-180,404` wires a drag handle (`handlePreviewResizeStart` on `onMouseDown`) reading/writing `resultView.panelWidths.preview`; s39 re-flipped in `d186660 test(web): re-flip s39 to assert preview-panel width persistence (#739)`; fixing commit `992ef01 feat(web): resizable preview panel with width persistence (#739)`. FullResViewer-overlay half is explicitly **still awaiting the owner's sign-off**, not a code gap: `docs/features.md:392` states verbatim "a movable/resizable overlay window ... awaits the issue owner's explicit sign-off ... before any work starts" — this is the issue's own second ask and remains open pending a human decision, not code. | no |
| #738 | Live Photo pairs (JPG+MOV / HEIC+MOV) scored as duplicates instead of Ref+passenger | implemented-admin-open | `scanner/scoring.py:156-164` adds `_LIVE_PHOTO_IMAGE_SUFFIXES = {heic, heif, jpg, jpeg}` used by the MOV-passenger rule (`:404,434`); commit message of `5f28bb3` confirms HEIC+MOV was **already** recognized pre-fix (peer set "only had heic/heif" — i.e. heic/heif were already wired, only jpg/jpeg was the gap), so the issue's "second undiagnosed failure mode" resolved itself as a non-issue once JPG/JPEG was added — both pair types now produce `score=NULL` passengers. New regression tests: `test_mov_with_jpg_peer_returns_none`, `test_mov_with_jpeg_peer_returns_none`, `test_jpg_orphan_not_penalised_as_live_photo`. `docs/features.md` updated (`30f5b98`) with a "Last verified: ... 2026-07-03 (#738 ...)" note. | yes |
| #737 | HEVC transcode fallback blocks first byte on full synchronous encode | partial | Quick levers DONE (the issue's own stated *minimum* bar — "at minimum the stall must become bounded and visible"): `infrastructure/transcode_service.py:171-178` `-preset ultrafast` (was `fast`), commit `a59022c perf(web): ffmpeg ultrafast preset for HEVC transcode fallback (#737)`; "Preparing video…" bounded/visible indicator at `FullResViewer.tsx:254` and `PreviewPane.tsx:122` (same commit). "Proper fixes" NOT done: searched `grep -rn "progressive\|fragmented\|transcode.*cache\|HEVC.*extension" infrastructure/transcode_service.py` and repo-wide for a persistent transcode cache or native-HEVC-detection path — none found; the response is still a full synchronous ffmpeg encode before any bytes stream (no `-progress` pipe, no fMP4 streaming, no cache keyed on (path, mtime)). The issue itself frames these as "separate PR" follow-ups, so the minimum/blocking bar is met but the issue's full scope is not. | no |
| #736 | Advanced Settings missing pHash/dHash/mean-color threshold controls + source-list sort | implemented-admin-open | `frontend/src/components/ScanDialog.tsx:679-763` adds pHash/dHash/mean-color threshold rows (short-desc + full hover tooltip, matching Qt #520/#533 pattern) wired to `WebScanRequest` (`:422-423` `dhash_threshold`/`mean_color_threshold`); source list case-insensitive sort at `:556-557` (`localeCompare(..., {sensitivity: 'base'})`, explicit comment "DISPLAY-ONLY alphabetical sort (case-insensitive...)"). Fixing commit `b245d41 feat(web): add pHash/dHash/mean-color threshold controls + alphabetized source list (#736)`, tests extended in `7446f11 test(web): extend s17 for #736 thresholds + source sort; harden s23b`. | yes |

## Verdict counts
- implemented-admin-open: 6 (#764, #742, #741, #740, #738, #736)
- real-gap: 1 (#757)
- partial: 4 (#744, #743, #739, #737)
- tracker/epic-or-out-of-scope: 0


---

<!-- census-T2-2 -->

# Census T2-2 — issues #735,734,733,721,718,713,712,709,708,703,702,699

| issue | title (short) | verdict | evidence | close-ready? |
|---|---|---|---|---|
| #735 | context menu missing By-Field/Execute-selected/group menu | implemented-admin-open | `frontend/src/components/ContextMenu.tsx:148-289` (By-Field + Execute-selected entries, `// #735` comments), `GroupRow.tsx:17-38` (`onContextMenu` wired). Fixing commit `1c69a8e feat(web): add By-Field, Execute-selected, group-row context menu (#735)` + `a5d5b37 docs(features): record #735`. Covered by `ContextMenu.test.tsx:241-307`. | yes |
| #734 | video tiles show placeholder, not real thumbnail | implemented-admin-open | `frontend/src/components/VideoTile.tsx:3-8,52-123` — real `<img>` poster w/ `onError` fallback, header comment corrected. Fixing commit `5095209 fix(web-grid): show real Shell/WIC poster frames on video tiles`. | yes |
| #733 | two destructive-op guardrails weaker on web (delete confirm scope + lock-confirm on decision writes) | implemented-admin-open | (a) `ExecuteDialog.tsx:65,221-222` `findAllDeleteGroupIds` narrows per-group (not whole-scope); test comment at `ExecuteDialog.test.tsx:675` documents the old bug and its fix. (b) `useAppStore.ts:310-313` `setDecision` routes through `setDecisions` for 409 `locked_paths` handling; `lockConflict` state (`useAppStore.ts:403,722,775,834`) drives `LockConfirmDialog` op=`decision`. Fixing commits `a4b7271`, `3e5950d`, `f5cb182`, `ce60986`, `207cd92`, `3637f31` (all tagged #733). | yes |
| #721 | Cancel on execute-time lock-confirm also closes Execute dialog | implemented-admin-open | `ExecuteDialog.tsx:413-429` `onInteractOutside` now blocks ALL outside interaction (not gated on `executeRunning`), comment explicitly cites #721 and explains the Radix nested-modal race. Fixing commit `8a7f57f fix(web): keep Execute dialog open when cancelling the lock-confirm (#721)` (an earlier WIP attempt `7aa9f66` failed live-run before this landed — two-iteration fix, now confirmed present). | yes |
| #718 | manifest.error not cleared between actions (stale error persists) | implemented-admin-open | Clear-on-entry added to `loadManifest` (`useAppStore.ts:272-273`, pre-existing), `setLock` (`:320-325`), `resolvePruneLock` (`:896-902`), `saveManifest` (`:966-971`) — all tagged `// #718`. `applyPrune` (`:857-893`) deliberately has NO clear-on-entry, preserving the #702 resolvePruneLock→applyPrune error-survival chain per the issue's explicit constraint. Fixing commit `7c3f07f fix(web): clear the manifest-error footer on the next action (#718)`. | yes |
| #713 | s61 missing lock_apply_remove variant (Remove after Unlock&Apply) | real-gap | `qa/web/scenarios/s61_actioned_singleton_prune.py:205-325`: `_run_variant`'s lock branch (`:251-263`) hardcodes `PRUNE_BTN_KEEP` for every lock-variant call; the only invocations of `PRUNE_BTN_REMOVE` (`:269,276`) are in the non-lock `else` branch. Caller list at `:320-325` has exactly 6 variants (`remove_plain`,`remove_both`,`keep`,`lock_cancel`,`lock_unlocked_only`,`lock_apply`) — no `lock_apply_remove`. Grep patterns tried: `lock_apply_remove` (0 hits repo-wide), `PRUNE_BTN_REMOVE` (only non-lock branch). No fixing commit found (`git log --grep "#713"` — 0 hits). Same underlying gap as #702 item 4 ("No live Remove-after-Unlock&Apply variant"), explicitly deferred from #702 to this issue. | no |
| #712 | manifest.error written but rendered nowhere | implemented-admin-open | `App.tsx:425-436` renders `manifest.error` as a footer alert line, comment `// Additive error line (#712)`. Fixing commit `fb64941 feat(web): render manifest.error as a footer alert line (#712)`. | yes |
| #709 | no roving arrow-key focus in ResultTree | real-gap | `frontend/src/components/ResultTree.tsx` — grep for `ArrowDown|ArrowUp|activedescendant` = 0 hits. Grep patterns tried: `ArrowDown\|ArrowUp\|scrollMargin\|activedescendant` in `ResultTree.tsx` (0 hits), `git log --all --grep "#709"` (0 hits). No `useDecisionShortcuts`-style roving-focus hook exists anywhere under `frontend/src/hooks/`. | no |
| #708 | d/k decision keyboard shortcuts missing | implemented-admin-open | `frontend/src/hooks/useDecisionShortcuts.ts` mounted at `App.tsx:15,232`. Fixing commit `5fe1ab0 feat(web): d/k decision keyboard shortcuts over the main-tree selection (#708)`. | yes |
| #703 | tab-close during scan doesn't cancel the worker (409 lock-out) | implemented-admin-open | `frontend/src/hooks/useBeforeUnloadGuard.ts:12-13,20,65-73` — `pagehide` listener fires `navigator.sendBeacon('/api/scan/{taskId}/cancel')`, explicitly NOT `beforeunload` per the issue's own caveat. Fixing commit `f0046dc feat(web): cancel the orphaned scan worker on Leave-mid-scan (#703)`. | yes |
| #702 | 5 non-blocking hardening items from #701 adversarial review | partial (tracker) | Item 1 (s67 forward-defensive doc) — doc-only, not verified this pass (low priority, no code change expected). Item 2 (Cancel-discriminator) — fixed: `s61_actioned_singleton_prune.py` docstring `:40-44` + `_prune_candidates_locked` helper (`:107-124`) reads `/api/prune/candidates` post-Cancel/Unlocked-only. Item 3 (failed-unlock-PATCH silent hold) — fixed: `useAppStore.ts:927-939` surfaces `"Couldn't unlock the locked items — they were held back from pruning."`. Item 5 (`LOCK_CONFIRM_BTN_UNLOCKED_ONLY` not driven live) — fixed: `s61` caller list `:324` drives `lock_unlocked_only` with the candidates discriminator. Item 4 (live Remove-after-Unlock&Apply variant) — explicitly deferred to #713, which is confirmed still open/real-gap above. Fixing commit `2b70f60 fix(web): hold still-locked items out of the prune set when their unlock fails (#702)` covers items 2/3/5; item 4 has its own tracking issue. | no — stays open until #713 lands (or re-scope to close #702 and let #713 carry the residual alone) |
| #699 | ResultTree virtualizer missing scrollMargin (sticky-header coordinate offset) | real-gap | Grep patterns tried: `scrollMargin\|paddingStart` across `frontend/src` (0 hits); `useVirtualizer` call site `ResultTree.tsx:155-166` has no `scrollMargin` option. `git log --all --grep "#699"` — 0 hits. No larger-than-5-row scroll qa fixture found either (issue's second ask). | no |

## Verdict counts
- implemented-admin-open: 8 (#735, #734, #733, #721, #718, #712, #708, #703)
- real-gap: 3 (#713, #709, #699)
- partial (tracker): 1 (#702)

## Real-gap / partial issue numbers
#713, #709, #699 (real-gap); #702 (partial — 4/5 sub-items done, item 4 lives on in #713)


---

<!-- census-T2-3 -->

# Census T2.3 — Issues #697, #694, #692, #689, #688, #687, #686, #685, #680, #678, #676, #674

| issue | title (short) | verdict | evidence | close-ready? |
|---|---|---|---|---|
| #697 | ExecuteDialog in-dialog Ctrl/Shift multi-select (completes s64) | implemented-admin-open | `frontend/src/components/execute/ExecuteTree.tsx:284-285` (`ctrl: e.ctrlKey \|\| e.metaKey`, `shift: e.shiftKey`); fixing commit `5ad2ff5 feat(web): in-dialog Ctrl/Shift multi-select in the execute dialog (#697)` | yes — admin close |
| #694 | result-tree "Remove from list" stages vs desktop finalizes | implemented-admin-open | `frontend/src/components/ContextMenu.tsx:117-129` `handleSetRemove()` now calls `removeFromList(targetPaths)` (finalize path, was `setDecision(...,'ignore')`); fixing commit `b1c44bf fix(web): result-tree Remove-from-list finalizes outcome=ignored (#694)` | yes — admin close |
| #692 | highlight auto-select KEEP keeper row post-scan (Qt #239 parity) | implemented-admin-open | `frontend/src/components/ResultTree.tsx:168-178` — `scrollToPath` one-shot effect scrolls to first auto-selected KEEP row after `loadManifest({selectKeepers})`; fixing commit `71fe34b feat(web): highlight + scroll to the auto-select keeper after a scan (#692)` | yes — admin close |
| #689 | ActionDialog Simple section disabled + note at 0 groups (blocks s55) | implemented-admin-open | `frontend/src/components/action/RegexPanel.tsx:69,79,157-181` — `hasGroups` prop disables `ACTION_SIMPLE_OP`/`ACTION_SIMPLE_TEXT` and renders `ACTION_SIMPLE_DISABLED_NOTE`; s55 covered (`ActionDialog.test.tsx:85,109` exercises `totalGroups: 20` vs `0`); fixing commit `1043bb6 feat(web): ActionDialog Simple placeholder at 0 groups (#689)` | yes — admin close |
| #688 | rebase desktop-only scenarios s28/s39/s48/s59 to skip, 67→63 | implemented-admin-open | `qa/web/scenario_map.yml:159,214-215,277-280,332-335` — all four scenarios carry `notes: "SKIP — desktop-only (#688)"` / equivalent rationale; fixing commit `57adcd6 chore(qa-web): rebase desktop-only scenarios s28/s39/s48 to skip (#688)` (s59 skip-rebase folded into the same effort, confirmed live in scenario_map.yml) | yes — admin close |
| #687 | beforeunload guard for in-progress scan (blocks s63 part 2) | implemented-admin-open | `frontend/src/hooks/useBeforeUnloadGuard.ts:7-72` — registers `window.addEventListener('beforeunload', warn)` while scan running, uses `pagehide` for the cancel path; test at `useBeforeUnloadGuard.test.ts:22`; fixing commit `c6e0cea feat(web): beforeunload scan-guard + port s63 close-guard (#687)` | yes — admin close |
| #686 | mixed-bucket prune + remove-triggers-prune + 3-value prune_singletons (s61, s67) | implemented-admin-open | `frontend/src/components/dialogs/PruneConfirmDialog.tsx:58,79,147` — `includeActioned` checkbox state gates `pruneSingletons(includeActioned)` in mixed bucket; `frontend/src/store/useAppStore.ts:853,958` — `removeFromList` now also sets `state.execute.prunePrompt` (not just `executeDecisions`); fixing commit `6048d69 feat(web): port singleton-prune flow — 3-value pref, mixed-bucket dialog, D6 lock gate (#686)` | yes — admin close |
| #685 | sortable + resizable, persisted column model (blocks s45, s47) | implemented-admin-open | `frontend/src/components/result/ColumnHeaderRow.tsx:23-56` (`onResize` live-update + mouseup-commit-to-localStorage pattern), `frontend/src/lib/resultColumns.ts`, `frontend/src/lib/columnWidths.ts` all present; original commit `bdf13dd feat(web): result-tree column model — sort + resize/persist (port s45/s47)`; later reused verbatim as "the #685 column-resize recipe" for the preview panel (`992ef01`, #739) | yes — admin close |
| #680 | expose per-dimension scoring signals in web FileRow (gps_present/exif_tag_count/xmp_derived) | real-gap | `core/app_service/review_view.py` `_build_file_row`: zero hits for `gps_present\|exif_tag_count\|xmp_derived` (grepped whole file); `frontend/src/api/types.ts`: zero hits for same tokens. Repo-wide grep (excl. node_modules) shows these three fields exist through the manifest/scoring layer (`infrastructure/manifest_repository.py:97-99`, `scanner/scoring.py`, `scanner/media_extract.py`) but never cross into `_build_file_row` or the web `FileRow` type. `qa/web/scenarios/s42_scoring.py:6,43-44` docstring explicitly states the signals are "NOT" serialized and "deferred to #680" — self-documents the gap. Grep patterns tried: `grep -rn "gps_present\|exif_tag_count\|xmp_derived" --include=*.py --include=*.ts --include=*.tsx .` (excl. node_modules); `grep -n ... core/app_service/review_view.py frontend/src/api/types.ts` (0 hits both); `git log --grep "#680"` (only turns up the s42 port commit that *defers* to #680, no implementing commit) | no — real gap, still open correctly |
| #678 | Phase-3.5 feature-parity backlog — 6 missing surfaces (tracking issue) | tracker (partially resolved, stays open) | Checklist A–F re-verified against code: **A** ScanDialog Advanced Settings — done, commit `84a3ce7` (s49/s57/s66 ported). **B** Execute-tree row context menu — done, commit `019fb71` (s30/s53/s54 ported). **C** Inline path validation (blocks s38) — NOT done, explicitly declined: commit `eac87ca` states "exact-parity pre-scan GET /api/fs/stat validation deferred as #678 item C, declined to avoid a filesystem-info-disclosure endpoint for a timing-only nicety" (s38 ported via a divergent scan-failure-surface strategy instead). **D** Multi-row selection + List menu — partial: multi-select done (`28c9e22` main-tree, `5ad2ff5`/#697 execute-dialog), but the List-menu entry itself was never built — `28c9e22` commit body: "marks s21 skip (web has no menu-bar List menu — covered by this context-menu port)". **E** Scan-session/settings persistence — done, commit `219b1ab` (s23a/s23b ported). **F** Save-Manifest entry point — resolved by design-decision-not-literal-build: commit `5e46906` (#673) establishes the web has no dirty state / Save step needed by construction (`PATCH /api/decision` commits synchronously), ports s12 as a stricter durability test instead of building a Save button. Net: 4/6 clusters closed, C is a genuine deferred/declined gap, D's List-menu half is a deliberate skip-instead-of-build. Issue legitimately stays open as the tracker for C + D's residual. | no — tracker, 2 real sub-gaps remain (C, D-List-menu) |
| #676 | web Execute ignores active type filter + no "hidden destructive" warning | implemented-admin-open | `frontend/src/components/execute/ExecuteDialog.tsx:92-113,337,344-351` — `scopePaths`/`filter` gating logic (`gateScope = filter === "all" ? null : scopePaths`) plus the group-qualification rules documented at lines 92-99; fixing commit `1c632a6 fix(web): scope Execute to the active type filter + hidden-destructive banner` (paired with test-port commit `f94405d` for s13/s33/s36/s64) | yes — admin close |
| #674 | ActionDialog inline lock-conflict dialog lacks testids + "Unlocked Only" verdict (blocks s32) | implemented-admin-open | `frontend/src/components/action/ActionDialog.tsx:48,270-271,397,427,434` — `ACTION_LOCK_CONFIRM_BTN_UNLOCKED_ONLY` testid wired to the "Apply to Unlocked Only" button, comment at 270-271 explicitly cites "#674, mirrors Qt APPLY_UNLOCKED_ONLY"; fixing commit `1ecd137 feat(web): port "Apply to Unlocked Only" verdict to ActionDialog lock-confirm (#674)` | yes — admin close |

## Verdict counts
- implemented-admin-open: 10 (#697, #694, #692, #689, #688, #687, #686, #685, #676, #674)
- real-gap: 1 (#680)
- tracker (partial): 1 (#678)
- partial (standalone): 0

## Notes
- All "implemented-admin-open" issues have a direct fixing commit whose message or `Refs #NNN`/`(#NNN)` tag names the issue, confirmed via `git log --oneline --all --grep "#NNN"` plus a live grep of the cited file:line in the current worktree.
- #680 is the only clean real-gap: grepped both the specific files the issue names (`review_view.py`, `types.ts`) and repo-wide for the three field names: zero crossing into the web API boundary, and the web s42 scenario's own docstring self-documents the deferral to #680.
- #678 required re-deriving each checklist letter (A–F) independently since the issue body's raw markdown checkboxes are all unchecked (`- [ ]`) regardless of actual completion state — GitHub UI checkbox state isn't reflected in the API `body` field the same way a completed sub-issue is. Verified via commit search + code grep instead of trusting the checkbox rendering.


---

<!-- census-T2-4 -->

# Issue census T2-4 — 16 open issues vs actual code on docs/web-port-feasibility

Checkout: `.claude/worktrees/web-port-phase2` (≈ FB head). Date: 2026-07-11.
Reminder: fixes merge to `docs/web-port-feasibility`, so GitHub never auto-closes — "open" ≠ "gap".

| issue | title (short) | verdict | evidence | close-ready? |
|---|---|---|---|---|
| #673 | port remaining Qt menu-bar items (Save/Exec-Selected/List/Log/Exit) | **partial** | Done half: toolbar Execute gating fixed — `frontend/src/App.tsx:295-301` (`disabled={manifestPath === null}` with `// #673` comment). Not done: the 5 menu items are still absent — `frontend/src/components/MenuBar.tsx:10-15` header comment explicitly lists Save Manifest / Remove from List / Log menu / Exit as unported; grep `Save Manifest\|Execute Selected\|Remove from List\|saveManifest` in MenuBar.tsx hits only that comment block. | no |
| #669 | migrate remaining web strings to `web.*` i18n namespace | **real-gap** | Bulk unmigrated. Greps tried: `t\(['"]web\.` per named file — `ContextMenu.tsx`: 0, `execute/ExecuteDialog.tsx`: 0, `result/FileRow.tsx`: 0, `PreviewPane.tsx`: 0, `lib/format.ts`: 0 hits; only `ScanDialog.tsx` (3) and `dialogs/DeleteConfirmDialog.tsx` (1) have any `web.*` calls. Foundation (#668) exists (`app/web/routes/i18n.py`), the migration of listed components does not. | no |
| #662 | CSRF Origin/Host guard + manifest-root trust boundary | **real-gap** | Greps tried: `origin\|referer\|csrf` (case-insensitive) across `app/web/**/*.py` → only dev-CORS config `app/web/main.py:134-138`; no middleware/dependency rejecting cross-origin on mutating routes (`execute.py`, `action.py`, `review.py`). Item 2 also open: `allowed_roots` still runtime state seeded empty then manifest-derived (`app/web/main.py:43,61`, `app/web/routes/execute.py:5,48`) — no independent OS-configured allow-list, no documented trust-boundary decision. No `#662` code-comment tags found. | no |
| #661 | Scan SSE lifecycle — no error on server-drop, status stuck after success | **implemented-admin-open** | Both halves fixed. (1) Connection-drop watchdog: `frontend/src/hooks/useScanSSE.ts:11-15,54-139` (timer rearmed on every event incl. ping, synthesizes connection-lost; `#661` cited in file). (2) Stale status: `frontend/src/components/ScanDialog.tsx:285,296` — finished/cancelled paths now call `resetScan()`. Commits `2bc8ee5`, `dd1f161` (both grep `#661`). Component tests exist (`ScanDialog.test.tsx`). | yes |
| #658 | JsonSettings.set clobbers non-dict intermediate node | **real-gap** | `infrastructure/settings.py:32-40` unchanged: `if not isinstance(node.get(part), dict): node[part] = {}` — still silently overwrites a scalar intermediate, no raise. Greps tried: read-back of `settings.py:25-46`; `raise\|TypeError` in the file (none in `set()`); no `#658` tag anywhere. Web exposure via `PATCH /api/settings` allowlist unchanged. | no |
| #653 | make JsonSettings.save atomic (temp + os.replace) | **real-gap** | `infrastructure/settings.py:42-46` — `save()` is still plain `open("w")` + `json.dump`. Greps tried: `os.replace\|fsync\|tmp` in `infrastructure/settings.py` → zero hits; no `#653` tag. (Contrast: `scanner/manifest.py` has the atomic pattern this issue asks to copy.) | no |
| #652 | web scan must read back hash-pool/read-knee calibration cache | **implemented-admin-open** | `app/web/models.py:230-254` — `WebScanRequest.to_config` now mirrors the Qt dialog: loads `scan.hash_pool_cache` keyed by `hash_pool_fingerprint(...)` (:246-251) and `scan.read_knee_cache` (:254), populating rates/knees on cache hit. Fixing commit `bae8acb` "fix(web-scan): read back hash-pool/read-knee calibration + exif_workers". | yes |
| #651 | fold apply_auto_select into the manifest write transaction (option-a) | **implemented-admin-open** | `scanner/manifest.py:52-57` — `write_manifest(..., keepers=, non_keepers_for_delete=)` with `#651` design comment at :73-81 (decisions applied inside the same atomic write, `keepers=None` = byte-identical legacy path). Caller wired: `core/app_service/scan_runner.py:1212-1217`. Commits `c9c45f3` (fix) + `abacb84` (docs/testing coverage note); `tests/test_scan_runner.py` asserts non-keeper delete decisions (:262+). | yes |
| #647 | Web-port program epic & tracker (Phase 0–4) | **tracker/epic** | Umbrella for #641-#646; stays open until PR #640 (integration) is ready. Substantive status: Phases 0/1 done, Phase 2 done, Phase 3 in progress, Phase 4 not started — its checkbox list is stale (shows #641/#642/#643 unchecked though shipped). | no |
| #646 | Phase 4 — Playwright cutover + remove PySide6 + blocking gates | **tracker/epic** | Substantive status: **not started** — `requirements.txt:1` still `PySide6>=6.6`, `app/views/` still present, Qt qa-batch still the CI gate (`.github/workflows/qa-batch.yml`). | no |
| #645 | Phase 3 — remaining dialogs + OS integration | **tracker/epic** | Substantive status: **largely done, tail remains** — ExecuteDialog/SettingsDialog/ScanDialog(+FsBrowser browse)/ContextMenu/confirm dialogs exist under `frontend/src/components/`, `/api/reveal` in `app/web/routes/execute.py`, i18n delivery via `app/web/routes/i18n.py` (#668); open tail = #669 (string migration) + #673 (menu long-tail) + unassigned-concerns doc sign-off. | no |
| #644 | Phase 2 — React frontend core (tree, preview, pywebview) | **tracker/epic** | Substantive status: **substantively done** — `frontend/src/components/ResultTree.tsx`, `PreviewPane.tsx`, `FullResViewer.tsx`, `GroupGrid.tsx`, `MenuBar.tsx` + component tests; `pywebview>=6.2` in `requirements.txt:5`; execute backend shipped (Phase 2C1, per #662 context). Checkboxes stale. | no |
| #643 | Phase 1 — scan API + SSE + image serving + Playwright bootstrap | **tracker/epic** | Substantive status: **substantively done** — `app/web/routes/scan.py` (POST /api/scan + SSE + cancel, commit `ab041cd`), `app/web/routes/image.py`, Playwright harness live at `qa/web/` (`_pw.py`, `_batch.py`, `scenario_map.yml`, `scenarios/`). Checkboxes stale. | no |
| #642 | Phase 0 — headless-core extraction (MVC seam) | **tracker/epic** | Substantive status: **substantively done** — `core/app_service/` fully populated (scan_runner, cancel_token `_CancelToken`, dtos, review/execute/action services, path_safety, fs_browse); modules document + enforce zero PySide6 (`core/app_service/action_service.py:6`); Qt app consumes it. Checkboxes stale. | no |
| #641 | widen CI workflow triggers to the web-port integration base | **implemented-admin-open** | Acceptance met by removing branch restrictions entirely: `.github/workflows/pr-gates.yml`, `qa-batch.yml`, `tests.yml` all run on `pull_request:` with no `branches:` filter ("Run on every PR regardless of target branch" comments in each); `news-gate.yml` likewise all-PR; so sub-PRs targeting `docs/web-port-feasibility` are CI-gated today (observed on shipped phase PRs). | yes |
| #622 | Preview/cache redesign — viewport-bounded sink, byte-budget LRU | **partial** | Phase 1 DONE: commits `9187360` (viewport cap + embedded JPEG + modal full-res viewer) + `f1d1795` (async res + status notice); byte-budget tier caches `infrastructure/image_service.py:68-69,225` (64 MB thumb / 192 MB preview, RAM-scaled); `app/views/dialogs/full_res_viewer.py` exists. Phase 2 NOT done: `app/views/preview_coordinator.py` and `infrastructure/device_key.py` do not exist (ls confirms); Phase 3 real-NAS verification not evidenced. Stays open for Phases 2-3. | no |


---

<!-- addendum-2026-07-16 -->

# 2026-07-16 re-audit deltas

This matrix's baseline was commit `d3eb09c`. The tree has since moved to
`6d5e3ce` (5 merged phase PRs ahead). The rows above are **left unchanged
as the dated historical record** — this section lists what the
[2026-07-16 goal audit](web-port-goal-audit-2026-07-16.md) (adversarial
recheck detail incorporated) found has changed since, so a reader isn't
misled by a stale verdict cell. Cite this section, not a silently-edited
row above, when reporting current status.

- **#662 (T2-4, was real-gap) — SHIPPED + CLOSED.** `app/web/security.py`
  now implements `OriginHostGuard` (same-origin check on `Origin` with a
  dev-origin allowlist, `Referer` fallback, explicit rejection of literal
  `Origin: null`), wired as ASGI middleware at `app/web/main.py:148-155`.
  Shipped via PR #777, commit `6d5e3ce` "feat(web-security): Origin/Host
  CSRF guard on mutating routes (#662)". The item-2 sub-item
  (`allowed_roots`/manifest-root trust boundary) is resolved by a
  documented decision in the tech-design's security addendum
  (`docs/design/web-port-tech-design.md`, unassigned-concerns section);
  the owner did not veto that resolution at the #777 review. If the owner
  instead wants the trust-boundary sub-item tracked as further work, file
  a follow-up issue — it is not re-opened here.
- **#658 / #653 (T2-4, were real-gap) — FIXED + CLOSED.** Both landed in
  commit `61e0c3f` "fix(settings): raise on non-dict intermediate + atomic
  save (#658, #653) (#775)": `infrastructure/settings.py`'s `set()` now
  raises `TypeError` naming the conflicting path instead of silently
  clobbering a non-dict intermediate node (#658), and `save()` now writes
  to a sibling `.tmp` file + `fsync` + `os.replace` (#653), matching
  `scanner/manifest.py`'s existing atomic pattern.
- **#678 (T2-3, was tracker/partial) — item D closed; net 5/6 clusters
  done.** Item D (multi-row selection + List menu) shipped its remaining
  List-menu half via commit `506735f` "feat(web): List menu — Remove from
  List menu-bar entry (#678-D) (#776)" — `frontend/src/components/MenuBar.tsx:157-170`
  now renders a real `MENU_LIST` dropdown wired to `onRemoveFromList`.
  Only item C (inline pre-scan path validation) remains a genuine
  declined/deferred gap; A/B/D/E/F are now all done.
- **#673 (T2-4, was partial) — List sub-item shipped.** Of the 5 unported
  menu items this row's evidence originally listed (Save Manifest /
  Execute Selected / Remove from List / Log menu / Exit), "Remove from
  List" is now shipped — same commit `506735f` as the #678-D item above.
  The row's overall **partial** verdict stands: Save Manifest, Log menu,
  and Exit remain unported.
- **#744 (T2-1, was partial) — 3 more commits landed, divergence-list
  artifact still the open deliverable.** Beyond the button-row fix
  (`2394320`) already recorded above, `apply_best_copy` shipped
  (commits `a829b95`/`3c530b9`, "feat(web): apply best-copy decisions to a
  group via context menu (#744)") plus a doc-hygiene commit
  (`dc46d43`/`c893d1a`) flagging tech-design §5 stale — see the §5.7
  correction in `docs/design/web-port-tech-design.md:2076-2079`. The
  issue's actual deliverable — a full reconciliation pass producing a
  structured divergence list with owner keep/fix sign-off — still does
  not exist (`grep -rln "Daylight|Aperture" docs/` finds only the two
  audit docs already on record). Verdict stays **partial**.
- **#737 (T2-1, was partial) — evidence correction, verdict unchanged.**
  This row's "quick levers done, proper fix not done" categorization is
  still accurate, but the specific "no cache found" phrasing in a prior
  evidence pass was wrong even at the matrix's own audit date:
  `infrastructure/transcode_service.py:96,104`'s `sha1(source_path|mtime_ns)`-keyed
  cache predates this matrix's baseline (`d3eb09c`) — it was never new.
  The **partial** categorization itself is correct (no `-progress` pipe
  or fMP4 streaming exists); only the evidence text describing what
  already existed was in error.
- **Endorsed-omission #2 (List menu) — "scheduled" is now "shipped".**
  The G1 sign-off table's row 2 fallback text ("scheduled to be
  implemented in P3.2(c) instead of endorsed") is fulfilled: commit
  `506735f` (#678-D / #776, same commit as above) ships the List-menu
  entry. This is no longer a pending-schedule note.
- **P-1 contradiction (ctx-apply-best-copy vs the tech-design §5.7
  staleness note) — resolved in favor of the code.** The tech-design
  doc's "shipped only as a no-op stub" claim for `ctx-apply-best-copy`
  was itself stale/false; the feature is fully implemented (see the #744
  entry above and the corrected note at
  `docs/design/web-port-tech-design.md:2076-2079`). `qa/web/scenario_map.yml`
  s72 (`status: done`) was accurate; the design doc was the stale side of
  the contradiction.
